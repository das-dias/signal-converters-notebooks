{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Mixed-Signal Converters Course","text":"<p>Educational Jupyter Notebooks for the Mixed-Signal Converters course at the Faculty of Sciences and Technology, NOVA University of Lisbon (FCT-NOVA).</p> <p>This collection covers the systems theory, design, and simulation of Nyquist data-rate Digital-to-Analog (DAC) and Analog-to-Digital Converters (ADC) using Python.</p> <p>Future Content</p> <p>The current notebooks focus exclusively on Nyquist-rate converter architectures (flash, SAR, pipeline, resistive/capacitive/current-steering DACs). Content on oversampled converters \u2014 sigma-delta modulators, noise shaping, decimation filters \u2014 is planned for future additions.</p>"},{"location":"#practical-classes","title":"Practical Classes","text":"Class Topic 1. Signal Representation Time/frequency domain, Fourier series, windowing, white noise 2. Ideal ADC/DAC Modeling Lambda-style transfer functions, binary/decimal conversion 3. Linear &amp; Non-linear Errors Offset, gain, DNL, INL for DAC and ADC 4. DAC/ADC Error Exercises SNR computation, non-ideal DAC, spectral analysis 5. Noise Modeling Jitter, thermal, quantization noise 6. Resistive DAC Modeling Thermometer, binary weighted, R-2R, Monte Carlo 7. Charge Redistribution DAC Capacitive DAC, parasitic effects 8. Current Steering DAC Binary/unitary current steering, statistical analysis 9. Resistive ADC Architectures Flash, 2-step sub-ranging Flash ADC 10. Capacitive ADC (SAR) SAR ADC modeling"},{"location":"#projects","title":"Projects","text":"Project Topic Pipeline SAR ADC 2-step loop-unrolled pipeline SAR ADC with digital error correction"},{"location":"#getting-started","title":"Getting Started","text":""},{"location":"#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.11+ (managed with uv)</li> <li>just task runner (<code>brew install just</code>)</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>git clone https://github.com/das-dias/signal-converters-notebooks.git\ncd signal-converters-notebooks\nuv sync\n</code></pre>"},{"location":"#running-notebooks","title":"Running Notebooks","text":"<pre><code>uv run jupyter lab\n</code></pre>"},{"location":"#key-dependencies","title":"Key Dependencies","text":"<ul> <li>NumPy / SciPy \u2014 Signal processing, FFT, window functions</li> <li>Matplotlib + SciencePlots \u2014 Publication-quality plots</li> <li>Seaborn \u2014 Statistical visualizations</li> </ul>"},{"location":"notebooks/practical_class_1/","title":"1. Signal Representation","text":"<pre><code>import numpy as np\nfrom numpy.random import normal, uniform\nimport matplotlib.pyplot as plt\nfrom scipy import signal\nimport seaborn as sns\nimport scienceplots  # noqa: F401\n\nplt.style.use([\"science\", \"notebook\"])\n%matplotlib inline\n</code></pre> <pre><code>Np = 1001\nFs = 100e3\nTs = 1 / Fs\ntD = np.arange(0, Np) * Ts\n</code></pre> <pre><code>Amp1 = 1\nf1 = 1e3\nphase1 = 0\nphase1_rad = np.deg2rad(phase1)\nx1 = Amp1 * np.sin(2 * np.pi * f1 * tD + phase1_rad)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(10, 4))\nax.plot(tD, x1, lw=2, ls=\"-\", color=\"b\", ms=3, label=r\"$x_1(t)$\")\nax.grid(True)\nax.set_ylabel(\"Voltage (V)\")\nax.set_xlabel(\"Time (s)\")\nax.legend()\nplt.show()\n</code></pre> <pre><code>Amp2 = 1\nf2 = 0.5e3\nphase2 = 0\nphase2_rad = np.deg2rad(phase2)\nx2 = Amp2 * np.sin(2 * np.pi * f2 * tD + phase2_rad)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(10, 4))\nax.plot(tD, x2, lw=2, ls=\"-\", color=\"r\", ms=3, label=r\"$x_2(t)$\")\nax.grid(True)\nax.set_ylabel(\"Voltage (V)\")\nax.set_xlabel(\"Time (s)\")\nax.legend()\nplt.show()\n</code></pre> <pre><code>Amp2 = 0.5\nf2 = 2 * f1\nphase2_rad = np.deg2rad(0)\nx2 = Amp2 * np.sin(2 * np.pi * f2 * tD + phase2_rad)\n\nAmp3 = 0.2\nf3 = 3 * f1\nphase3_rad = np.deg2rad(0)\nx3 = Amp3 * np.sin(2 * np.pi * f3 * tD + phase3_rad)\n</code></pre> <p>Observe how the superposition of harmonics shapes the resulting waveform. Summing \\(x_1\\) with its odd harmonic \\(x_3\\) begins to approximate a square wave \u2014 a direct consequence of the Fourier series, where a square wave is composed exclusively of odd harmonics.</p> <pre><code>fig, ax = plt.subplots(3, 1, figsize=(10, 10))\nax[0].plot(tD, x1, lw=2, ls=\"-\", color=\"b\", marker=\"None\", ms=3, label=\"x1\")\nax[0].plot(tD, x2, lw=2, ls=\"-\", color=\"g\", marker=\"None\", ms=3, label=\"x2\")\nax[0].plot(tD, x3, lw=2, ls=\"-\", color=\"r\", marker=\"None\", ms=3, label=\"x3\")\nax[0].set_title(\"x1(t), x3(t)\")\nax[0].legend(frameon=True, facecolor=\"pink\", edgecolor=\"none\")\nax[0].grid(True)\nax[0].set_ylabel(\"Voltage (V)\")\nax[1].plot(tD, x1 + x2, lw=2, ls=\"-\", color=\"k\", marker=\"None\", ms=3)\nax[1].set_title(\"x1(t) + x2(t)\")\nax[1].grid(True)\nax[1].set_ylabel(\"Voltage (V)\")\nax[2].plot(tD, x1 + x3, lw=2, ls=\"-\", color=\"k\", marker=\"None\", ms=3)\nax[2].set_title(\"x1(t) + x3(t)\")\nax[2].grid(True)\nax[2].set_ylabel(\"Voltage (V)\")\nax[2].set_xlabel(\"Time (s)\")\nfig.tight_layout()\nplt.show()\n</code></pre> <p>The FFT output is normalized by dividing by the number of points \\(N_p\\), converting the transform from total energy to per-sample amplitude. The power spectrum is then \\(|X(f)|^2\\).</p> <pre><code>yt = x1 + x2 + x3\nyf = np.abs(np.fft.fftshift(np.fft.fft(yt) / Np))  # [V]\nfD = np.fft.fftshift(np.fft.fftfreq(Np, Ts))  # [Hz]\npower_yf = yf * yf  # [V^2]\npower_yf_dB = 10 * np.log10(power_yf)  # [dB]\n\nyf2 = np.abs(np.fft.fftshift(np.fft.fft(x1) / Np))  # [V]\npower_yf2 = yf2 * yf2  # [V^2]\npower_yf2_dB = 10 * np.log10(power_yf2)  # [dB]\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(10, 5))\nf_scale = 1e3  # [Hz]\np_scale = 1  # [dB]\nax[0].stem(\n    fD[fD &amp;gt;= 0] / f_scale,\n    power_yf_dB[fD &amp;gt;= 0] / p_scale,\n    bottom=np.min(power_yf_dB / p_scale),\n    linefmt=\"b-\",\n    markerfmt=\"none\",\n    basefmt=\"r-\",\n)\nax[0].set_xlabel(\"Frequency (KHz)\")\nax[0].set_ylabel(\"Power (dB)\")\nax[0].set_title(\"Frequency spectrum of x1+x2+x3\")\nf_lim = 4e3 / f_scale\nax[0].set_xlim([0, f_lim])\nax[0].set_ylim([-40, 0])\nax[1].plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    power_yf2_dB[fD &amp;gt;= 0] / p_scale,\n    ls=\"-\",\n    color=\"k\",\n    marker=\"none\",\n)\nax[1].set_xlabel(\"Frequency (KHz)\")\nax[1].set_ylabel(\"Power (dB)\")\nax[1].set_xlim([0, f_lim])\nfig.tight_layout()\n</code></pre> <pre><code>window = signal.windows.blackmanharris(Np)\nfig, ax = plt.subplots(1, figsize=(4, 4))\nax.set_xlabel(\"Samples\")\nax.set_ylabel(\"Window Amplitude\")\nplt.plot(window)\n</code></pre> <pre>\n<code>[&lt;matplotlib.lines.Line2D at 0x115e38150&gt;]</code>\n</pre> <pre><code>ytbm = yt * signal.windows.blackmanharris(Np)\nyfbm = np.abs(np.fft.fftshift(np.fft.fft(ytbm) / Np))  # [V]\npower_yfbm = yfbm * yfbm  # [V^2]\npower_yfbm_dB = 10 * np.log10(power_yfbm)  # [dB]\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(10, 5))\nf_scale = 1e3  # [Hz]\np_scale = 1  # [dB]\nax[0].stem(\n    fD[fD &amp;gt;= 0] / f_scale,\n    power_yf_dB[fD &amp;gt;= 0] / p_scale,\n    bottom=np.min(power_yf_dB / p_scale),\n    linefmt=\"b-\",\n    markerfmt=\"none\",\n    basefmt=\"r-\",\n)\nax[0].set_xlabel(\"Frequency (KHz)\")\nax[0].set_ylabel(\"Power (dB)\")\nax[0].set_title(\"Frequency spectrum of x1+x2+x3\")\nf_lim = 4e3 / f_scale\nax[0].set_xlim([0, f_lim])\nax[0].set_ylim([-40, 0])\nax[1].plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    power_yf_dB[fD &amp;gt;= 0] / p_scale,\n    ls=\"-\",\n    color=\"k\",\n    marker=\"none\",\n)\nax[1].plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    power_yfbm_dB[fD &amp;gt;= 0] / p_scale,\n    ls=\"-\",\n    color=\"r\",\n    marker=\"none\",\n)\nax[1].set_xlabel(\"Frequency (KHz)\")\nax[1].set_ylabel(\"Power (dB)\")\nax[1].set_xlim([0, f_lim])\nfig.tight_layout()\n</code></pre> <pre><code>nbits = 4\nvref = 0.9  # [V]\nvlsb = vref / (2**nbits)  # [V]\n\nvqmean = 0  # [V]\nvqvar = (vlsb**2) / 12  # [V^2]\nvqstddev = np.sqrt(vqvar)  # [V]\n\nvq = uniform(-vqstddev, vqstddev, Np)  # [V]\n\nvqf = np.abs(np.fft.fftshift(np.fft.fft(vq) / Np))  # [V]\npqf = vqf * vqf  # [V^2]\npqf_dB = 10 * np.log10(pqf)  # [dB]\n\nfig, ax = plt.subplots(3, 1, figsize=(10, 10))\nax[0].plot(tD, vq, ls=\"-\", color=\"k\", marker=\"none\")\nax[0].set_xlabel(\"Time (s)\")\nax[0].set_ylabel(\"Voltage (V)\")\nax[0].set_title(\"Quantization noise signal\")\nax[1].plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    pqf_dB[fD &amp;gt;= 0] / p_scale,\n    ls=\"-\",\n    color=\"gray\",\n    marker=\"none\",\n)\nax[1].set_xlabel(\"Frequency (KHz)\")\nax[1].set_ylabel(\"Power (dB)\")\nax[1].set_title(\"Quantization noise signal's frequency spectrum\")\nsns.histplot(vq, ax=ax[2], color=\"gray\")\nax[2].hlines(\n    y=np.mean(np.histogram(vq, bins=10)[0]), xmin=-vqstddev, xmax=vqstddev, color=\"r\"\n)\nax[2].set_xlabel(\"Voltage (V)\")\nax[2].set_ylabel(\"Count\")\nax[2].set_title(\"Quantization noise signal's histogram\")\nax[0].grid()\nax[1].grid()\nax[2].grid(False)\nfig.tight_layout()\n</code></pre> <pre><code>vqmean = 0  # [V]\nvqvar = (vlsb**2) / 12  # [V^2]\nvqstddev = np.sqrt(vqvar)  # [V]\n\nvq = normal(vqmean, vqstddev, Np)  # [V]\n\nvqf = np.abs(np.fft.fftshift(np.fft.fft(vq) / Np))  # [V]\npqf = vqf * vqf  # [V^2]\npqf_dB = 10 * np.log10(pqf)  # [dB]\n\nfig, ax = plt.subplots(3, 1, figsize=(10, 10))\nax[0].plot(tD, vq, ls=\"-\", color=\"k\", marker=\"none\")\nax[0].set_xlabel(\"Time (s)\")\nax[0].set_ylabel(\"Voltage (V)\")\nax[0].set_title(\"Gaussian noise signal\")\nax[1].plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    pqf_dB[fD &amp;gt;= 0] / p_scale,\n    ls=\"-\",\n    color=\"gray\",\n    marker=\"none\",\n)\nax[1].set_xlabel(\"Frequency (KHz)\")\nax[1].set_ylabel(\"Power (dB)\")\nax[1].set_title(\"Gaussian noise signal's frequency spectrum\")\nsns.histplot(vq, ax=ax[2], kde=True, stat=\"density\", color=\"gray\")\nax[2].set_xlabel(\"Voltage (V)\")\nax[2].set_ylabel(\"Density\")\nax[2].set_title(\"Gaussian noise signal's histogram\")\nax[0].grid()\nax[1].grid()\nax[2].grid(False)\nfig.tight_layout()\n</code></pre> <pre><code>Amp1 = 0.2  # [V]\nx1 = Amp1 * np.sin(2 * np.pi * f1 * tD + phase1_rad)  # [V]\nvq = uniform(-vqstddev, vqstddev, Np)  # [V]\nst = x1 + vq\n\nstf = np.abs(np.fft.fftshift(np.fft.fft(st) / Np))  # [V]\nptf = stf * stf  # [V^2]\nptf_dB = 10 * np.log10(ptf)  # [dB]\n\nfig, ax = plt.subplots(3, 1, figsize=(10, 10))\nax[0].plot(tD, st, ls=\"-\", color=\"k\", marker=\"none\")\nax[0].set_xlabel(\"Time (s)\")\nax[0].set_ylabel(\"Voltage (V)\")\nax[0].set_title(r\"Quantized signal - $s(t)$\")\nax[1].plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    ptf_dB[fD &amp;gt;= 0] / p_scale,\n    ls=\"-\",\n    color=\"gray\",\n    marker=\"none\",\n)\nax[1].set_xlabel(\"Frequency (KHz)\")\nax[1].set_ylabel(\"Power (dB)\")\nax[1].set_title(\"Quantized signal's frequency spectrum\")\nsns.histplot(st, ax=ax[2], color=\"gray\")\nax[2].set_xlabel(\"Voltage (V)\")\nax[2].set_ylabel(\"Count\")\nax[2].set_title(\"Quantized signal's histogram\")\nax[0].grid()\nax[1].grid()\nax[2].grid(False)\nfig.tight_layout()\n</code></pre>"},{"location":"notebooks/practical_class_1/#practical-class-1-signal-representation","title":"Practical Class 1 \u2014 Signal Representation","text":"<p>This notebook reviews time-domain and frequency-domain signal representations using Python and NumPy, in the context of mixed-signal converter design.</p>"},{"location":"notebooks/practical_class_1/#1-signal-representation-in-the-time-domain","title":"1. Signal Representation in the Time Domain","text":"<p>A signal \u2014 whether a voltage, current, or digital code \u2014 can be decomposed into a sum of fundamental trigonometric functions (Fourier's theorem). Two key aspects are explored:</p> <ol> <li>Fourier decomposition \u2014 any periodic signal can be represented as an infinite sum of sines and cosines at harmonic frequencies.</li> <li>Differential signaling \u2014 processing signals differentially cancels even-order harmonic distortion, improving linearity.</li> </ol>"},{"location":"notebooks/practical_class_1/#simulation-time-base","title":"Simulation Time Base","text":"<p>The number of simulation points \\(N_p\\) controls the time-domain resolution and directly affects computation time and memory usage. The discrete time vector is constructed as \\(t[n] = n \\cdot T_s\\) for \\(n = 0, 1, \\ldots, N_p - 1\\), where \\(T_s = 1/F_s\\) is the sampling period.</p> <p>&gt; MATLAB equivalent: <code>(0:Np-1)</code> corresponds to <code>np.arange(0, Np)</code> in NumPy.</p>"},{"location":"notebooks/practical_class_1/#harmonics-and-fourier-series-intuition","title":"Harmonics and Fourier Series Intuition","text":"<p>A signal can be represented as a sum of sinusoids at integer multiples of a fundamental frequency \\(f_1\\). The second harmonic has frequency \\(f_2 = 2f_1\\) and the third has \\(f_3 = 3f_1\\). Below, <code>x2</code> and <code>x3</code> are redefined as the second and third harmonics of <code>x1</code>.</p>"},{"location":"notebooks/practical_class_1/#observations","title":"Observations","text":"<ul> <li>Odd harmonics preserve the waveform symmetry about its mean value. Square waves, for instance, contain only odd harmonics.</li> <li>Even harmonics break this symmetry, skewing the positive and negative half-cycles unequally.</li> </ul>"},{"location":"notebooks/practical_class_1/#differential-signaling","title":"Differential Signaling","text":"<p>A signal processing or conversion chain should be differential: the two signal paths carry waveforms that are \\(180^\\circ\\) out of phase. Differential operation naturally cancels even-order harmonic distortion, improving output linearity.</p> <p>Demonstration. Consider a single-tone input \\(x(t) = A \\sin(2\\pi f_1 t)\\) passed through a weakly nonlinear system modeled by a power series:</p> \\[ y(t) = \\alpha_1 x(t) + \\alpha_2 x^2(t) + \\alpha_3 x^3(t) \\] <p>Applying trigonometric power-reduction identities, the output contains components at DC, \\(f_1\\), \\(2f_1\\), and \\(3f_1\\):</p> \\[ y(t) = \\frac{\\alpha_2 A^2}{2} + \\left(\\alpha_1 A + \\frac{3\\alpha_3 A^3}{4}\\right)\\sin(2\\pi f_1 t) - \\frac{\\alpha_2 A^2}{2}\\cos(4\\pi f_1 t) - \\frac{\\alpha_3 A^3}{4}\\sin(6\\pi f_1 t) \\] <p>For the anti-phase input \\(x'(t) = -x(t)\\), the even-order terms (\\(\\alpha_2\\) contributions) remain unchanged in sign, while the odd-order terms flip. Subtracting:</p> \\[ y(t) - y'(t) = 2\\left(\\alpha_1 A + \\frac{3\\alpha_3 A^3}{4}\\right)\\sin(2\\pi f_1 t) - \\frac{\\alpha_3 A^3}{2}\\sin(6\\pi f_1 t) \\] <p>The even-order (\\(2f_1\\)) component and DC offset are eliminated, reducing total harmonic distortion.</p>"},{"location":"notebooks/practical_class_1/#2-signal-representation-in-the-frequency-domain","title":"2. Signal Representation in the Frequency Domain","text":"<p>The Fourier transform maps a time-domain signal to its frequency-domain representation. The composite signal \\(x_1 + x_2 + x_3\\) should produce three spectral peaks at \\(f_1\\), \\(2f_1\\), and \\(3f_1\\), corresponding to the fundamental, second, and third harmonics.</p>"},{"location":"notebooks/practical_class_1/#spectral-leakage","title":"Spectral Leakage","text":"<p>The spectrum of \\(x_1\\) alone shows energy spreading across many frequency bins instead of concentrating at a single bin. This is spectral leakage, caused by the finite observation window truncating the signal.</p>"},{"location":"notebooks/practical_class_1/#21-windowing","title":"2.1 Windowing","text":"<p>Multiplying the time-domain signal by a window function (e.g., Blackman-Harris) tapers the signal edges toward zero, concentrating each harmonic's energy into fewer bins and dramatically reducing leakage. The trade-off is a wider main lobe: the Blackman-Harris window spreads each harmonic across approximately 5 bins.</p> <p>&gt; Note: An alternative (and often preferred) approach is coherent sampling \u2014 choosing \\(F_s\\) and \\(N_p\\) such that the signal completes an integer number of cycles within the observation window, with the number of cycles being coprime to \\(N_p\\). Both techniques can be combined.</p>"},{"location":"notebooks/practical_class_1/#3-white-noise-and-quantization-noise","title":"3. White Noise and Quantization Noise","text":"<p>Accurate simulation of signal converters requires modeling noise. Quantizing an ideal, noiseless signal introduces a quantization error:</p> \\[ s_Q(t) = s(t) + \\varepsilon_Q(t) \\tag{1} \\] <p>where \\(s(t)\\) is the input signal, \\(\\varepsilon_Q(t)\\) is the quantization error, and \\(s_Q(t)\\) is the quantized output.</p> <p>In practice, analog signals always carry some noise \\(n(t)\\). When the superimposed noise power is sufficiently large relative to the quantization step, the quantization error is well approximated by white noise \u2014 a signal with flat (constant) power spectral density across the entire bandwidth:</p> \\[ s_Q(t) = \\bigl(x(t) + n(t)\\bigr) + \\varepsilon_Q(t) = x(t) + v_{NQ}(t) \\tag{2} \\] <p>where \\(v_{NQ}(t) \\approx n(t) + \\varepsilon_Q(t)\\) is the effective noise, commonly modeled as a uniform distribution over \\([-V_\\text{LSB}/2,\\; V_\\text{LSB}/2]\\).</p>"},{"location":"notebooks/practical_class_1/#31-uniform-quantization-noise","title":"3.1 Uniform (Quantization) Noise","text":"<p>A quantizer with \\(N\\) bits and reference voltage \\(V_\\text{ref}\\) has a step size \\(V_\\text{LSB} = V_\\text{ref} / 2^N\\). The quantization noise variance is \\(\\sigma_Q^2 = V_\\text{LSB}^2 / 12\\).</p>"},{"location":"notebooks/practical_class_1/#32-gaussian-noise","title":"3.2 Gaussian Noise","text":"<p>When quantization noise is combined with other noise sources (thermal, flicker), the resulting distribution tends toward Gaussian by the central limit theorem. Below, the same noise parameters are used but with a normal distribution.</p>"},{"location":"notebooks/practical_class_1/#33-signal-with-superimposed-quantization-noise","title":"3.3 Signal with Superimposed Quantization Noise","text":"<p>Adding quantization noise to a sinusoidal signal produces a noisy waveform whose spectrum shows the signal tone rising above a flat noise floor \u2014 the hallmark of a quantized signal.</p>"},{"location":"notebooks/practical_class_10/","title":"10. Capacitive ADC (SAR)","text":"<pre><code>import numpy as np\nimport seaborn as sns\n\nsns.set_theme()\nsns.set_palette(\"colorblind\")\n\nfrom numpy.random import normal\nimport matplotlib.pyplot as plt\nfrom utils import bin2dec\n\n%matplotlib inline\n</code></pre> <pre><code>Np = 2**13\nFs = 1e9  # Hz\nTs = 1 / Fs  # s\ntD = np.arange(0, Np) * Ts\n</code></pre> <pre><code># Volts\nV = 1.0\nmV = V * 1e-3\nuV = V * 1e-6\nnV = V * 1e-9\n\n# Seconds\ns = 1.0\nms = s * 1e-3\nus = s * 1e-6\nns = s * 1e-9\n\n# Frequency\nHz = 1.0\nkHz = Hz * 1e3\nMHz = Hz * 1e6\nGHz = Hz * 1e9\n\n# Length\nm = 1.0\nmm = m * 1e-3\num = m * 1e-6\nnm = m * 1e-9\n\n# Farad\nF = 1.0\npF = F * 1e-12\nfF = F * 1e-15\n</code></pre> <pre><code>ENABLE_NOISE = False\nvn_mean = 0.0 * V\nvn_std = 1 * mV\nvn_in = normal(vn_mean, vn_std, Np)\n</code></pre> <pre><code>if ENABLE_NOISE:\n    fig, ax = plt.subplots(figsize=(4, 3))\n    sns.histplot(\n        vn_in, ax=ax, kde=True, stat=\"probability\", color=\"gray\", label=\"noise signal\"\n    )\n    ax.set_xlabel(\"Voltage [V]\")\n    ax.set_ylabel(\"Probability density\")\n    ax.set_title(\"Histogram of the noise signal\")\n    plt.show()\n</code></pre> <pre><code>vdd = 1.0\nvss = 0.0\nvcm = np.mean([vdd, vss])\nvref = vdd\n\nA1 = vref\nfbin = 7\nf1 = fbin * (Fs / Np)\nph1 = 0\nvin1t = A1 * np.sin(2 * np.pi * f1 * tD + ph1) + vcm\n\nif ENABLE_NOISE:\n    vin1t = vin1t + vn_in\n\nvin1f = np.abs(np.fft.fftshift(np.fft.fft(vin1t) / Np))  # V\nfD = np.fft.fftshift(np.fft.fftfreq(Np, Ts))  # Hz\npower_vin1f = vin1f * vin1f  # V^2\npower_vinf_dB = 10 * np.log10(power_vin1f)  # dB\n</code></pre> <pre>\n<code>/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/3528769582.py:18: RuntimeWarning: divide by zero encountered in log10\n  power_vinf_dB = 10 * np.log10(power_vin1f)  # dB\n</code>\n</pre> <pre><code>NUM_CIRCUITS = 1000\nCAP_MISMATCH_MEAN = 0.0\nCAP_MISMATCH_STD = 0.01  # 1% capacitance mismatch\nNUM_COMP_OFSTS = 5\nMAX_COMP_OFST = 5 * mV\n</code></pre> <pre><code>vref = vdd\nadc_gain = 1\nvref = vref / adc_gain\nvrefp = vcm + vref / 2\nvrefn = vcm - vref / 2\nNbits = 10\nvlsb = vref / (2**Nbits)\n\nvin1 = np.linspace(vrefn, vrefp, Np)\n</code></pre> <pre><code>avg_linearity = np.zeros(NUM_COMP_OFSTS)\nlinearities = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\n\nadc_offsets = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\nadc_eos = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\nadc_gainerr = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\n\ncomp_offsets = np.linspace(0, MAX_COMP_OFST, NUM_COMP_OFSTS)\n\nfor i, comp_ofst in enumerate(comp_offsets):\n    for j in range(NUM_CIRCUITS):\n        unitary_caps = np.ones(2**Nbits)\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        caps = unitary_caps * (1 + cap_error)\n        num_caps_cdac = Nbits\n        cres = caps[-1]\n        cdac = np.array(\n            [np.sum(caps[2**k - 1 : 2 ** (k + 1) - 1]) for k in range(num_caps_cdac)]\n        )\n        cdac = cdac[::-1]\n\n        ctotal = np.sum(cdac) + cres\n\n        vcomp = vcm + normal(0.0, comp_ofst, 1)[0]\n\n        vx = np.zeros((Nbits + 2, len(vin1)))\n\n        # Bottom plate sampling: top plate connected to V_CM\n        vx[0, :] = vcm\n\n        # Charge redistribution: transfer input to top plate\n        vx[1, :] = (vcm - vin1) + vrefn\n\n        dout = np.zeros((Nbits, len(vin1)))\n        dout_neg = 1 - dout\n\n        cvrefp = dout.T @ cdac\n        cvrefn = dout_neg.T @ cdac + cres\n\n        cvrefp_old = cvrefp\n        cvrefn_old = cvrefn\n\n        charge_vrefp = np.zeros((Nbits + 2, len(vin1)))\n\n        for k in range(2, Nbits + 2):\n            dout[k - 2, :] = 1\n            dout_neg[k - 2, :] = 1 - dout[k - 2, :]\n            cvrefp = dout.T @ cdac\n            cvrefn = dout_neg.T @ cdac + cres\n\n            vx[k, :] = (\n                vx[k - 1, :]\n                + (vrefn * (cvrefn - cvrefn_old) + vrefp * (cvrefp - cvrefp_old))\n                / ctotal\n            )\n            charge_vrefp[k, :] = (cvrefp - cvrefp_old) * vrefp\n\n            dout[k - 2, :] = (vx[k, :] &amp;lt; vcomp).astype(int)\n            dout_neg[k - 2, :] = 1 - dout[k - 2, :]\n            cvrefp_old = cvrefp\n            cvrefn_old = cvrefn\n\n        vres = vx[-1, :] - vrefn\n        energy_vrefp = np.sum(charge_vrefp, axis=0) * vrefp\n\n        dout_dec = bin2dec(dout, width=Nbits, reverse=False)\n        dout_dec_trans = dout_dec[np.where(np.diff(dout_dec) &amp;gt; 0)]\n        vtrans = vin1[np.where(np.diff(dout_dec) &amp;gt; 0)]\n\n        vlsb_measured = (vtrans[-1] - vtrans[0]) / (2**Nbits - 2)\n        dnl = np.diff(vtrans) / vlsb_measured - 1\n        inl = np.array([0] + np.cumsum(dnl).tolist())\n        adc_linearity = Nbits - np.log2(np.max(inl) - np.min(inl))\n        linearities[j, i] = adc_linearity\n\n        offset = (vtrans[-1] - vtrans[0]) / vlsb\n        adc_offsets[j, i] = offset\n        eos_error = (vtrans[-1] - (2**Nbits - 1) * vlsb) / vlsb\n        adc_eos[j, i] = eos_error\n        gain_error = 100 * ((vtrans[-1] - vtrans[0]) / ((2**Nbits - 1) * vlsb - 0) - 1)\n        adc_gainerr[j, i] = gain_error\n\n    avg_linearity[i] = np.mean(linearities[:, i])\n\nprint(\"CDAC Capacitance     : \", cdac, \" (F/F)\")\nprint(\"Residue Capacitance  : \", cres, \" (F/F)\")\nprint(\"Total Capacitance    : \", ctotal, \" (F/F)\")\nprint(\"Comparator Threshold : \", vcomp / mV, \" (mV)\")\n</code></pre> <pre>\n<code>CDAC Capacitance     :  [512.20012164 256.0804031  128.1030161   64.0465703   31.97900129\n  16.06635553   8.01879906   4.01249643   2.01340414   0.98733064]  (F/F)\nResidue Capacitance  :  0.9941349958990241  (F/F)\nTotal Capacitance    :  1024.5016332320467  (F/F)\nComparator Threshold :  500.3991278836326  (mV)\n</code>\n</pre> <pre><code>fig, ax = plt.subplots(figsize=(6, 6))\nax.step(vin1, dout_dec)\nax.set_xlabel(\"Vin\")\nax.set_ylabel(\"Dout\")\nax.grid(True)\nax.set_title(\"ADC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'ADC Transfer Function')</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 2, figsize=(8, 5))\nidx = 0\n\nax[0, 0].step(\n    list(range(len(vx[:, idx][1:]))),\n    vx[:, idx][1:],\n    label=\"vx\",\n    color=\"blue\",\n    where=\"mid\",\n)\nax[0, 0].set_xlabel(\"conversion step\")\nax[0, 0].set_ylabel(\"voltage [V]\")\nax[0, 0].set_title(f\"Node voltages of the ADC\\n for the input code {dout[:, idx]}\")\nax[0, 0].grid(True)\nax[0, 0].legend()\n\nvx_trans = np.diff(vx[:, idx][1:]) / vlsb_measured\nax[0, 1].bar(list(range(len(vx_trans))), vx_trans, label=\"vx\", color=\"gray\", alpha=0.6)\nax[0, 1].set_xlabel(\"conversion step transition\")\nax[0, 1].set_ylabel(\"LSB (V/V)\")\nax[0, 1].set_title(\n    f\"Node voltage transitions of the ADC\\n for the input code {dout[:, idx]}\"\n)\nax[0, 1].grid(True)\nax[0, 1].legend()\n\nax[1, 0].step(\n    list(range(len(charge_vrefp))),\n    charge_vrefp[:, idx],\n    label=\"charge drawn from vref\",\n    color=\"blue\",\n    where=\"mid\",\n)\nax[1, 0].set_xlabel(\"conversion step\")\nax[1, 0].set_ylabel(\"normalized charge [C/C]\")\nax[1, 0].set_title(f\"Charge drawn from Vref \\n for the input code {dout[:, idx]}\")\nax[1, 0].grid(True)\n\nax[1, 1].step(\n    vin1 / mV, energy_vrefp, label=\"energy drawn from vref\", color=\"gray\", where=\"mid\"\n)\nax[1, 1].set_xlabel(\"Vin [mV]\")\nax[1, 1].set_ylabel(\"normalized charge [C/C]\")\nax[1, 1].set_title(\"Charge drawn from Vref - first sub ADC\")\nax[1, 1].grid(True)\n\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(5, 4))\nax[1].step(dout_dec_trans, inl, label=\"inl\", color=\"gray\")\nax[1].set_xlabel(\"Code\")\nax[1].set_ylabel(\"INL [LSB]\")\nax[1].set_title(\"INL\")\nax[1].grid(True)\nax[1].legend()\nax[0].step(dout_dec_trans[:-1], dnl, label=\"dnl\", color=\"gray\")\nax[0].set_xlabel(\"Code\")\nax[0].set_ylabel(\"DNL [LSB]\")\nax[0].set_title(\"DNL\")\nax[0].grid(True)\nax[0].legend()\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 4, figsize=(10, 3))\nax[0].hist(adc_offsets.flatten(), bins=10, label=\"offset\", color=\"gray\")\nax[0].set_xlabel(\"Offset [LSB]\")\nax[0].set_ylabel(\"Occurences\")\nax[0].set_title(\"Offset Error\")\nax[0].grid(True)\n\nax[1].hist(adc_eos.flatten(), bins=10, label=\"eos\", color=\"gray\")\nax[1].set_xlabel(\"EOS [LSB]\")\nax[1].set_ylabel(\"Occurences\")\nax[1].set_title(\"EOS Error\")\nax[1].grid(True)\n\nax[2].hist(adc_gainerr.flatten(), bins=10, label=\"gain\", color=\"gray\")\nax[2].set_xlabel(\"Gain Error [%]\")\nax[2].set_ylabel(\"Occurences\")\nax[2].set_title(\"Gain Error\")\nax[2].grid(True)\n\nax[3].hist(linearities.flatten(), bins=10, label=\"linearity\", color=\"gray\")\nax[3].set_xlabel(\"Linearity [Bit]\")\nax[3].set_ylabel(\"Occurences\")\nax[3].set_title(\"ADC Linearity\")\nax[3].grid(True)\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(5, 3))\nax.bar(comp_offsets / mV, avg_linearity, label=\"avg linearity\", color=\"gray\")\nax.set_xlabel(\"Comparator Offset [mV]\")\nax.set_ylabel(\"Avg. Linearity [LSB]\")\nax.set_title(\"Avg. Linearity vs. Comparator Offset\")\nax.grid(True)\n</code></pre> <pre><code>vref = vdd\nadc_gain = 1\nvref = vref / adc_gain\nvrefp = vcm + vref / 2\nvrefn = vcm - vref / 2\nNbits = 10\nvlsb = vref / (2**Nbits)\n\nvin1 = np.linspace(-vref, vref, Np)\nvin1p = vin1 / 2 + vcm\nvin1n = -vin1 / 2 + vcm\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(5, 3))\nax.plot(vin1 / mV, vin1p / mV, label=\"vin1p\", color=\"red\")\nax.plot(vin1 / mV, vin1n / mV, label=\"vin1n\", color=\"black\")\nax.set_xlabel(\"Differential input [mV]\")\nax.set_ylabel(\"Single ended inputs [mV]\")\nax.set_title(\"Ramp input of the Differential SAR-ADC\")\nax.grid(True)\nax.legend()\n</code></pre> <pre>\n<code>&lt;matplotlib.legend.Legend at 0x119ba8550&gt;</code>\n</pre> <pre><code>avg_linearity = np.zeros(NUM_COMP_OFSTS)\nlinearities = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\n\nadc_offsets = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\nadc_eos = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\nadc_gainerr = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\n\ncomp_offsets = np.linspace(0, MAX_COMP_OFST, NUM_COMP_OFSTS)\n\nvin1p = vin1 / 2 + vcm\nvin1n = -vin1 / 2 + vcm\n\nfor i, comp_ofst in enumerate(comp_offsets):\n    for j in range(NUM_CIRCUITS):\n        # The first sampling phase obtains the MSB directly,\n        # so the MSB capacitor is not needed in the CDAC array.\n\n        # Positive comparator input rail\n        num_caps_cdac = Nbits\n        unitary_caps = np.ones(2 ** (num_caps_cdac))\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        caps = unitary_caps * (1 + cap_error)\n        pos_cres = caps[-1]\n        pos_cdac = np.array(\n            [np.sum(caps[2**k - 1 : 2 ** (k + 1) - 1]) for k in range(num_caps_cdac)]\n        )\n        pos_cdac = pos_cdac[::-1]\n        pos_ctotal = np.sum(pos_cdac) + pos_cres\n\n        # Negative comparator input rail\n        unitary_caps = np.ones(2 ** (num_caps_cdac))\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        caps = unitary_caps * (1 + cap_error)\n        neg_cres = caps[-1]\n        neg_cdac = np.array(\n            [np.sum(caps[2**k - 1 : 2 ** (k + 1) - 1]) for k in range(num_caps_cdac)]\n        )\n        neg_cdac = neg_cdac[::-1]\n        neg_ctotal = np.sum(neg_cdac) + neg_cres\n\n        vcomp_offset = normal(0.0, comp_ofst, 1)[0]\n\n        vp = np.zeros((Nbits + 2, len(vin1)))\n        vn = np.zeros((Nbits + 2, len(vin1)))\n\n        # Bottom plate sampling\n        vp[0, :] = vcm\n        vn[0, :] = vcm\n\n        dout = np.zeros((Nbits, len(vin1)))\n        dout_neg = 1 - dout\n\n        # Positive rail capacitance partitioning\n        cpos_vrefp = dout.T @ pos_cdac\n        cpos_vrefn = dout_neg.T @ pos_cdac + pos_cres\n\n        # Negative rail capacitance partitioning\n        cneg_vrefp = dout_neg.T @ neg_cdac + neg_cres\n        cneg_vrefn = dout.T @ neg_cdac\n\n        cpos_vrefp_old = cpos_vrefp\n        cpos_vrefn_old = cpos_vrefn\n        cneg_vrefp_old = cneg_vrefp\n        cneg_vrefn_old = cneg_vrefn\n\n        charge_vrefp = np.zeros((Nbits + 1, len(vin1)))\n        charge_vrefp[0, :] = vrefp * (cpos_vrefp_old + cneg_vrefp_old)\n\n        # Transfer input to top plates\n        vp[1, :] = (vcm - vin1p) + vrefn\n        vn[1, :] = (vcm - vin1n) + vrefp\n\n        for k in range(2, Nbits + 2):\n            dout[k - 2, :] = 1\n            dout_neg[k - 2, :] = 1 - dout[k - 2, :]\n\n            cpos_vrefp = dout.T @ pos_cdac\n            cpos_vrefn = dout_neg.T @ pos_cdac + pos_cres\n\n            cneg_vrefp = dout_neg.T @ neg_cdac + neg_cres\n            cneg_vrefn = dout.T @ neg_cdac\n\n            # Charge conservation\n            vp[k, :] = (\n                vp[k - 1, :]\n                + (\n                    vrefn * (cpos_vrefn - cpos_vrefn_old)\n                    + vrefp * (cpos_vrefp - cpos_vrefp_old)\n                )\n                / pos_ctotal\n            )\n            vn[k, :] = (\n                vn[k - 1, :]\n                + (\n                    vrefn * (cneg_vrefn - cneg_vrefn_old)\n                    + vrefp * (cneg_vrefp - cneg_vrefp_old)\n                )\n                / neg_ctotal\n            )\n\n            charge_vrefp[k - 1, :] = vrefp * (cpos_vrefp + cneg_vrefp)\n\n            dout[k - 2, :] = (vp[k, :] - vn[k, :] &amp;lt; vcomp_offset).astype(int)\n            dout_neg[k - 2, :] = 1 - dout[k - 2, :]\n\n            cpos_vrefp_old = cpos_vrefp\n            cpos_vrefn_old = cpos_vrefn\n            cneg_vrefp_old = cneg_vrefp\n            cneg_vrefn_old = cneg_vrefn\n\n        vres = vp[-1, :] - vn[-1, :]\n        energy_vrefp = np.sum(charge_vrefp, axis=0) * vrefp\n\n        dout_dec = bin2dec(dout, width=Nbits, reverse=False)\n        dout_dec_trans = dout_dec[np.where(np.diff(dout_dec) &amp;gt; 0)]\n        vtrans = vin1[np.where(np.diff(dout_dec) &amp;gt; 0)]\n\n        vlsb_measured = (vtrans[-1] - vtrans[0]) / (2**Nbits - 2)\n        dnl = np.diff(vtrans) / vlsb_measured - 1\n        inl = np.array([0] + np.cumsum(dnl).tolist())\n        adc_linearity = Nbits - np.log2(np.max(inl) - np.min(inl))\n        linearities[j, i] = adc_linearity\n\n        offset = (vtrans[-1] - vtrans[0]) / vlsb\n        adc_offsets[j, i] = offset\n        eos_error = (vtrans[-1] - (2**Nbits - 1) * vlsb) / vlsb\n        adc_eos[j, i] = eos_error\n        gain_error = 100 * ((vtrans[-1] - vtrans[0]) / ((2**Nbits - 1) * vlsb - 0) - 1)\n        adc_gainerr[j, i] = gain_error\n\n    avg_linearity[i] = np.mean(linearities[:, i])\n\nprint(\"CDAC Capacitance     : \", pos_cdac, \" (F/F)\")\nprint(\"Residue Capacitance  : \", pos_cres, \" (F/F)\")\nprint(\"Total Capacitance    : \", pos_ctotal, \" (F/F)\")\nprint(\"Comparator Threshold : \", vcomp_offset / mV, \" (mV)\")\n</code></pre> <pre>\n<code>CDAC Capacitance     :  [511.85996627 255.95618543 127.9286841   64.04352154  31.91981059\n  15.98723317   8.00000967   3.99066547   1.98596984   0.99178713]  (F/F)\nResidue Capacitance  :  1.0070679306729295  (F/F)\nTotal Capacitance    :  1023.6709011336675  (F/F)\nComparator Threshold :  -2.854649666473991  (mV)\n</code>\n</pre> <pre><code>fig, ax = plt.subplots(figsize=(6, 6))\nax.step(vin1, dout_dec)\nax.set_xlabel(\"Vin\")\nax.set_ylabel(\"Dout\")\nax.grid(True)\nax.set_title(\"ADC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'ADC Transfer Function')</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 2, figsize=(8, 5))\nidx = 0\n\nax[0, 0].step(\n    list(range(len(vp[:, idx][0:]))),\n    vp[:, idx][0:],\n    label=\"vp\",\n    color=\"blue\",\n    where=\"mid\",\n)\nax[0, 0].step(\n    list(range(len(vn[:, idx][0:]))),\n    vn[:, idx][0:],\n    label=\"vn\",\n    color=\"orange\",\n    where=\"mid\",\n)\nax[0, 0].set_xlabel(\"conversion step\")\nax[0, 0].set_ylabel(\"voltage [V]\")\nax[0, 0].set_title(f\"Node voltages of the ADC\\n for the input code {dout[:, idx]}\")\nax[0, 0].grid(True)\nax[0, 0].legend()\n\nvn_trans = np.diff(vn[:, idx][0:]) / vlsb\nvp_trans = np.diff(vp[:, idx][0:]) / vlsb\nax[0, 1].bar(\n    list(range(len(vn_trans))), vn_trans, label=\"vn\", color=\"orange\", alpha=0.6\n)\nax[0, 1].bar(list(range(len(vp_trans))), vp_trans, label=\"vp\", color=\"blue\", alpha=0.6)\nax[0, 1].set_xlabel(\"conversion step transition\")\nax[0, 1].set_ylabel(\"LSB (V/V)\")\nax[0, 1].set_title(\n    f\"Node voltage transitions of the ADC\\n for the input code {dout[:, idx]}\"\n)\nax[0, 1].grid(True)\nax[0, 1].legend()\n\nax[1, 0].step(\n    list(range(len(charge_vrefp))),\n    charge_vrefp[:, idx],\n    label=\"charge drawn from vref\",\n    color=\"blue\",\n    where=\"mid\",\n)\nax[1, 0].set_xlabel(\"conversion step\")\nax[1, 0].set_ylabel(\"normalized charge [C/C]\")\nax[1, 0].set_title(f\"Charge drawn from Vref \\n for the input code {dout[:, idx]}\")\nax[1, 0].grid(True)\n\nax[1, 1].step(\n    vin1 / mV, energy_vrefp, label=\"energy drawn from vref\", color=\"gray\", where=\"mid\"\n)\nax[1, 1].set_xlabel(\"Vin [mV]\")\nax[1, 1].set_ylabel(\"normalized charge [C/C]\")\nax[1, 1].set_title(\"Charge drawn from Vref - first sub ADC\")\nax[1, 1].grid(True)\n\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(5, 4))\nax[1].step(dout_dec_trans, inl, label=\"inl\", color=\"gray\")\nax[1].set_xlabel(\"Code\")\nax[1].set_ylabel(\"INL [LSB]\")\nax[1].set_title(\"INL\")\nax[1].grid(True)\nax[1].legend()\nax[0].step(dout_dec_trans[:-1], dnl, label=\"dnl\", color=\"gray\")\nax[0].set_xlabel(\"Code\")\nax[0].set_ylabel(\"DNL [LSB]\")\nax[0].set_title(\"DNL\")\nax[0].grid(True)\nax[0].legend()\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 4, figsize=(10, 3))\nax[0].hist(adc_offsets.flatten(), bins=10, label=\"offset\", color=\"gray\")\nax[0].set_xlabel(\"Offset [LSB]\")\nax[0].set_ylabel(\"Occurences\")\nax[0].set_title(\"Offset Error\")\nax[0].grid(True)\n\nax[1].hist(adc_eos.flatten(), bins=10, label=\"eos\", color=\"gray\")\nax[1].set_xlabel(\"EOS [LSB]\")\nax[1].set_ylabel(\"Occurences\")\nax[1].set_title(\"EOS Error\")\nax[1].grid(True)\n\nax[2].hist(adc_gainerr.flatten(), bins=10, label=\"gain\", color=\"gray\")\nax[2].set_xlabel(\"Gain Error [%]\")\nax[2].set_ylabel(\"Occurences\")\nax[2].set_title(\"Gain Error\")\nax[2].grid(True)\n\nax[3].hist(linearities.flatten(), bins=10, label=\"linearity\", color=\"gray\")\nax[3].set_xlabel(\"Linearity [Bit]\")\nax[3].set_ylabel(\"Occurences\")\nax[3].set_title(\"ADC Linearity\")\nax[3].grid(True)\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(5, 3))\nax.bar(comp_offsets / mV, avg_linearity, label=\"avg linearity\", color=\"gray\")\nax.set_xlabel(\"Comparator Offset [mV]\")\nax.set_ylabel(\"Avg. Linearity [LSB]\")\nax.set_title(\"Avg. Linearity vs. Comparator Offset\")\nax.grid(True)\n</code></pre> <pre><code>vref = vdd\nadc_gain = 1\nvref = vref / adc_gain\nvrefp = vcm + vref / 2\nvrefn = vcm - vref / 2\nNbits = 10\nvlsb = vref / (2**Nbits)\n\nvin1 = np.linspace(-vref, vref, Np)\nvin1p = vin1 / 2 + vcm\nvin1n = -vin1 / 2 + vcm\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(5, 3))\nax.plot(vin1 / mV, vin1p / mV, label=\"vin1p\", color=\"red\")\nax.plot(vin1 / mV, vin1n / mV, label=\"vin1n\", color=\"black\")\nax.set_xlabel(\"Differential input [mV]\")\nax.set_ylabel(\"Single ended inputs [mV]\")\nax.set_title(\"Ramp input of the Differential SAR-ADC\")\nax.grid(True)\nax.legend()\n</code></pre> <pre>\n<code>&lt;matplotlib.legend.Legend at 0x119b10bd0&gt;</code>\n</pre> <pre><code>avg_linearity = np.zeros(NUM_COMP_OFSTS)\nlinearities = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\n\nadc_offsets = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\nadc_eos = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\nadc_gainerr = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\n\ncomp_offsets = np.linspace(0, MAX_COMP_OFST, NUM_COMP_OFSTS)\n\nvin1p = vin1 / 2 + vcm\nvin1n = -vin1 / 2 + vcm\n\nfor i, comp_ofst in enumerate(comp_offsets):\n    for j in range(NUM_CIRCUITS):\n        # First sampling phase yields MSB directly;\n        # CDAC uses N-1 binary-weighted caps + residue.\n\n        # Positive comparator input rail\n        num_caps_cdac = Nbits - 1\n        unitary_caps = np.ones(2 ** (num_caps_cdac))\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        caps = unitary_caps * (1 + cap_error)\n        pos_cres = caps[-1]\n        pos_cdac = np.array(\n            [np.sum(caps[2**k - 1 : 2 ** (k + 1) - 1]) for k in range(num_caps_cdac)]\n        )\n        pos_cdac = pos_cdac[::-1]\n        pos_ctotal = np.sum(pos_cdac) + pos_cres\n\n        # Negative comparator input rail\n        unitary_caps = np.ones(2 ** (num_caps_cdac))\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        caps = unitary_caps * (1 + cap_error)\n        neg_cres = caps[-1]\n        neg_cdac = np.array(\n            [np.sum(caps[2**k - 1 : 2 ** (k + 1) - 1]) for k in range(num_caps_cdac)]\n        )\n        neg_cdac = neg_cdac[::-1]\n        neg_ctotal = np.sum(neg_cdac) + neg_cres\n\n        vcomp_offset = normal(0.0, comp_ofst, 1)[0]\n\n        vp = np.zeros((Nbits + 1, len(vin1)))\n        vn = np.zeros((Nbits + 1, len(vin1)))\n\n        # Top plate sampling\n        vp[0, :] = vin1p\n        vn[0, :] = vin1n\n\n        dout = np.zeros((Nbits, len(vin1)))\n        dout_neg = 1 - dout\n\n        charge_vrefp = np.zeros((Nbits + 1, len(vin1)))\n\n        # Monotonic switching: positive rail caps connected to Vrefp initially\n        cpos_vrefp = (1 - dout)[:-1, :].T @ pos_cdac + pos_cres\n        cpos_vrefn = dout[:-1, :].T @ pos_cdac\n\n        cneg_vrefp = dout_neg[:-1, :].T @ neg_cdac + neg_cres\n        cneg_vrefn = (1 - dout_neg)[:-1, :].T @ neg_cdac\n\n        cpos_vrefp_old = cpos_vrefp\n        cpos_vrefn_old = cpos_vrefn\n        cneg_vrefp_old = cneg_vrefp\n        cneg_vrefn_old = cneg_vrefn\n\n        charge_vrefp[0, :] = (vp[0, :] - vrefp) * cpos_vrefp_old + (\n            vn[0, :] - vrefp\n        ) * cneg_vrefp_old\n\n        # MSB from first comparison (top plate sampling)\n        dout[0, :] = (vp[0, :] - vn[0, :] &amp;gt; vcomp_offset).astype(int)\n        # dout_neg controls the negative array independently\n        dout_neg[0, :] = dout[0, :]\n\n        for k in range(1, Nbits):\n            cpos_vrefp = (1 - dout)[:-1, :].T @ pos_cdac + pos_cres\n            cpos_vrefn = dout[:-1, :].T @ pos_cdac\n\n            cneg_vrefp = dout_neg[:-1, :].T @ neg_cdac + neg_cres\n            cneg_vrefn = (1 - dout_neg)[:-1, :].T @ neg_cdac\n\n            # Charge conservation\n            vp[k, :] = (\n                vp[k - 1, :]\n                + (\n                    vrefn * (cpos_vrefn - cpos_vrefn_old)\n                    + vrefp * (cpos_vrefp - cpos_vrefp_old)\n                )\n                / pos_ctotal\n            )\n            vn[k, :] = (\n                vn[k - 1, :]\n                + (\n                    vrefn * (cneg_vrefn - cneg_vrefn_old)\n                    + vrefp * (cneg_vrefp - cneg_vrefp_old)\n                )\n                / neg_ctotal\n            )\n\n            charge_vrefp[k, :] = (vp[k, :] - vrefp) * cpos_vrefp + (\n                vn[k, :] - vrefp\n            ) * cneg_vrefp\n\n            dout[k, :] = (vp[k, :] - vn[k, :] &amp;gt; vcomp_offset).astype(int)\n            dout_neg[k, :] = dout[k, :]\n\n            cpos_vrefp_old = cpos_vrefp\n            cpos_vrefn_old = cpos_vrefn\n            cneg_vrefp_old = cneg_vrefp\n            cneg_vrefn_old = cneg_vrefn\n\n        k += 1\n\n        # Program residue capacitor with the last bit\n        cpos_vrefp = (1 - dout)[:-1, :].T @ pos_cdac + ((1 - dout)[-1, :] * pos_cres)\n        cpos_vrefn = dout[:-1, :].T @ pos_cdac + (dout[-1, :] * pos_cres)\n\n        cneg_vrefp = dout_neg[:-1, :].T @ neg_cdac + (dout_neg[-1, :] * neg_cres)\n        cneg_vrefn = (1 - dout_neg)[:-1, :].T @ neg_cdac + (\n            (1 - dout_neg)[-1, :] * neg_cres\n        )\n\n        vp[k, :] = (\n            vp[k - 1, :]\n            + (\n                vrefn * (cpos_vrefn - cpos_vrefn_old)\n                + vrefp * (cpos_vrefp - cpos_vrefp_old)\n            )\n            / pos_ctotal\n        )\n        vn[k, :] = (\n            vn[k - 1, :]\n            + (\n                vrefn * (cneg_vrefn - cneg_vrefn_old)\n                + vrefp * (cneg_vrefp - cneg_vrefp_old)\n            )\n            / neg_ctotal\n        )\n\n        charge_vrefp[k, :] = (vp[k, :] - vrefp) * cpos_vrefp + (\n            vn[k, :] - vrefp\n        ) * cneg_vrefp\n\n        vres = vp[-1, :] - vn[-1, :]\n        energy_vrefp = np.sum(charge_vrefp, axis=0) * vrefp\n\n        dout_dec = bin2dec(dout, width=Nbits, reverse=False)\n        dout_dec_trans = dout_dec[np.where(np.diff(dout_dec) &amp;gt; 0)]\n        vtrans = vin1[np.where(np.diff(dout_dec) &amp;gt; 0)]\n\n        vlsb_measured = (vtrans[-1] - vtrans[0]) / (2**Nbits - 2)\n        dnl = np.diff(vtrans) / vlsb_measured - 1\n        inl = np.array([0] + np.cumsum(dnl).tolist())\n        adc_linearity = Nbits - np.log2(np.max(inl) - np.min(inl))\n        linearities[j, i] = adc_linearity\n\n        offset = (vtrans[-1] - vtrans[0]) / vlsb\n        adc_offsets[j, i] = offset\n        eos_error = (vtrans[-1] - (2**Nbits - 1) * vlsb) / vlsb\n        adc_eos[j, i] = eos_error\n        gain_error = 100 * (\n            (vtrans[-1] - vtrans[0]) / ((2**Nbits - 1) * vlsb_measured - 0) - 1\n        )\n        adc_gainerr[j, i] = gain_error\n\n    avg_linearity[i] = np.mean(linearities[:, i])\n\nprint(\"CDAC Capacitance     : \", pos_cdac, \" (F/F)\")\nprint(\"Residue Capacitance  : \", pos_cres, \" (F/F)\")\nprint(\"Total Capacitance    : \", pos_ctotal, \" (F/F)\")\nprint(\"Comparator Threshold : \", vcomp_offset / mV, \" (mV)\")\n</code></pre> <pre>\n<code>CDAC Capacitance     :  [255.93617444 127.93626831  63.93344617  32.0043581   16.00273843\n   7.97347465   4.00249335   1.98003869   0.99558172]  (F/F)\nResidue Capacitance  :  0.9876759069744782  (F/F)\nTotal Capacitance    :  511.7522497666552  (F/F)\nComparator Threshold :  0.16740071658510738  (mV)\n</code>\n</pre> <pre><code>fig, ax = plt.subplots(figsize=(6, 6))\nax.step(vin1, dout_dec)\nax.set_xlabel(\"Vin\")\nax.set_ylabel(\"Dout\")\nax.grid(True)\nax.set_title(\"ADC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'ADC Transfer Function')</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 2, figsize=(8, 5))\nidx = 1\n\nax[0, 0].step(\n    list(range(len(vp[:, idx][0:]))),\n    vp[:, idx][0:],\n    label=\"vp\",\n    color=\"blue\",\n    where=\"mid\",\n)\nax[0, 0].step(\n    list(range(len(vn[:, idx][0:]))),\n    vn[:, idx][0:],\n    label=\"vn\",\n    color=\"orange\",\n    where=\"mid\",\n)\nax[0, 0].set_xlabel(\"conversion step\")\nax[0, 0].set_ylabel(\"voltage [V]\")\nax[0, 0].set_title(f\"Node voltages of the ADC\\n for the input code {dout[:, idx]}\")\nax[0, 0].grid(True)\nax[0, 0].legend()\n\nvn_trans = np.diff(vn[:, idx][0:]) / vlsb\nvp_trans = np.diff(vp[:, idx][0:]) / vlsb\nax[0, 1].bar(\n    list(range(len(vn_trans))), vn_trans, label=\"vn\", color=\"orange\", alpha=0.6\n)\nax[0, 1].bar(list(range(len(vp_trans))), vp_trans, label=\"vp\", color=\"blue\", alpha=0.6)\nax[0, 1].set_xlabel(\"conversion step transition\")\nax[0, 1].set_ylabel(\"LSB (V/V)\")\nax[0, 1].set_title(\n    f\"Node voltage transitions of the ADC\\n for the input code {dout[:, idx]}\"\n)\nax[0, 1].grid(True)\nax[0, 1].legend()\n\nax[1, 0].step(\n    list(range(len(charge_vrefp))),\n    charge_vrefp[:, idx],\n    label=\"charge drawn from vref\",\n    color=\"blue\",\n    where=\"mid\",\n)\nax[1, 0].set_xlabel(\"conversion step\")\nax[1, 0].set_ylabel(\"normalized charge [C/C]\")\nax[1, 0].set_title(f\"Charge drawn from Vref \\n for the input code {dout[:, idx]}\")\nax[1, 0].grid(True)\n\nax[1, 1].step(\n    vin1 / mV, energy_vrefp, label=\"energy drawn from vref\", color=\"gray\", where=\"mid\"\n)\nax[1, 1].set_xlabel(\"Vin [mV]\")\nax[1, 1].set_ylabel(\"normalized charge [C/C]\")\nax[1, 1].set_title(\"Charge drawn from Vref - first sub ADC\")\nax[1, 1].grid(True)\n\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(5, 4))\nax[1].step(dout_dec_trans, inl, label=\"inl\", color=\"gray\")\nax[1].set_xlabel(\"Code\")\nax[1].set_ylabel(\"INL [LSB]\")\nax[1].set_title(\"INL\")\nax[1].grid(True)\nax[1].legend()\nax[0].step(dout_dec_trans[:-1], dnl, label=\"dnl\", color=\"gray\")\nax[0].set_xlabel(\"Code\")\nax[0].set_ylabel(\"DNL [LSB]\")\nax[0].set_title(\"DNL\")\nax[0].grid(True)\nax[0].legend()\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 4, figsize=(10, 3))\nax[0].hist(adc_offsets.flatten(), bins=10, label=\"offset\", color=\"gray\")\nax[0].set_xlabel(\"Offset [LSB]\")\nax[0].set_ylabel(\"Occurences\")\nax[0].set_title(\"Offset Error\")\nax[0].grid(True)\n\nax[1].hist(adc_eos.flatten(), bins=10, label=\"eos\", color=\"gray\")\nax[1].set_xlabel(\"EOS [LSB]\")\nax[1].set_ylabel(\"Occurences\")\nax[1].set_title(\"EOS Error\")\nax[1].grid(True)\n\nax[2].hist(adc_gainerr.flatten(), bins=10, label=\"gain\", color=\"gray\")\nax[2].set_xlabel(\"Gain Error [%]\")\nax[2].set_ylabel(\"Occurences\")\nax[2].set_title(\"Gain Error\")\nax[2].grid(True)\n\nax[3].hist(linearities.flatten(), bins=10, label=\"linearity\", color=\"gray\")\nax[3].set_xlabel(\"Linearity [Bit]\")\nax[3].set_ylabel(\"Occurences\")\nax[3].set_title(\"ADC Linearity\")\nax[3].grid(True)\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(5, 3))\nax.bar(comp_offsets / mV, avg_linearity, label=\"avg linearity\", color=\"gray\")\nax.set_xlabel(\"Comparator Offset [mV]\")\nax.set_ylabel(\"Avg. Linearity [LSB]\")\nax.set_title(\"Avg. Linearity vs. Comparator Offset\")\nax.grid(True)\n</code></pre> <pre><code>vref = vdd\nadc_gain = 1\nvref = vref / adc_gain\nvrefp = vcm + vref / 2\nvrefn = vcm - vref / 2\nNbits_dac1 = 2\nNbits_dac2 = 3\nvlsb = vref / (2**Nbits)\n\nvin1 = np.linspace(-vref, vref, Np)\nvin1p = vin1 / 2 + vcm\nvin1n = -vin1 / 2 + vcm\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(5, 3))\nax.plot(vin1 / mV, vin1p / mV, label=\"vin1p\", color=\"red\")\nax.plot(vin1 / mV, vin1n / mV, label=\"vin1n\", color=\"black\")\nax.set_xlabel(\"Differential input [mV]\")\nax.set_ylabel(\"Single ended inputs [mV]\")\nax.set_title(\"Ramp input of the Differential SAR-ADC\")\nax.grid(True)\nax.legend()\n</code></pre> <pre>\n<code>&lt;matplotlib.legend.Legend at 0x11a0e8a10&gt;</code>\n</pre> <pre><code>avg_linearity = np.zeros(NUM_COMP_OFSTS)\nlinearities = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\n\nadc_offsets = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\nadc_eos = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\nadc_gainerr = np.zeros((NUM_CIRCUITS, NUM_COMP_OFSTS))\n\ncomp_offsets = np.linspace(0, MAX_COMP_OFST, NUM_COMP_OFSTS)\n\nvin1p = vin1 / 2 + vcm\nvin1n = -vin1 / 2 + vcm\n\nVERBOSE = False\n\nfor i, comp_ofst in enumerate(comp_offsets):\n    for j in range(NUM_CIRCUITS):\n        # First sampling phase yields MSB directly.\n\n        # Positive rail: first CDAC sub-array\n        num_caps_cdac1 = Nbits_dac1 - 1\n        num_caps_cdac2 = Nbits_dac2\n        unitary_caps = np.ones(2 ** (num_caps_cdac1) - 1)\n\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        caps = unitary_caps * (1 + cap_error)\n        pos_cdac1 = np.array(\n            [np.sum(caps[2**k - 1 : 2 ** (k + 1) - 1]) for k in range(num_caps_cdac1)]\n        )\n        pos_cdac1 = pos_cdac1[::-1]\n        pos_ctotal1 = np.sum(pos_cdac1)\n\n        # Positive rail: second CDAC sub-array\n        unitary_caps = np.ones(2 ** (num_caps_cdac2))\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        caps = unitary_caps * (1 + cap_error)\n        pos_cres = caps[-1]\n        pos_cdac2 = np.array(\n            [np.sum(caps[2**k - 1 : 2 ** (k + 1) - 1]) for k in range(num_caps_cdac2)]\n        )\n        pos_cdac2 = np.array(pos_cdac2[::-1].tolist() + [pos_cres])\n        pos_ctotal2 = np.sum(pos_cdac2)\n\n        # Voltage dividing capacitor (positive rail)\n        unitary_caps = np.ones(2 ** (num_caps_cdac1))\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        unitary_caps = unitary_caps * (1 + cap_error)\n        pos_cdiv = np.sum(unitary_caps)\n\n        pos_ctotal = pos_ctotal1 + pos_cdiv / (pos_cdiv + pos_ctotal2)\n\n        if VERBOSE:\n            print(\"Positive Cdac 1      : \", pos_cdac1, \" (F/F)\")\n            print(\"Positive Cdac 2      : \", pos_cdac2, \" (F/F)\")\n            print(\"Positive Cdac Div Cap: \", pos_cdiv, \" (F/F)\")\n\n        # Negative rail: first CDAC sub-array\n        unitary_caps = np.ones(2 ** (num_caps_cdac1) - 1)\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        caps = unitary_caps * (1 + cap_error)\n        neg_cdac1 = np.array(\n            [np.sum(caps[2**k - 1 : 2 ** (k + 1) - 1]) for k in range(num_caps_cdac1)]\n        )\n        neg_cdac1 = neg_cdac1[::-1]\n        neg_ctotal1 = np.sum(neg_cdac1)\n\n        # Negative rail: second CDAC sub-array\n        unitary_caps = np.ones(2 ** (num_caps_cdac2))\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        caps = unitary_caps * (1 + cap_error)\n        neg_cres = caps[-1]\n        neg_cdac2 = np.array(\n            [np.sum(caps[2**k - 1 : 2 ** (k + 1) - 1]) for k in range(num_caps_cdac2)]\n        )\n        neg_cdac2 = np.array(neg_cdac2[::-1].tolist() + [neg_cres])\n        neg_ctotal2 = np.sum(neg_cdac2)\n\n        # Voltage dividing capacitor (negative rail)\n        unitary_caps = np.ones(2 ** (num_caps_cdac1))\n        cap_error = normal(CAP_MISMATCH_MEAN, CAP_MISMATCH_STD, len(unitary_caps))\n        unitary_caps = unitary_caps * (1 + cap_error)\n        neg_cdiv = np.sum(unitary_caps)\n\n        neg_ctotal = neg_ctotal1 + neg_cdiv / (neg_cdiv + neg_ctotal2)\n\n        if VERBOSE:\n            print(\"Negative Cdac 1      : \", neg_cdac1, \" (F/F)\")\n            print(\"Negative Cdac 2      : \", neg_cdac2, \" (F/F)\")\n            print(\"Negative Cdac Div Cap: \", neg_cdiv, \" (F/F)\")\n\n        vcomp_offset = normal(0.0, comp_ofst, 1)[0]\n\n        vp = np.zeros((Nbits_dac1 + Nbits_dac1 + 2, len(vin1)))\n        vn = np.zeros((Nbits_dac1 + Nbits_dac1 + 2, len(vin1)))\n\n        dout = np.zeros((Nbits_dac1 + Nbits_dac2, len(vin1)))\n        dout_neg = np.zeros((Nbits_dac1 + Nbits_dac2, len(vin1)))\n\n        # Bottom plate sampling\n        vp[0, :] = vcm\n        vn[0, :] = vcm\n\n        # Positive rail capacitance partitioning\n        cpos1_vrefp = dout_neg[: Nbits_dac1 - 1, :].T @ pos_cdac1\n        cpos2_vrefp = dout_neg[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n        cpos2_vrefp = pos_cdiv / (pos_cdiv + cpos2_vrefp)\n\n        cpos1_vrefp_old = cpos1_vrefp\n        cpos2_vrefp_old = cpos2_vrefp\n\n        cpos1_vrefn = dout[: Nbits_dac1 - 1, :].T @ pos_cdac1\n        cpos2_vrefn = dout[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n        cpos2_vrefn = pos_cdiv / (pos_cdiv + cpos2_vrefn)\n\n        cpos1_vrefn_old = cpos1_vrefn\n        cpos2_vrefn_old = cpos2_vrefn\n\n        cpos1_vcm_old = pos_ctotal1 - cpos1_vrefp_old - cpos1_vrefn_old\n        cpos2_vcm_old = pos_ctotal2 - cpos2_vrefp_old - cpos2_vrefn_old\n\n        # Negative rail capacitance partitioning\n        cneg1_vrefp = dout[: Nbits_dac1 - 1, :].T @ neg_cdac1\n        cneg2_vrefp = dout[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n        cneg2_vrefp = neg_cdiv / (neg_cdiv + cneg2_vrefp)\n\n        cneg1_vrefp_old = cneg1_vrefp\n        cneg2_vrefp_old = cneg2_vrefp\n\n        cneg1_vrefn = dout_neg[: Nbits_dac1 - 1, :].T @ neg_cdac1\n        cneg2_vrefn = dout_neg[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n        cneg2_vrefn = neg_cdiv / (neg_cdiv + cneg2_vrefn)\n\n        cneg1_vrefn_old = cneg1_vrefn\n        cneg2_vrefn_old = cneg2_vrefn\n\n        cneg1_vcm_old = neg_ctotal - cneg1_vrefp_old - cneg1_vrefn_old\n        cneg2_vcm_old = neg_ctotal2 - cneg2_vrefp_old - cneg2_vrefn_old\n\n        # Charge redistribution\n        vp[1, :] = vcm - vin1p + vcm\n        vn[1, :] = vcm - vin1n + vcm\n\n        # First bit decided without DAC switching\n        dout[0, :] = (vp[1, :] - vn[1, :] &amp;lt; vcomp_offset).astype(int)\n        dout_neg[0, :] = 1 - dout[0, :]\n\n        charge_vrefp = np.zeros((Nbits_dac1 + Nbits_dac1 + 2, len(vin1)))\n\n        for k in range(2, Nbits_dac1 + Nbits_dac2 + 1):\n            # Positive rail\n            cpos1_vrefp = dout_neg[: Nbits_dac1 - 1, :].T @ pos_cdac1\n            cpos2_vrefp = dout_neg[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n            cpos2_vrefp = pos_cdiv / (pos_cdiv + cpos2_vrefp)\n\n            cpos1_vrefn = dout[: Nbits_dac1 - 1, :].T @ pos_cdac1\n            cpos2_vrefn = dout[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n            cpos2_vrefn = pos_cdiv / (pos_cdiv + cpos2_vrefn)\n\n            cpos1_vcm = pos_ctotal1 - cpos1_vrefp - cpos1_vrefn\n            cpos2_vcm = pos_ctotal2 - cpos2_vrefp - cpos2_vrefn\n\n            # Negative rail\n            cneg1_vrefp = dout[: Nbits_dac1 - 1, :].T @ neg_cdac1\n            cneg2_vrefp = dout[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n            cneg2_vrefp = neg_cdiv / (neg_cdiv + cneg2_vrefp)\n\n            cneg1_vrefn = dout_neg[: Nbits_dac1 - 1, :].T @ neg_cdac1\n            cneg2_vrefn = dout_neg[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n            cneg2_vrefn = neg_cdiv / (neg_cdiv + cneg2_vrefn)\n\n            cneg1_vcm = neg_ctotal - cneg1_vrefp - cneg1_vrefn\n            cneg2_vcm = neg_ctotal2 - cneg2_vrefp - cneg2_vrefn\n\n            # Charge conservation\n            vp[k, :] = (\n                vp[k - 1, :]\n                + (\n                    vrefn\n                    * (cpos1_vrefn + cpos2_vrefn - cpos1_vrefn_old - cpos2_vrefn_old)\n                    + vrefp\n                    * (cpos1_vrefp + cpos2_vrefp - cpos1_vrefp_old - cpos2_vrefp_old)\n                    + vcm * (cpos1_vcm + cpos2_vcm - cpos1_vcm_old - cpos2_vcm_old)\n                )\n                / pos_ctotal\n            )\n\n            vn[k, :] = (\n                vn[k - 1, :]\n                + (\n                    vrefn\n                    * (cneg1_vrefn + cneg2_vrefn - cneg1_vrefn_old - cneg2_vrefn_old)\n                    + vrefp\n                    * (cneg1_vrefp + cneg2_vrefp - cneg1_vrefp_old - cneg2_vrefp_old)\n                    + vcm * (cneg1_vcm + cneg2_vcm - cneg1_vcm_old - cneg2_vcm_old)\n                )\n                / neg_ctotal\n            )\n\n            dout[k - 1, :] = (vp[k, :] - vn[k, :] &amp;lt; vcomp_offset).astype(int)\n            dout_neg[k - 1, :] = 1 - dout[k - 1, :]\n\n            cpos1_vcm_old = cpos1_vcm\n            cpos2_vcm_old = cpos2_vcm\n            cpos1_vrefp_old = cpos1_vrefp\n            cpos2_vrefp_old = cpos2_vrefp\n            cpos1_vrefn_old = cpos1_vrefn\n            cpos2_vrefn_old = cpos2_vrefn\n\n            cneg1_vcm_old = cneg1_vcm\n            cneg2_vcm_old = cneg2_vcm\n            cneg1_vrefp_old = cneg1_vrefp\n            cneg2_vrefp_old = cneg2_vrefp\n            cneg1_vrefn_old = cneg1_vrefn\n            cneg2_vrefn_old = cneg2_vrefn\n\n        vres = vp[-1, :] - vn[-1, :]\n        energy_vrefp = np.sum(charge_vrefp, axis=0) * vrefp\n\n        dout_dec = bin2dec(dout, width=Nbits_dac1 + Nbits_dac2, reverse=False)\n        dout_dec_trans = dout_dec[np.where(np.diff(dout_dec) &amp;gt; 0)]\n        vtrans = vin1[np.where(np.diff(dout_dec) &amp;gt; 0)]\n\n        vlsb_measured = (vtrans[-1] - vtrans[0]) / (2**Nbits - 2)\n        dnl = np.diff(vtrans) / vlsb_measured - 1\n        inl = np.array([0] + np.cumsum(dnl).tolist())\n        adc_linearity = Nbits - np.log2(np.max(inl) - np.min(inl))\n        linearities[j, i] = adc_linearity\n\n        offset = (vtrans[-1] - vtrans[0]) / vlsb\n        adc_offsets[j, i] = offset\n        eos_error = (vtrans[-1] - (2**Nbits - 1) * vlsb) / vlsb\n        adc_eos[j, i] = eos_error\n        gain_error = 100 * ((vtrans[-1] - vtrans[0]) / ((2**Nbits - 1) * vlsb - 0) - 1)\n        adc_gainerr[j, i] = gain_error\n\n    avg_linearity[i] = np.mean(linearities[:, i])\n\nprint(\"CDAC Capacitance     : \", pos_cdac, \" (F/F)\")\nprint(\"Residue Capacitance  : \", pos_cres, \" (F/F)\")\nprint(\"Total Capacitance    : \", pos_ctotal, \" (F/F)\")\nprint(\"Comparator Threshold : \", vcomp_offset / mV, \" (mV)\")\n</code></pre> <pre>\n<code>/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:104: RuntimeWarning: divide by zero encountered in matmul\n  cpos2_vrefp = dout_neg[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:104: RuntimeWarning: overflow encountered in matmul\n  cpos2_vrefp = dout_neg[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:104: RuntimeWarning: invalid value encountered in matmul\n  cpos2_vrefp = dout_neg[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:111: RuntimeWarning: divide by zero encountered in matmul\n  cpos2_vrefn = dout[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:111: RuntimeWarning: overflow encountered in matmul\n  cpos2_vrefn = dout[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:111: RuntimeWarning: invalid value encountered in matmul\n  cpos2_vrefn = dout[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:122: RuntimeWarning: divide by zero encountered in matmul\n  cneg2_vrefp = dout[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:122: RuntimeWarning: overflow encountered in matmul\n  cneg2_vrefp = dout[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:122: RuntimeWarning: invalid value encountered in matmul\n  cneg2_vrefp = dout[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:129: RuntimeWarning: divide by zero encountered in matmul\n  cneg2_vrefn = dout_neg[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:129: RuntimeWarning: overflow encountered in matmul\n  cneg2_vrefn = dout_neg[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:129: RuntimeWarning: invalid value encountered in matmul\n  cneg2_vrefn = dout_neg[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:151: RuntimeWarning: divide by zero encountered in matmul\n  cpos2_vrefp = dout_neg[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:151: RuntimeWarning: overflow encountered in matmul\n  cpos2_vrefp = dout_neg[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:151: RuntimeWarning: invalid value encountered in matmul\n  cpos2_vrefp = dout_neg[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:156: RuntimeWarning: divide by zero encountered in matmul\n  cpos2_vrefn = dout[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:156: RuntimeWarning: overflow encountered in matmul\n  cpos2_vrefn = dout[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:156: RuntimeWarning: invalid value encountered in matmul\n  cpos2_vrefn = dout[Nbits_dac1 - 1 :, :].T @ pos_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:165: RuntimeWarning: divide by zero encountered in matmul\n  cneg2_vrefp = dout[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:165: RuntimeWarning: overflow encountered in matmul\n  cneg2_vrefp = dout[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:165: RuntimeWarning: invalid value encountered in matmul\n  cneg2_vrefp = dout[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:170: RuntimeWarning: divide by zero encountered in matmul\n  cneg2_vrefn = dout_neg[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:170: RuntimeWarning: overflow encountered in matmul\n  cneg2_vrefn = dout_neg[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:170: RuntimeWarning: invalid value encountered in matmul\n  cneg2_vrefn = dout_neg[Nbits_dac1 - 1 :, :].T @ neg_cdac2\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33446/1749062559.py:229: RuntimeWarning: divide by zero encountered in log2\n  adc_linearity = Nbits - np.log2(np.max(inl) - np.min(inl))\n</code>\n</pre> <pre>\n<code>CDAC Capacitance     :  [255.93617444 127.93626831  63.93344617  32.0043581   16.00273843\n   7.97347465   4.00249335   1.98003869   0.99558172]  (F/F)\nResidue Capacitance  :  0.9916489687630584  (F/F)\nTotal Capacitance    :  1.2116565558574097  (F/F)\nComparator Threshold :  0.3117950839782589  (mV)\n</code>\n</pre> <pre><code>fig, ax = plt.subplots(figsize=(6, 6))\nax.step(vin1, dout_dec)\nax.set_xlabel(\"Vin\")\nax.set_ylabel(\"Dout\")\nax.grid(True)\nax.set_title(\"ADC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'ADC Transfer Function')</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 2, figsize=(8, 5))\nidx = 0\n\nax[0, 0].step(\n    list(range(len(vp[:, idx][0:]))),\n    vp[:, idx][0:],\n    label=\"vp\",\n    color=\"blue\",\n    where=\"mid\",\n)\nax[0, 0].step(\n    list(range(len(vn[:, idx][0:]))),\n    vn[:, idx][0:],\n    label=\"vn\",\n    color=\"orange\",\n    where=\"mid\",\n)\nax[0, 0].set_xlabel(\"conversion step\")\nax[0, 0].set_ylabel(\"voltage [V]\")\nax[0, 0].set_title(f\"Node voltages of the ADC\\n for the input code {dout[:, idx]}\")\nax[0, 0].grid(True)\nax[0, 0].legend()\n\nvn_trans = np.diff(vn[:, idx][0:]) / vlsb\nvp_trans = np.diff(vp[:, idx][0:]) / vlsb\nax[0, 1].bar(\n    list(range(len(vn_trans))), vn_trans, label=\"vn\", color=\"orange\", alpha=0.6\n)\nax[0, 1].bar(list(range(len(vp_trans))), vp_trans, label=\"vp\", color=\"blue\", alpha=0.6)\nax[0, 1].set_xlabel(\"conversion step transition\")\nax[0, 1].set_ylabel(\"LSB (V/V)\")\nax[0, 1].set_title(\n    f\"Node voltage transitions of the ADC\\n for the input code {dout[:, idx]}\"\n)\nax[0, 1].grid(True)\nax[0, 1].legend()\n\nax[1, 0].step(\n    list(range(len(charge_vrefp))),\n    charge_vrefp[:, idx],\n    label=\"charge drawn from vref\",\n    color=\"blue\",\n    where=\"mid\",\n)\nax[1, 0].set_xlabel(\"conversion step\")\nax[1, 0].set_ylabel(\"normalized charge [C/C]\")\nax[1, 0].set_title(f\"Charge drawn from Vref \\n for the input code {dout[:, idx]}\")\nax[1, 0].grid(True)\n\nax[1, 1].step(\n    vin1 / mV, energy_vrefp, label=\"energy drawn from vref\", color=\"gray\", where=\"mid\"\n)\nax[1, 1].set_xlabel(\"Vin [mV]\")\nax[1, 1].set_ylabel(\"normalized charge [C/C]\")\nax[1, 1].set_title(\"Charge drawn from Vref - first sub ADC\")\nax[1, 1].grid(True)\n\nfig.tight_layout()\n</code></pre>"},{"location":"notebooks/practical_class_10/#capacitive-adc-high-level-modelling","title":"Capacitive ADC High-Level Modelling","text":""},{"location":"notebooks/practical_class_10/#monte-carlo-simulation-setup","title":"Monte Carlo Simulation Setup","text":""},{"location":"notebooks/practical_class_10/#successive-approximation-register-sar-adc-bottom-plate-sampling","title":"Successive Approximation Register (SAR) ADC -- Bottom Plate Sampling","text":"<p>The SAR ADC uses a binary search algorithm to find the input voltage. It is composed of a DAC, a comparator, and a SAR control logic and register. The binary search divides the search domain in two at each conversion step, making the comparator input approximate the ADC input voltage by reprogramming each bit of the intrinsic DAC to uncover the digital word that best represents the input.</p> <p>&gt; TODO: Insert diagram of a classic SAR ADC architecture.</p> <p>&gt; TODO: Insert diagram of the binary search domain division during conversion.</p> <p>&gt; TODO: Insert diagram of the ADC configuration at each conversion step.</p>"},{"location":"notebooks/practical_class_10/#bottom-plate-sampling-sar-algorithm","title":"Bottom-Plate Sampling SAR Algorithm","text":"<p>In the bottom-plate sampling configuration, the top plate of the CDAC is connected to V_CM during the first sampling phase. After sampling, the input voltage is transferred to the top plate via charge redistribution: V_x = (V_CM - V_in) + V_refn.</p> <p>At each subsequent conversion step, the SAR logic tentatively sets the current bit to 1 (connecting that capacitor to V_refp) and checks whether the top plate voltage is below the comparator threshold. If it is, the bit remains 1; otherwise it is reset to 0. The top plate voltage update follows the charge conservation principle between successive phases.</p>"},{"location":"notebooks/practical_class_10/#differential-successive-approximation-register-sar-adc-bottom-plate-sampling","title":"Differential Successive Approximation Register (SAR) ADC -- Bottom Plate Sampling","text":"<p>The differential SAR ADC uses two matched CDAC arrays (positive and negative rails) to process a differential input signal. Bottom plate sampling connects both top plates to V_CM during the sampling phase, then redistributes charge to transfer the differential input. Because the first comparison after sampling already reveals the MSB, the MSB capacitor is not strictly required in the CDAC array.</p> <p>&gt; TODO: Insert diagram of a differential SAR ADC with bottom plate sampling.</p> <p>&gt; TODO: Insert diagram of the search domain division explaining why the first bit is obtained without DAC switching.</p> <p>&gt; TODO: Insert diagram of the ADC configuration at each conversion step.</p>"},{"location":"notebooks/practical_class_10/#differential-sar-conversion-loop","title":"Differential SAR Conversion Loop","text":"<p>In the differential bottom-plate sampling SAR, after the sampling phase transfers the input to both top plates, each conversion step tentatively sets the current bit to 1. This connects the corresponding capacitor to V_refp on the positive rail and to V_refn on the negative rail (non-monotonic approach). The comparator evaluates the differential voltage (V_p - V_n) to decide whether the bit stays at 1 or resets to 0.</p> <p>The <code>dout_neg</code> control word is simply the complement of <code>dout</code>, ensuring that when a capacitor is connected to V_refp on one rail, its counterpart is connected to V_refn on the other.</p>"},{"location":"notebooks/practical_class_10/#successive-approximation-register-sar-adc-top-plate-sampling-monotonic-sar","title":"Successive Approximation Register (SAR) ADC -- Top Plate Sampling &amp; Monotonic SAR","text":"<p>In the top plate sampling configuration, the input signal is sampled directly onto the top plates of the CDAC. Combined with a monotonic switching scheme, this architecture reduces the number of capacitor switching events per conversion cycle, lowering dynamic power consumption.</p> <p>&gt; TODO: Insert diagram of a differential SAR ADC with top plate sampling and monotonic switching.</p> <p>&gt; TODO: Insert diagram of the search domain division with the monotonic algorithm.</p> <p>&gt; TODO: Insert diagram of the ADC configuration at each conversion step.</p>"},{"location":"notebooks/practical_class_10/#top-plate-sampling-with-monotonic-switching","title":"Top-Plate Sampling with Monotonic Switching","text":"<p>With top plate sampling, V_p and V_n are initialized directly to the differential input voltages. The first comparison immediately yields the MSB, allowing the CDAC to use N-1 binary-weighted capacitors plus a residue capacitor.</p> <p>Monotonic switching scheme: Instead of switching capacitors bidirectionally (to V_refp or V_refn), this approach switches in only one direction per array: - When V_p &gt; V_n (bit = 1): lower V_n by switching the negative array capacitor from V_refp to V_refn - When V_p &lt; V_n (bit = 0): lower V_p by switching the positive array capacitor from V_refp to V_refn</p> <p>Both voltages drift monotonically toward V_refn, favoring an NMOS differential pair in the comparator (faster settling than PMOS). The <code>dout_neg</code> word independently controls the negative array -- it is set equal to <code>dout</code> (not its complement) to enable independent per-array switching.</p>"},{"location":"notebooks/practical_class_10/#successive-approximation-register-sar-adc-v_cm-based-with-bottom-plate-sampling-and-a-split-capacitor-array-divided-by-a-capacitive-voltage-divider","title":"Successive Approximation Register (SAR) ADC -- V_CM-Based with Bottom Plate Sampling and a Split Capacitor Array Divided by a Capacitive Voltage Divider","text":"<p>This architecture splits the CDAC into two sub-arrays connected through a capacitive voltage divider. The first sub-array resolves the MSBs while the second resolves the LSBs. The voltage dividing capacitor scales the contribution of the LSB array, allowing smaller total capacitance compared to a fully binary-weighted array. Bottom plate sampling with V_CM-based switching is used to obtain the first bit without DAC switching.</p>"},{"location":"notebooks/practical_class_10/#consolidation-toward-a-reusable-sar-simulation","title":"Consolidation: Toward a Reusable SAR Simulation","text":"<p>The four SAR architectures above share a common simulation structure: CDAC initialization, iterative bit-decision with charge redistribution, and output code assembly. To avoid code duplication, these loops could be consolidated into a single parameterized function:</p> <pre><code>def simulate_sar(nbits, vrefp, vrefn, vcm, vin, caps, sampling=\"bottom_plate\",\n                 differential=False, monotonic=False):\n    \"\"\"Simulate a SAR ADC conversion cycle.\n\n    Parameters:\n        nbits: ADC resolution\n        vrefp, vrefn, vcm: reference and common-mode voltages\n        vin: input voltage(s)\n        caps: capacitor array values\n        sampling: \"bottom_plate\" or \"top_plate\"\n        differential: if True, use differential CDAC arrays\n        monotonic: if True, use monotonic switching scheme\n    Returns:\n        dout: output digital code(s)\n    \"\"\"\n    ...\n</code></pre> <p>This refactoring would reduce ~400 lines to ~100 + 4 short configuration calls, improving maintainability while preserving the pedagogical clarity of each architecture's unique switching behavior.</p>"},{"location":"notebooks/practical_class_2/","title":"2. Ideal ADC/DAC Modeling","text":"<pre><code>import numpy as np\nimport matplotlib.pyplot as plt\nfrom utils import dec2bin, ideal_dac, ideal_adc\n</code></pre> <pre><code>vref = 5  # [V]\nn_bits = 3\n</code></pre> <p>Note: The step function representation of the DAC always uses a post-step (<code>where=\"post\"</code>), meaning the output voltage is held constant until the next code transition.</p> <pre><code>idac = ideal_dac(vref, n_bits)\n\nx = np.arange(2**n_bits)\nx_bin = np.array([dec2bin(i, n_bits) for i in x])\nx_bin_str = [\"\".join([str(num) for num in xv]) for xv in x_bin]\ny = idac(x_bin)\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(x_bin_str, y, marker=\"o\", mfc=\"None\")\nax[0].set_xticklabels(x_bin_str, rotation=45)\nax[0].set_yticks(y)\nax[0].set_xlabel(\"Code\")\nax[0].set_ylabel(\"Voltage (V)\")\nax[0].set_title(\"Ideal DAC Transfer Function (Vout(Din))\")\nax[0].grid(True)\nax[1].step(x_bin_str, y, marker=\"None\", color=\"k\", label=\"floor\", where=\"post\")\nax[1].set_xticklabels(x_bin_str, rotation=45)\nax[1].set_yticks(y)\nax[1].set_xlabel(\"Code\")\nax[1].set_ylabel(\"Voltage (V)\")\nax[1].grid(True)\nfig.tight_layout()\n</code></pre> <pre>\n<code>/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_31985/3717026776.py:10: UserWarning: set_ticklabels() should only be used with a fixed number of ticks, i.e. after set_ticks() or using a FixedLocator.\n  ax[0].set_xticklabels(x_bin_str, rotation=45)\n/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_31985/3717026776.py:17: UserWarning: set_ticklabels() should only be used with a fixed number of ticks, i.e. after set_ticks() or using a FixedLocator.\n  ax[1].set_xticklabels(x_bin_str, rotation=45)\n</code>\n</pre> <pre><code>vref = 5  # [V]\nn_bits = 3\n</code></pre> <pre><code>iadc = ideal_adc(vref, n_bits, np.round)\niadc_ceil = ideal_adc(vref, n_bits, np.ceil)\niadc_floor = ideal_adc(vref, n_bits, np.floor)\n\nx = np.arange(0, vref, vref / 2**n_bits)\n\ny = iadc(x)\ny_str = [\"\".join([str(num) for num in yv]) for yv in y]\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(x, y_str, marker=\"o\", color=\"k\", mfc=\"None\")\nax[0].set_xticks(x)\nax[0].set_xticklabels(x, rotation=45)\nax[0].set_xlabel(\"Voltage (V)\")\nax[0].set_ylabel(\"Code\")\n\nax[1].step(x, y_str, marker=\"None\", color=\"k\", label=\"round\", where=\"mid\")\n\ny = iadc_ceil(x)\ny_str = [\"\".join([str(num) for num in yv]) for yv in y]\nax[1].step(x, y_str, marker=\"None\", color=\"r\", label=\"ceil\", where=\"post\")\nax[1].set_xticks(x)\nax[1].set_xticklabels(x, rotation=45)\nax[1].set_xlabel(\"Voltage (V)\")\nax[1].set_ylabel(\"Code\")\n\ny = iadc_floor(x)\ny_str = [\"\".join([str(num) for num in yv]) for yv in y]\nax[1].step(x, y_str, marker=\"None\", color=\"b\", label=\"floor\", where=\"pre\")\nax[1].set_xticks(x)\nax[1].set_xticklabels(x, rotation=45)\nax[1].set_xlabel(\"Voltage (V)\")\nax[1].set_ylabel(\"Code\")\n\nax[0].set_xlim([0, vref])\nax[1].set_xlim([0, vref])\nfig.tight_layout()\nax[0].set_title(\"Dout(Vin)\")\nax[0].grid(True)\nax[1].grid(True)\nax[1].legend()\n</code></pre> <pre>\n<code>&lt;matplotlib.legend.Legend at 0x113483cd0&gt;</code>\n</pre>"},{"location":"notebooks/practical_class_2/#practical-class-2-ideal-adc-and-dac-modelling","title":"Practical Class 2 \u2014 Ideal ADC and DAC Modelling","text":"<p>The models of an ideal ADC and DAC biased with a \\(V_{ref}\\) voltage and \\(N\\) bits of resolution are presented in this notebook. The conversion functions of both converters were created using the lambda programming style. The creator function for the ADC or DAC receives the two fundamental parameters defining these ideal electronic systems:</p> <ul> <li>\\(V_{ref}\\) \u2014 the reference/biasing voltage of the converter</li> <li>\\(N_{bits}\\) \u2014 the number of bits of the converter system (its digital resolution)</li> </ul> <p>The converter returns a function that, depending on its input, generates an output code or voltage for the ADC or DAC, respectively.</p>"},{"location":"notebooks/practical_class_2/#1-digital-to-analog-converter-modelling","title":"1. Digital-to-Analog Converter Modelling","text":"<p>The ideal model for the transfer function of a DAC is implemented through a lambda programming style, as shown in (1). Other approaches, such as object-oriented programming with a DAC object, are also possible.</p> \\[V_{OUT}(D_{in}) = \\text{base}_{10}\\{D_{in}\\} \\cdot V_{LSB} \\hspace{10pt} (1)\\] <p>where \\(D_{in}\\) is the input binary word, \\(V_{LSB}\\) is the least significant bit step voltage, and \\(V_{OUT}\\) is the output voltage of the DAC.</p>"},{"location":"notebooks/practical_class_2/#2-analog-to-digital-converter-modelling","title":"2. Analog-to-Digital Converter Modelling","text":"<p>The ideal model for the transfer function of an ADC is implemented through a lambda programming style, as shown in (2). Other approaches, such as object-oriented programming, are also possible.</p> \\[D_{OUT}(V_{in}, f_{Round}) = \\text{base}_2\\{f_{Round}(V_{in}/V_{LSB})\\} \\hspace{10pt} (2)\\] <p>where \\(V_{in}\\) is the input voltage, \\(V_{LSB}\\) is the least significant bit step voltage, and \\(D_{OUT}\\) is the output digital code. \\(f_{Round}\\) is the rounding function implemented by the ADC, which can be:</p> <ul> <li><code>round(.)</code> \u2014 rounding to the nearest decimal</li> <li><code>ceil(.)</code> \u2014 rounding to the highest decimal</li> <li><code>floor(.)</code> \u2014 rounding to the lowest decimal</li> </ul> <p>The rounding function is parsed as an input to the ADC's transfer function for practical, low-code purposes.</p>"},{"location":"notebooks/practical_class_3/","title":"3. Linear & Non-linear Errors","text":"<pre><code>import numpy as np\nfrom numpy.random import uniform\nimport matplotlib.pyplot as plt\nfrom utils import ideal_dac, ideal_adc, nonideal_adc, dec2bin, bin2dec\n</code></pre> <pre><code>vref = 1.0  # [V]\nn_bits = 5\nvlsb = vref / (2**n_bits)\n\nidac = ideal_dac(vref, n_bits)\n\nvoff = 0.05\ndac_off = lambda x: idac(x) + voff\n\ngain_err = -0.1\ntarget_gain = 1.0 + gain_err\ndac_eos = lambda x: idac(x) * target_gain\n\ndac_off_eos = lambda x: idac(x) * target_gain + voff\n\ndin = np.arange(2**n_bits)\ndinb = dec2bin(din, n_bits)\n\nvout = idac(dinb)\nvout_off = dac_off(dinb)\nvout_eos = dac_eos(dinb)\nvout_off_eos = dac_off_eos(dinb)\n</code></pre> <p>To accurately measure the linear errors of DACs, it is only necessary to observe the initial and final points of the transfer function. From these points three measurements can be performed:</p> <ul> <li>Offset Error, represented in Eq. (1)</li> <li>End of Scale Error, represented in Eq. (2)</li> <li>Gain Error, represented in Eq. (3)</li> </ul> <p>The Gain Error of the DAC is directly related to both the End of Scale and Offset Errors and is derived from them. The errors are measured in LSB units.</p> \\[\\epsilon_{OFF} = \\frac{V_{OUT}'[T_0] - V_{OUT}[T_0]}{V_{LSB}} \\hspace{4 pt} [LSB] \\hspace{10 pt} (1)\\] \\[\\epsilon_{EOS} = \\frac{V_{OUT}'[T_{2^N - 1}] - V_{OUT}[T_{2^N - 1}]}{V_{LSB}} \\hspace{4 pt} [LSB] \\hspace{10 pt} (2)\\] \\[GER_{[\\%]} = \\left(\\frac{V_{OUT}'[T_{2^N - 1}] - V_{OUT}'[T_{0}]}{V_{OUT}[T_{2^N - 1}] - V_{OUT}[T_{0}]} - 1\\right)\\times100 \\hspace{10 pt} (3)\\] <p>where \\(V_{OUT}[T_i]\\) and \\(V_{OUT}'[T_i]\\) are the output voltages of the ideal and real DAC at the \\(i^{th}\\) transition, respectively.</p> <pre><code>eoff = (vout_off_eos[0] - vout[0]) / vlsb\neeos = (vout_off_eos[2**n_bits - 1] - vout[2**n_bits - 1]) / vlsb\nger_pct = (\n    (vout_off_eos[2**n_bits - 1] - vout_off_eos[0]) / (vout[2**n_bits - 1] - vout[0])\n    - 1\n) * 100\nprint(\"The offset error is {:.2f} LSB\".format(eoff))\nprint(\"The end of scale error is {:.2f} LSB\".format(eeos))\nprint(\"The gain error is {:.2f} %\".format(ger_pct))\n</code></pre> <pre>\n<code>The offset error is 1.60 LSB\nThe end of scale error is -1.50 LSB\nThe gain error is -10.00 %\n</code>\n</pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(din, vout, label=\"Ideal DAC\", marker=\"None\", mfc=\"None\", color=\"k\")\nax.step(din, vout_off, where=\"post\", label=\"DAC w/ offset error\")\nax.step(din, vout_eos, where=\"post\", label=\"DAC w/ end of scale error\")\nax.step(din, vout_off_eos, where=\"post\", label=\"DAC w/ offset &amp;amp; end of scale error\")\nax.set_xlabel(\"Input code\")\nax.set_ylabel(\"Output voltage [V]\")\nax.grid(True)\nax.set_title(\"DAC Transfer Function\")\nax.legend(fontsize=8, fancybox=False, frameon=False)\nax.annotate(\n    r\"$\\epsilon_{OFF}$: \" + \"{:.2f} LSB\".format(eoff),\n    xy=(0, vout_off_eos[0]),\n    xytext=(0, vout_off_eos[0] - 0.1),\n    arrowprops=dict(arrowstyle=\"-&amp;gt;\"),\n    fontsize=8,\n)\nax.annotate(\n    r\"$\\epsilon_{EOS}$: \" + \"{:.2f} LSB\".format(eeos),\n    xy=(2**n_bits - 1, vout_off_eos[2**n_bits - 1]),\n    xytext=(2**n_bits - 1, vout_off_eos[2**n_bits - 1] + 0.1),\n    arrowprops=dict(arrowstyle=\"-&amp;gt;\"),\n    fontsize=8,\n)\nax.annotate(\n    \"Gain error: {:.2f} %\".format(ger_pct),\n    xy=(0, 0),\n    xytext=(2**n_bits - 15, 0),\n    fontsize=8,\n)\nfig.tight_layout()\n</code></pre> <pre><code>vref = 1.0  # [V]\nn_bits = 4\nvlsb = vref / (2**n_bits)\n\nidac = ideal_dac(vref, n_bits)\n\nvq_var = (vlsb**2) / 12  # [V^2]\nvq_std_dev = np.sqrt(vq_var)\nnoise_factor = 2\ndac_nonlinear = lambda x: (\n    idac(x) + noise_factor * uniform(-vq_std_dev, vq_std_dev, len(x))\n)\n\ndin = np.arange(2**n_bits)\ndinb = dec2bin(din, n_bits)\n\nvout = idac(dinb)\nvout_nonlinear = dac_nonlinear(dinb)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(din, vout, label=\"Ideal DAC\", marker=\"None\", mfc=\"None\", color=\"k\")\nax.step(din, vout_nonlinear, where=\"post\", label=\"Non-Linear DAC\", color=\"r\")\nax.set_xticks(din)\nax.set_yticks(np.arange(0, vref + vlsb, vlsb))\nax.set_xlabel(\"Input code\")\nax.set_ylabel(\"Output voltage [V]\")\nax.set_title(\"DAC Transfer Function\")\nax.legend(fontsize=8, fancybox=False, frameon=False)\nax.grid(True)\nfig.tight_layout()\n</code></pre> <p>As observed above, the exaggerated transfer function non-linearities feature variable output voltages for the non-ideal DAC due to the quantization noise. The noise was multiplied by an amplification factor to better observe the effect of quantization noise on the non-linear behaviour of the DAC's transfer function.</p> <p>The non-linear errors of converters come in two forms:</p> <ul> <li>Differential Non-Linearity (DNL), given in Eq. (5)</li> <li>Integral Non-Linearity (INL), given in Eq. (6)</li> </ul> <p>The DNL and INL are measured in LSB units. The DNL provides a local measure of how much each step size changes at each transition. The INL is the accumulation of the DNL up to the respective transition, enabling the measurement of global step-size variability throughout the transfer function. In DACs, the INL and DNL are measured on the vertical axis (voltage). Eq. (4) presents the measured least significant bit step voltage of the DAC.</p> \\[ V_{LSB}' = \\frac{V_{OUT}'[2^N - 1] - V_{OUT}'[0]}{2^N - 1} \\hspace{4 pt} [V] \\hspace{10 pt} (4)\\] \\[ DNL[n] = \\frac{V_{OUT}'[n+1] - V_{OUT}'[n]}{V_{LSB}'} \\hspace{4 pt} [LSB], n\\in [0, 2^N-2] \\hspace{10 pt} (5) \\] \\[ INL[n] = \\frac{V_{OUT}'[n] - n\\times V_{LSB}' - V_{OUT}'[0]}{V_{LSB}'} \\hspace{4 pt} [LSB], n\\in [0, 2^N-1] \\hspace{10 pt} (6) \\] <p>where \\(V_{OUT}'[n]\\) is the output voltage of the real DAC at the \\(n^{th}\\) transition and \\(V_{LSB}'\\) is the measured step size of the non-ideal DAC.</p> <p>NOTE: The INL and DNL can only be calculated after removing the linear error sources (offset and gain errors).</p> <pre><code>vlsb_measured = (vout_nonlinear[2**n_bits - 1] - vout_nonlinear[0]) / (2**n_bits - 1)\nprint(\"The ideal vlsb is {:.4f} V\".format(vlsb))\nprint(\"The measured vlsb is {:.4f} V\".format(vlsb_measured))\n\ndnl = np.diff(vout_nonlinear) / vlsb_measured - 1\nassert len(dnl) == 2**n_bits - 1, \"The DNL has a wrong length\"\n\nidx = np.arange(2**n_bits)\ninl = (vout_nonlinear - idx * vlsb_measured - vout_nonlinear[0]) / vlsb_measured\nassert len(inl) == 2**n_bits, \"The INL has a wrong length\"\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(din[1::], dnl, label=\"DNL\", marker=\"None\", mfc=\"None\", color=\"k\")\nax[1].plot(din, inl, label=\"INL\", marker=\"None\", mfc=\"None\", color=\"r\")\nax[0].set_xlabel(\"Input code\")\nax[1].set_xlabel(\"Input code\")\nax[0].set_ylabel(\"DNL\")\nax[1].set_ylabel(\"INL\")\nax[0].set_title(\"DAC Non-Linearity\")\nax[0].grid(True)\nax[1].grid(True)\nfig.tight_layout()\nfig.legend(fancybox=False, frameon=False, fontsize=8)\n</code></pre> <pre>\n<code>The ideal vlsb is 0.0625 V\nThe measured vlsb is 0.0624 V\n</code>\n</pre> <pre>\n<code>&lt;matplotlib.legend.Legend at 0x10fee5190&gt;</code>\n</pre> <p>To model ADC linear errors, we use a physically-motivated ADC model where the transfer function is defined by its transition voltages. A transition in the output code occurs only when the input voltage exceeds the corresponding transition voltage, matching the behaviour of a comparator-based ADC.</p> <p>The gain error of the ADC is directly related to the \\(V_{REF}\\) voltage variation. A lower \\(V_{REF}\\) reduces \\(V_{LSB}\\), increasing the end-of-scale error as the gain decreases. Conversely, a higher \\(V_{REF}\\) increases \\(V_{LSB}\\) and the ADC gain.</p> <pre><code>vref = 1.0  # [V]\nn_bits = 4\nvlsb = vref / (2**n_bits)\nroundf = np.round\niadc = ideal_adc(vref, n_bits, roundf)\n\nvoff = 0.05\nadc_off, vt_off = nonideal_adc(vref, n_bits, roundf=roundf, ofst=voff)\n\nvref_err = -0.05\ntrue_vref = (1 + vref_err) * vref\nadc_eos, vt_eos = nonideal_adc(true_vref, n_bits, roundf=roundf)\n\nadc_off_eos, vt_off_eos_raw = nonideal_adc(true_vref, n_bits, roundf=roundf, ofst=voff)\n\n_, vt_ideal = nonideal_adc(vref, n_bits, roundf=roundf)\n\nvin = np.arange(0, vref + vlsb, vlsb)\n\nvt_off = np.concatenate(([0], vt_off, [vt_off[-1]]))\nvt_eos = np.concatenate(([0], vt_eos, [vt_eos[-1]]))\nvt_off_eos = np.concatenate(([0], vt_off_eos_raw, [vt_off_eos_raw[-1]]))\n\ndout = iadc(vin)\ndout_off = adc_off(vin)\ndout_eos = adc_eos(vin)\ndout_off_eos = adc_off_eos(vin)\n\ndout_dec = bin2dec(dout, n_bits)\ndout_off_dec = bin2dec(dout_off, n_bits)\ndout_eos_dec = bin2dec(dout_eos, n_bits)\ndout_off_eos_dec = bin2dec(dout_off_eos, n_bits)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(vin, dout_dec, label=\"Ideal ADC\", marker=\"None\", mfc=\"None\", color=\"k\")\nax.step(vt_off, dout_off_dec, where=\"post\", label=\"ADC with offset error\", color=\"b\")\nax.step(\n    vt_eos, dout_eos_dec, where=\"post\", label=\"ADC with end of scale error\", color=\"r\"\n)\nax.step(\n    vt_off_eos,\n    dout_off_eos_dec,\n    where=\"post\",\n    label=\"ADC with offset and end of scale error\",\n    color=\"g\",\n)\nax.set_xlabel(\"Input voltage [V]\")\nax.set_ylabel(\"Output code\")\nax.set_title(\"ADC Transfer Function\")\nax.legend(fontsize=8, fancybox=False, frameon=False)\nax.grid(True)\nfig.tight_layout()\n</code></pre> <pre><code>e_off = (vt_off_eos_raw[0] - vt_ideal[0]) / vlsb\ne_eos = (vt_off_eos_raw[-1] - vt_ideal[-1]) / vlsb\nger_pct = (\n    (vt_off_eos_raw[-1] - vt_off_eos_raw[0]) / (vt_ideal[-1] - vt_ideal[0]) - 1\n) * 100\nprint(\"The offset error is {:.2f} LSB\".format(e_off))\nprint(\"The end of scale error is {:.2f} LSB\".format(e_eos))\nprint(\"The gain error is {:.2f} %\".format(ger_pct))\n</code></pre> <pre>\n<code>The offset error is 0.75 LSB\nThe end of scale error is 0.05 LSB\nThe gain error is -5.00 %\n</code>\n</pre> <pre><code>vref = 1.0  # [V]\nn_bits = 5\nvlsb = vref / (2**n_bits)\nroundf = np.round\niadc = ideal_adc(vref, n_bits, roundf)\n\nvq_var = (vlsb**2) / 12  # [V^2]\nvq_std_dev = np.sqrt(vq_var)\nnoise_factor = 2\nadc_nonlinear, vtrans = nonideal_adc(\n    vref, n_bits, roundf=roundf, vnq=noise_factor * vq_std_dev\n)\n\nvin = np.arange(0, vref + vlsb, vlsb)\n\ndout = bin2dec(iadc(vin), n_bits)\ndout_nonlinear = bin2dec(adc_nonlinear(vin), n_bits)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(vin, dout, label=\"Ideal ADC\", marker=\"None\", mfc=\"None\", color=\"k\")\nax.step(vin, dout_nonlinear, where=\"post\", label=\"Non-linear ADC\", color=\"r\")\nax.set_xlabel(\"Input voltage [V]\")\nax.set_ylabel(\"Output code\")\nax.set_title(\"ADC Transfer Function\")\nax.legend(fontsize=8, fancybox=False, frameon=False)\nax.grid(True)\nfig.tight_layout()\n</code></pre> <p>From the figures in this ADC section (2.1 and 2.2), an important conclusion emerges: both linear and non-linear errors can lead to missing codes in the ADC output, which results in a loss of effective resolution.</p> <p>Analogously to the DAC section, the non-linear errors come in two forms:</p> <ul> <li>Differential Non-Linearity (DNL), given in Eq. (8)</li> <li>Integral Non-Linearity (INL), given in Eq. (9)</li> </ul> <p>The DNL and INL are measured in LSB units. The DNL locally measures how much each step size changes at each transition voltage. The INL is the accumulation of the DNL up to the respective transition, measuring the global transition voltage variability throughout the transfer function. In ADCs, the INL and DNL are measured on the horizontal axis (voltage). Eq. (7) presents the measured least significant bit step voltage of the ADC.</p> \\[ V_{LSB}' = \\frac{V_{T}'[2^N - 1] - V_{T}'[1]}{2^N - 2} \\hspace{4 pt} [V] \\hspace{10 pt} (7)\\] \\[ DNL[n] = \\frac{V_{T}'[n+1] - V_{T}'[n]}{V_{LSB}'} \\hspace{4 pt} [LSB], n\\in [0, 2^N-3] \\hspace{10 pt} (8) \\] \\[ INL[n] = \\frac{V_{T}'[n] - n\\times V_{LSB}' - V_{T}'[1]}{V_{LSB}'} \\hspace{4 pt} [LSB], n\\in [0, 2^N-2] \\hspace{10 pt} (9) \\] <p>where \\(V_{T}'[n]\\) is the transition voltage of the real ADC at the \\(n^{th}\\) transition and \\(V_{LSB}'\\) is the measured step size of the non-ideal ADC.</p> <p>In the ADC case, there are \\(2^N\\) intervals but only \\(2^N - 1\\) transition voltages, whereas in the DAC case there are \\(2^N\\) intervals and \\(2^N\\) output voltages.</p> <p>NOTE: The INL and DNL can only be calculated after removing the linear error sources (offset and gain errors).</p> <pre><code>assert len(vtrans) == 2**n_bits - 1\n\nvlsb_measured = (vtrans[-1] - vtrans[0]) / (len(vtrans) - 1)\nprint(\"The ideal vlsb is {:.4f} V\".format(vlsb))\nprint(\"The measured vlsb is {:.4f} V\".format(vlsb_measured))\n\ndnl = np.diff(vtrans) / vlsb_measured - 1\nassert len(dnl) == 2**n_bits - 2, \"The number of DNL values is not correct\"\n\nidx = np.arange(0, len(vtrans), 1)\ninl = (vtrans - idx * vlsb_measured - vtrans[0]) / vlsb_measured\nassert len(inl) == 2**n_bits - 1, \"The number of INL values is not correct\"\n</code></pre> <pre>\n<code>The ideal vlsb is 0.0312 V\nThe measured vlsb is 0.0305 V\n</code>\n</pre> <pre><code>codes = np.arange(1, len(vtrans), 1)\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(codes, dnl, label=\"DNL\", marker=\"None\", mfc=\"None\", color=\"k\")\nax[1].plot(idx, inl, label=\"INL\", marker=\"None\", mfc=\"None\", color=\"r\")\nax[0].set_xlabel(\"Code\")\nax[0].set_ylabel(\"DNL\")\nax[1].set_xlabel(\"Code\")\nax[1].set_ylabel(\"INL\")\nax[0].set_title(\"DNL and INL\")\nax[0].legend(fontsize=8, fancybox=False, frameon=False)\nax[1].legend(fontsize=8, fancybox=False, frameon=False)\nax[0].grid(True)\nax[1].grid(True)\nfig.tight_layout()\n</code></pre>"},{"location":"notebooks/practical_class_3/#practical-class-3-linear-and-non-linear-errors-in-dacs-and-adcs","title":"Practical Class 3: Linear and Non-linear Errors in DACs and ADCs","text":"<p>The introduction of quantization noise in the ADC or DAC introduces errors associated with the quantization step size defining the true characteristic of the transfer function. This notebook presents the modelling and study of the two main types of errors in converters: linear and non-linear.</p>"},{"location":"notebooks/practical_class_3/#1-dac-errors-modelling","title":"1. DAC Errors Modelling","text":""},{"location":"notebooks/practical_class_3/#11-dac-linear-errors","title":"1.1. DAC Linear Errors","text":"<p>This section presents the modelling of linear errors in the DAC transfer function. These errors manifest as a deviation of the real transfer function from the ideal one, where each step size is exactly the same height.</p>"},{"location":"notebooks/practical_class_3/#12-dac-non-linear-errors","title":"1.2. DAC Non-Linear Errors","text":"<p>This section presents the modelling of non-linear errors in the DAC transfer function. These errors arise when each step size differs in height due to the inherent non-linearity introduced during signal quantization.</p>"},{"location":"notebooks/practical_class_3/#2-adc-error-modelling","title":"2. ADC Error Modelling","text":""},{"location":"notebooks/practical_class_3/#21-adc-linear-errors","title":"2.1. ADC Linear Errors","text":"<p>This section presents the modelling of linear errors in the ADC transfer function. These errors manifest as a deviation of the real transfer function from the ideal one, where each step has exactly the same width. Note that in the ADC case, it is the transition voltages that deviate from ideal, without changing the step size.</p>"},{"location":"notebooks/practical_class_3/#22-adc-non-linear-errors","title":"2.2. ADC Non-Linear Errors","text":"<p>This section presents the modelling of non-linear errors in the ADC transfer function. These errors arise when each step differs in width due to the inherent non-linearity introduced during signal quantization. In a physical implementation, intrinsic device non-linearities are usually the main source of non-linear errors, directly affecting the position of the transition voltages.</p>"},{"location":"notebooks/practical_class_4/","title":"4. DAC/ADC Error Exercises","text":"<pre><code>%matplotlib inline\nimport numpy as np\nfrom numpy.random import normal\nimport matplotlib.pyplot as plt\n</code></pre> <pre><code>Np = 1000\nFclk = 100e3  # [Hz]\ndt = 1 / Fclk\ntD = np.arange(0, Np * dt, dt)\n</code></pre> <pre><code>A1 = 100e-3\nf1 = 1e3\nph1 = 0\ns1 = A1 * np.sin(2 * np.pi * f1 * tD + ph1)\n\nvref = 5  # [V]\nnbit = 12\nvlsb = vref / (2**nbit)\n\np_nq = (vlsb**2) / 12  # [V^2]\nv_nq = np.sqrt(p_nq)  # [V]\n\npin = (A1**2) / 2  # [V^2]\n\nsnr = 10 * np.log10(pin / p_nq)  # [dB]\nprint(\"SNR for A_in = 100 mV : {:.2f} dB\".format(snr))\n</code></pre> <pre>\n<code>SNR for A_in = 100 mV : 46.05 dB\n</code>\n</pre> <pre><code>A2 = vref / 2\nsnr_max = 10 * np.log10((A2**2 / 2) / p_nq)  # [dB]\nsnr_max_compare = 6.02 * nbit + 1.76  # [dB]\nprint(\"Maximum SNR for A_in = V_ref/2 : {:.2f} dB\".format(snr_max))\nprint(\"Maximum SNR (ideal expression) : {:.2f} dB\".format(snr_max_compare))\nassert np.isclose(snr_max, snr_max_compare, 1e-2)\n</code></pre> <pre>\n<code>Maximum SNR for A_in = V_ref/2 : 74.01 dB\nMaximum SNR (ideal expression) : 74.00 dB\n</code>\n</pre> <pre><code>s2 = A2 * np.sin(2 * np.pi * f1 * tD + ph1) + vref / 2\nscale_time = 1e-3\n\nfig, ax = plt.subplots(1, 1, figsize=(3, 3))\nax.plot(\n    tD / scale_time,\n    s2,\n    label=\"$A_{in} = V_{ref}/2$\",\n    color=\"r\",\n    linewidth=2,\n    marker=\"o\",\n    markersize=2,\n    mfc=\"None\",\n)\nax.set_xlabel(\"Time (ms)\")\nax.set_ylabel(\"Amplitude (V)\")\nax.set_title(\"Input signal\")\nax.legend()\nplt.show()\n</code></pre> <pre><code>dout = np.round(s2 / vlsb).astype(int)\n\nfig, ax = plt.subplots(1, 1, figsize=(3, 3))\nax.plot(\n    tD / scale_time, dout, color=\"b\", linewidth=2, marker=\"o\", markersize=2, mfc=\"None\"\n)\nax.set_xlabel(\"Time (ms)\")\nax.set_ylabel(\"Code\")\nax.set_title(\"Output codes\")\nplt.show()\n</code></pre> <pre><code>error = dout * vlsb - s2\n\nfig, ax = plt.subplots(1, 1, figsize=(3, 3))\nax.plot(\n    tD / scale_time,\n    error,\n    color=\"b\",\n    linewidth=1,\n    marker=\"None\",\n    markersize=2,\n    mfc=\"None\",\n)\nax.set_xlabel(\"Time (ms)\")\nax.set_ylabel(\"Error (V)\")\nax.set_title(\"Quantization error\")\nplt.show()\n</code></pre> <pre><code>std_dev_error = np.std(error)\nprint(\"Std. dev. of quantization error : {:.2f} mV\".format(std_dev_error * 1e3))\nprint(\"Std. dev. (theoretical)         : {:.2f} mV\".format(v_nq * 1e3))\nassert np.isclose(std_dev_error, v_nq, 1e-0)\n</code></pre> <pre>\n<code>Std. dev. of quantization error : 0.29 mV\nStd. dev. (theoretical)         : 0.35 mV\n</code>\n</pre> <pre><code>vref = 4  # [V]\nnbit = 3\nvlsb = vref / (2**nbit)\ndin = np.arange(0, 2**nbit, 1).astype(int)\n\nvout_nonideal = np.array([0.011, 0.507, 1.002, 1.501, 1.996, 2.495, 2.996, 3.491])\nvout_ideal = din * vlsb\n\nfig, ax = plt.subplots(1, 1, figsize=(3, 3))\nax.step(\n    din, vout_nonideal, color=\"b\", linewidth=2, marker=\"o\", markersize=2, mfc=\"None\"\n)\nax.set_xlabel(\"Input Code\")\nax.set_ylabel(\"Output Voltage (V)\")\nax.set_title(\"Output characteristic of the DAC\")\nax.grid()\nplt.show()\n</code></pre> <pre><code>offset = (vout_nonideal[0] - vout_ideal[0]) / vlsb  # [LSB]\nprint(\"Offset error       : {:.2f} LSB\".format(offset))\n\nend_of_scale = (vout_nonideal[-1] - vout_ideal[-1]) / vlsb  # [LSB]\nprint(\"End of scale error : {:.2f} LSB\".format(end_of_scale))\n</code></pre> <pre>\n<code>Offset error       : 0.02 LSB\nEnd of scale error : -0.02 LSB\n</code>\n</pre> <p>The expressions for DNL and INL already remove the offset and gain error of the DAC through the computation of the measured LSB voltage.</p> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.step(\n    din,\n    vout_nonideal,\n    color=\"r\",\n    linewidth=2,\n    marker=\"o\",\n    markersize=2,\n    mfc=\"None\",\n    label=\"Non-ideal\",\n)\nax.step(\n    din,\n    vout_ideal,\n    color=\"k\",\n    linewidth=2,\n    marker=\"o\",\n    markersize=2,\n    mfc=\"None\",\n    label=\"Ideal\",\n)\nax.set_xlabel(\"Input Code\")\nax.set_ylabel(\"Output Voltage (V)\")\nax.set_title(\"Output characteristic of the DAC\")\nax.legend()\nax.grid(True)\nplt.show()\n</code></pre> <pre><code>vlsb_measured = (vout_nonideal[-1] - vout_nonideal[0]) / ((2**nbit) - 1)\nprint(\"Measured LSB    : {:.2f} mV\".format(vlsb_measured * 1e3))\nprint(\"Theoretical LSB : {:.2f} mV\".format(vlsb * 1e3))\n\ndnl = np.diff(vout_nonideal) / vlsb_measured - 1\ndnl_classic = (vout_nonideal[1:] - vout_nonideal[:-1]) / vlsb_measured - 1\nassert np.allclose(dnl, dnl_classic)\n\ninl = (vout_nonideal - vlsb_measured * din - vout_nonideal[0]) / vlsb_measured\ninl_cumsum = np.array([0] + np.cumsum(dnl).tolist())\ndnl_diff = np.diff(inl)\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(din[1:], dnl, color=\"b\", linewidth=2, marker=\"o\", markersize=5, mfc=\"None\")\nax[0].plot(\n    din[1:], dnl_diff, color=\"k\", linewidth=2, marker=\"o\", markersize=5, mfc=\"None\"\n)\nax[0].set_ylabel(\"DNL\")\nax[0].set_xticks(din)\nax[1].plot(din, inl, color=\"b\", linewidth=2, marker=\"o\", markersize=5, mfc=\"None\")\nax[1].plot(\n    din, inl_cumsum, color=\"k\", linewidth=2, marker=\"o\", markersize=5, mfc=\"None\"\n)\nax[1].set_xlabel(\"Input Code\")\nax[1].set_xticks(din)\nax[1].set_ylabel(\"INL\")\nax[0].grid(True)\nax[1].grid(True)\nplt.show()\n</code></pre> <pre>\n<code>Measured LSB    : 497.14 mV\nTheoretical LSB : 500.00 mV\n</code>\n</pre> <p>Note: The first and last points of the INL are always 0.0 LSB. This is guaranteed because the expression used to compute the INL automatically removes the offset and gain error of the DAC (by subtracting <code>vlsb_measured * din[0]</code> and dividing by <code>vlsb_measured</code>, respectively).</p> <pre><code>dac_linearity = nbit - np.log2(np.max(inl) - np.min(inl))\nprint(\"Effective number of bits (ENOB) : {:.2f}\".format(dac_linearity))\n</code></pre> <pre>\n<code>Effective number of bits (ENOB) : 9.44\n</code>\n</pre> <p>The linearity of the DAC does not measure its resolution. It measures how closely each code transition aligns with those of an ideal DAC with 9.44 bits of resolution. As max(INL) - min(INL) converges toward 0 LSB, the linearity converges toward infinity, meaning the DAC can preserve resolution equivalent to an infinite number of bits during digital-to-analog conversion.</p> <pre><code>nbit = 8\nvref = 1  # [V]\nvlsb = vref / (2**nbit)\n\nA3 = vref / 2  # [V]\nf3 = 1e3  # [Hz]\nf3 = np.round(f3 / Fclk * Np) * Fclk / Np  # coherent sampling  # [Hz]\nph1 = 0  # [rad]\nvn = vlsb / 2  # [V]\nmV = 1e-3\ndc_level = vref / 2\nprint(\"vn level =\", vn / mV, \"mV\")\n\ns3 = A3 * np.sin(2 * np.pi * f3 * tD + ph1) + dc_level + normal(0, vn, Np)\n\np_nq = vlsb**2 / 12  # [V^2]\nvnq = np.sqrt(p_nq)  # [V]\ndout = np.round(s3 / vlsb).astype(int)\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD, s3, color=\"b\", linewidth=2)\nax[0].set_ylabel(\"Input Signal (V)\")\nax[1].plot(tD, dout, color=\"b\", linewidth=2)\nax[1].set_xlabel(\"Time (s)\")\nax[1].set_ylabel(\"Output Code\")\nax[0].grid(True)\nax[1].grid(True)\nax[0].set_title(\"Input signal and output code\")\nplt.show()\n</code></pre> <pre>\n<code>vn level = 1.953125 mV\n</code>\n</pre> <pre><code>print(f\"Dt = {dt / 1e-9} ns\")\nprint(f\"Np = {Np} samples\")\n\nf_scale = 1e3\nfD = np.fft.fftshift(np.fft.fftfreq(Np, dt))\ndout_f = np.abs(np.fft.fftshift(np.fft.fft(dout) / Np))\ndout_p = dout_f * dout_f\ndout_db = 10 * np.log10(dout_p, out=np.zeros_like(dout_p), where=(dout_p != 0))\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].stem(\n    fD[fD &amp;gt;= 0] / f_scale,\n    dout_db[fD &amp;gt;= 0],\n    label=\"Input signal\",\n    bottom=np.min(dout_db),\n    linefmt=\"b-\",\n    markerfmt=\"none\",\n    basefmt=\"r-\",\n)\nax[0].set_xlabel(\"Frequency (KHz)\")\nax[0].set_ylabel(\"Power (dB)\")\nax[0].set_title(\"Spectrum of Dout of the ADC\")\nax[0].grid(True)\nax[1].plot(fD[fD &amp;gt;= 0] / f_scale, dout_p[fD &amp;gt;= 0], label=\"Input signal\")\nax[1].set_xlabel(\"Frequency (KHz)\")\nax[1].set_ylabel(\"Power ($V^2$)\")\nax[1].grid(True)\nplt.show()\n</code></pre> <pre>\n<code>Dt = 10000.0 ns\nNp = 1000 samples\n</code>\n</pre> <pre><code>fbin = fD[np.argmin(np.abs(fD - f3))]\nassert not np.all(fbin == np.nan)\ndout_p_fin = dout_p[fD == fbin][0]\n\nnew_zero = 1e-10\n\nprint(\"Power of the input signal :\", dout_p_fin, \"V^2\")\n\nideal_spectrum = new_zero * np.ones(len(dout_p))\nideal_spectrum[fD == fbin] = dout_p_fin\nnoise_spectrum = dout_p - ideal_spectrum\nnoise_spectrum[noise_spectrum == 0] = new_zero\nq_noise_p = np.mean(noise_spectrum)\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    10 * np.log10(dout_p[fD &amp;gt;= 0]),\n    label=\"Output signal\",\n    color=\"b\",\n)\nax.plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    10 * np.log10(ideal_spectrum[fD &amp;gt;= 0]),\n    label=\"Ideal spectrum\",\n    color=\"r\",\n)\nax.plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    10 * np.log10(noise_spectrum[fD &amp;gt;= 0]),\n    label=\"Noise spectrum\",\n    color=\"purple\",\n)\nax.set_xlabel(\"Frequency (KHz)\")\nax.set_ylabel(\"Power (dB)\")\nax.set_title(\"Spectrum of Dout of the ADC\")\nax.grid(True)\nax.legend()\nplt.show()\n\nprint(\"Power of the quantization noise :\", q_noise_p, \"V^2\")\nsnr = 10 * np.log10(dout_p_fin / q_noise_p)  # [dB]\nprint(\"SNR of the ADC :\", snr, \"dB\")\n</code></pre> <pre>\n<code>Power of the input signal : 4097.497648299314 V^2\n</code>\n</pre> <pre>\n<code>Power of the quantization noise : 20.479776351600886 V^2\nSNR of the ADC : 23.011935033474728 dB\n</code>\n</pre> <pre><code>vni = 0.1e-3\nnoise_step = (vlsb / 2 - vni) / 10\nnoise = np.arange(vni, vlsb / 2, noise_step)\nqnoise = []\nsnrs = []\n\nfor vn in noise:\n    s3 = A3 * np.sin(2 * np.pi * f3 * tD + ph1) + dc_level + normal(0, vn, Np)\n    dout = np.round(s3 / vlsb).astype(int)\n    dout_f = np.abs(np.fft.fftshift(np.fft.fft(dout) / Np))\n    dout_p = dout_f * dout_f\n    dout_p[fD == 0] = new_zero\n    fbin = fD[np.argmin(np.abs(fD - f3))]\n    dout_p_fin = dout_p[fD == fbin]\n    ideal_spectrum = new_zero * np.ones(len(dout_p))\n    ideal_spectrum[fD == fbin] = dout_p_fin\n    noise_spectrum = dout_p - ideal_spectrum\n    noise_spectrum[noise_spectrum == 0] = new_zero\n    q_noise_p = np.mean(noise_spectrum)\n    snr = 10 * np.log10(dout_p_fin / q_noise_p)  # [dB]\n    qnoise.append(vn**2)\n    snrs.append(snr)\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(np.array(qnoise), np.array(snrs), marker=\"o\", color=\"b\")\nax.set_xlabel(\"Quantization noise ($V^2$)\")\nax.set_ylabel(\"SNR (dB)\")\nax.set_title(\"SNR vs. Quantization noise\")\nax.grid(True)\nplt.show()\n</code></pre>"},{"location":"notebooks/practical_class_4/#practical-class-4-exercises-on-dacadc-modelling-and-dnlinl-error-modelling","title":"Practical Class 4 - Exercises on DAC/ADC modelling and DNL/INL error modelling","text":""},{"location":"notebooks/practical_class_4/#problem-9","title":"Problem 9","text":""},{"location":"notebooks/practical_class_4/#problem-10","title":"Problem 10","text":""},{"location":"notebooks/practical_class_4/#problem-12","title":"Problem 12","text":""},{"location":"notebooks/practical_class_5/","title":"5. Noise Modeling","text":"<p>Noise is a very important factor in the design of a system. It is important to know how much noise is generated by a system and how much noise is introduced into the system by the surrounding environment/systems. This practical class covers how to model noise in a converter, and what are the impacts of the several sources of noise in the overall performance.</p> <p>There are three primary noise sources in a converter:</p> <ul> <li>Quantization Noise (Quantization Error Noise model approximated by a uniform distribution)</li> <li>Gaussian Noise (Inevitable source of noise, in the form of thermal, quantic and other electromagnetic phenomena happening in the circuit blocks)</li> <li>Thermal Noise</li> <li>Shot Noise</li> <li>Jitter Noise (Phase Noise generated by the random switching time stamps of the switches in the converter)</li> </ul> <p>The complete model of an Analog-to-Digital converter signal chain is presented in the figure below, including the noise sources and the noise propagation through the system.</p> <p></p> <pre><code>import numpy as np\nfrom scipy.constants import k\nfrom numpy.random import uniform, normal\nimport matplotlib.pyplot as plt\nimport seaborn as sns\n\nsns.set_theme()\nsns.set_palette(\"colorblind\")\n</code></pre> <pre><code>Np = 1000\nFclk = 100e3  # [Hz]\ndt = 1 / Fclk\ntD = np.arange(0, Np * dt, dt)\n</code></pre> <pre><code>nbit = 10\nvref = 1\nvlsb = vref / (2**nbit)\n\nA1 = vref / 2  # [V]\nf1 = 1e3  # [Hz]\nf1 = np.round(f1 / Fclk * Np) * Fclk / Np  # coherent sampling  # [Hz]\nph1 = 0  # [rad]\n\nmV = 1e-3\ndc_level = vref / 2\n\nsig_t = 1e-7  # sigma_t\njitter_stdev = A1 * Fclk * sig_t * 2 * np.pi  # jitter standard deviation\njitter_noise = normal(0, jitter_stdev, Np)\nsi = A1 * np.sin(2 * np.pi * f1 * tD + ph1) + dc_level + jitter_noise\ndout = np.round(si / vlsb).astype(int)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nfreq = int(1)\nv_scale = mV\nsns.histplot(jitter_noise, ax=ax, stat=\"probability\", bins=10, kde=True)\nx_ticks = ax.get_xticks()\nax.set_xticklabels([f\"{val / v_scale:.1f}\" for val in x_ticks[::freq]])\nax.set_ylabel(\"Probability\")\nax.set_title(\"Normal Distribution - Jitter Noise\")\nax.set_xlabel(\"Jitter Noise - Std. Dev (mV)\")\n</code></pre> <pre>\n<code>/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_32154/3264910755.py:6: UserWarning: set_ticklabels() should only be used with a fixed number of ticks, i.e. after set_ticks() or using a FixedLocator.\n  ax.set_xticklabels([f\"{val / v_scale:.1f}\" for val in x_ticks[::freq]])\n</code>\n</pre> <pre>\n<code>Text(0.5, 0, 'Jitter Noise - Std. Dev (mV)')</code>\n</pre> <pre><code>t_scale = 1e-3  # [ms]\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD / t_scale, si, color=\"b\", linewidth=2)\nax[0].set_ylabel(\"Input Signal (V)\")\nax[1].plot(tD / t_scale, dout, color=\"b\", linewidth=2)\nax[1].set_xlabel(\"Time (ms)\")\nax[1].set_ylabel(\"Output Code\")\nax[0].grid(True)\nax[1].grid(True)\nax[0].set_title(\"Input signal and output code\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Input signal and output code')</code>\n</pre> <pre><code>f_scale = 1e3\np_scale = 1\nvirt_zero = 1e-30\n\nfD = np.fft.fftshift(np.fft.fftfreq(Np, dt))\ndout_f = np.abs(np.fft.fftshift(np.fft.fft(dout) / Np))\ndout_p = dout_f * dout_f\ndout_p[dout_p == 0] = virt_zero\ndout_db = 10 * np.log10(dout_p)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    dout_db[fD &amp;gt;= 0] / p_scale,\n    color=\"gray\",\n    marker=\"None\",\n)\nax.grid(True)\nax.set_xlabel(\"Frequency (KHz)\")\nax.set_ylabel(\"Dout (dB)\")\nfig.tight_layout()\nax.set_title(\"Dout Frequency Spectrum - Jitter Noise Superposition\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Dout Frequency Spectrum - Jitter Noise Superposition')</code>\n</pre> <pre><code>bins = 1\n\nfDp = fD[fD &amp;gt;= 0]\ndout_p_fp = dout_p[fD &amp;gt;= 0]\n\nfin_bin = fDp[np.argmin(np.abs(f1 - fDp))]\np_in = np.sum(dout_p_fp[fDp == fin_bin][:bins])\n\nideal_spectrum = np.zeros(len(dout_p_fp))\nideal_spectrum[fDp == fin_bin] = p_in\n\nnoise_spectrum = dout_p_fp - ideal_spectrum\nnoise = np.mean(noise_spectrum[bins:])\nsnr = 10 * np.log10(p_in / noise)\nprint(f\"Obtained SNR= {snr:.2f} dB\")\n</code></pre> <pre>\n<code>Obtained SNR= 47.99 dB\n</code>\n</pre> <p>The circuit blocks that constitute a converter usually include resistors, capacitors, an amplifier, a comparator, a latch or any other form of CMOS-based circuit block, to perform bit conversions. As passive devices and transistors operate in normal conditions, they dissipate power in the form of heat, while transistors are also subjected to random quantum tunneling effects regarding the gate-accumulated electric charges that break the dielectric, passing from and to the transistor channel. The need arises then to model these effects into palpable random variables that superpose the input/output signal of A/D and D/A converters. Because thermal noise is the strongest of the described noise sources and is present in all converter architectures that are going to be described, only thermal noise will be modelled in this section.</p> <p>The thermal noise associated with a given component is directly proportional to its impedance. The higher the impedance, the higher will be the thermal noise superposed to the processed signal.</p> <p>Power spectral density of Resistor-associated Thermal Noise (voltage variance per hertz/bandwidth): $$ \\langle v^2_{T} \\rangle = 4 k_B T_{(K)} R \\hspace{4 pt} [V^2/Hz]$$</p> <p>Power spectral density of a Capacitor-associated Thermal Noise (voltage variance per hertz/bandwidth):</p> \\[ \\langle v^2_{T} \\rangle = \\frac{ k_B T_{(K)} }{ C } \\hspace{4 pt} [V^2/Hz]\\] <p>Power spectral density of a CMOS Transistor channel-associated Thermal Noise (seen at the gate) (voltage variance per hertz/bandwidth):</p> \\[ \\langle v^2_{T} \\rangle = \\frac{ \\gamma k_B T_{(K)} }{ g_m } \\hspace{4 pt} [V^2/Hz]\\] <p>where $ \\gamma $ is a technology parameter associated with quantum tunneling effects.</p> <p>Consider an ideal ADC converting an input signal superposed by thermal noise. The input signal passes through a sample-and-hold (S&amp;H) switch-capacitor circuit with the following parameters:</p> <ul> <li>Switch on-resistance: 1 kOhm</li> <li>Hold capacitor: 0.1 pF (100 fF)</li> <li>Operating temperature: 85 degrees Celsius</li> </ul> <p>The total thermal noise voltage variance combines the resistor and capacitor contributions over the signal bandwidth.</p> <pre><code>nbit = 10\nvref = 1\nvlsb = vref / (2**nbit)\n\nA2 = vref / 2  # [V]\nf2 = 1e3  # [Hz]\nf2 = np.round(f2 / Fclk * Np) * Fclk / Np  # coherent sampling  # [Hz]\nBW = f2 * 2  # signal bandwidth  # [Hz]\nph2 = 0  # [rad]\n\nmV = 1e-3\ndc_level = vref / 2\n\nR = 1e3  # [Ohm]\nC = 0.1e-12  # [F]\nT_cels = 85  # [deg C]\nT_kelv = T_cels + 273.15  # [K]\n\nvn_t_var = 4 * k * T_kelv * R * BW + BW * k * T_kelv / C  # [V^2]\nvn_t_std = np.sqrt(vn_t_var)  # [V]\nthermal_noise = normal(0, vn_t_std, Np)\nsi = A2 * np.sin(2 * np.pi * f2 * tD + ph2) + dc_level + thermal_noise\ndout = np.round(si / vlsb).astype(int)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nfreq = int(1)\nv_scale = mV\nsns.histplot(thermal_noise, ax=ax, stat=\"probability\", bins=10, kde=True)\nx_ticks = ax.get_xticks()\nax.set_xticklabels([f\"{val / v_scale:.1f}\" for val in x_ticks[::freq]])\nax.set_ylabel(\"Probability\")\nax.set_title(\"Normal Distribution - Thermal Noise\")\nax.set_xlabel(\"Thermal Noise - Std. Dev (mV)\")\n</code></pre> <pre>\n<code>/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_32154/1640861616.py:6: UserWarning: set_ticklabels() should only be used with a fixed number of ticks, i.e. after set_ticks() or using a FixedLocator.\n  ax.set_xticklabels([f\"{val / v_scale:.1f}\" for val in x_ticks[::freq]])\n</code>\n</pre> <pre>\n<code>Text(0.5, 0, 'Thermal Noise - Std. Dev (mV)')</code>\n</pre> <pre><code>t_scale = 1e-3  # [ms]\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD / t_scale, si, color=\"b\", linewidth=2)\nax[0].set_ylabel(\"Input Signal (V)\")\nax[1].plot(tD / t_scale, dout, color=\"b\", linewidth=2)\nax[1].set_xlabel(\"Time (ms)\")\nax[1].set_ylabel(\"Output Code\")\nax[0].grid(True)\nax[1].grid(True)\nax[0].set_title(\"Input signal and output code\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Input signal and output code')</code>\n</pre> <pre><code>f_scale = 1e3\np_scale = 1\nvirt_zero = 1e-30\n\nfD = np.fft.fftshift(np.fft.fftfreq(Np, dt))\ndout_f = np.abs(np.fft.fftshift(np.fft.fft(dout) / Np))\ndout_p = dout_f * dout_f\ndout_p[dout_p == 0] = virt_zero\ndout_db = 10 * np.log10(dout_p)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    dout_db[fD &amp;gt;= 0] / p_scale,\n    color=\"gray\",\n    marker=\"None\",\n)\nax.grid(True)\nax.set_xlabel(\"Frequency (KHz)\")\nax.set_ylabel(\"Dout (dB)\")\nfig.tight_layout()\nax.set_title(\"Dout Frequency Spectrum - Thermal Noise Superposition\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Dout Frequency Spectrum - Thermal Noise Superposition')</code>\n</pre> <pre><code>bins = 1\n\nfDp = fD[fD &amp;gt;= 0]\ndout_p_fp = dout_p[fD &amp;gt;= 0]\n\nfin_bin = fDp[np.argmin(np.abs(f2 - fDp))]\np_in = np.sum(dout_p_fp[fDp == fin_bin][:bins])\n\nideal_spectrum = np.zeros(len(dout_p_fp))\nideal_spectrum[fDp == fin_bin] = p_in\n\nnoise_spectrum = dout_p_fp - ideal_spectrum\nnoise = np.mean(noise_spectrum[bins:])\nsnr = 10 * np.log10(p_in / noise)\nprint(f\"Obtained SNR= {snr:.2f} dB\")\n</code></pre> <pre>\n<code>Obtained SNR= 57.91 dB\n</code>\n</pre> <pre><code>nbit = 10\nvref = 1\nvlsb = vref / (2**nbit)\n\nA3 = vref / 2  # [V]\nf3 = 1e3  # [Hz]\nf3 = np.round(f3 / Fclk * Np) * Fclk / Np  # coherent sampling  # [Hz]\nph3 = 0  # [rad]\nmV = 1e-3\ndc_level = vref / 2\ns3 = A3 * np.sin(2 * np.pi * f3 * tD + ph3) + dc_level\n\np_nq = vlsb**2 / 12  # [V^2]\nvnq = np.sqrt(p_nq)  # [V]\nvnq_t = uniform(-vnq, vnq, Np)\ns3 = s3 + vnq_t\ndout = np.round(s3 / vlsb).astype(int)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nfreq = int(1)\nv_scale = mV\nsns.histplot(vnq_t, ax=ax, stat=\"probability\", bins=10, kde=True)\nx_ticks = ax.get_xticks()\nax.set_xticklabels([f\"{val / v_scale:.1f}\" for val in x_ticks[::freq]])\nax.set_ylabel(\"Probability\")\nax.set_title(\"Uniform Distribution - Quantization Noise\")\nax.set_xlabel(\"Quantization Noise - Std. Dev (mV)\")\n</code></pre> <pre>\n<code>/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_32154/3859448297.py:6: UserWarning: set_ticklabels() should only be used with a fixed number of ticks, i.e. after set_ticks() or using a FixedLocator.\n  ax.set_xticklabels([f\"{val / v_scale:.1f}\" for val in x_ticks[::freq]])\n</code>\n</pre> <pre>\n<code>Text(0.5, 0, 'Quantization Noise - Std. Dev (mV)')</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD, s3, color=\"b\", linewidth=2)\nax[0].set_ylabel(\"Input Signal (V)\")\nax[1].plot(tD, dout, color=\"b\", linewidth=2)\nax[1].set_xlabel(\"Time (s)\")\nax[1].set_ylabel(\"Output Code\")\nax[0].grid(True)\nax[1].grid(True)\nax[0].set_title(\"Input signal and output code\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Input signal and output code')</code>\n</pre> <pre><code>f_scale = 1e3\np_scale = 1\nfD = np.fft.fftshift(np.fft.fftfreq(Np, dt))\nnew_zero = 1e-30\n\ndout_f = np.abs(np.fft.fftshift(np.fft.fft(dout) / Np))\ndout_p = dout_f * dout_f\ndout_p[dout_p == 0] = new_zero\ndout_db = 10 * np.log10(dout_p)\n</code></pre> <pre><code>bins = 1\n\nfDp = fD[fD &amp;gt;= 0]\ndout_p_fp = dout_p[fD &amp;gt;= 0]\n\nfin_bin = fDp[np.argmin(np.abs(f3 - fDp))]\np_in = np.sum(dout_p_fp[fDp == fin_bin][:bins])\n\nideal_spectrum = np.zeros(len(dout_p_fp))\nideal_spectrum[fDp == fin_bin] = p_in\n\nnoise_spectrum = dout_p_fp - ideal_spectrum\nnoise = np.mean(noise_spectrum[bins:])\nsnr = 10 * np.log10(p_in / noise)\nprint(f\"Obtained SNR= {snr:.2f} dB\")\n</code></pre> <pre>\n<code>Obtained SNR= 88.64 dB\n</code>\n</pre> <p>IMPORTANT NOTE: The obtained SNR considering quantization noise sets the theoretical maximum of the signal-to-noise ratio of the converter.</p> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(\n    fD[fD &amp;gt;= 0] / f_scale,\n    dout_db[fD &amp;gt;= 0] / p_scale,\n    color=\"gray\",\n    marker=\"None\",\n)\nax.grid(True)\nax.set_xlabel(\"Frequency (KHz)\")\nax.set_ylabel(\"Dout (dB)\")\nfig.tight_layout()\nax.set_title(\"Dout Frequency Spectrum - Quantization Noise Superposition\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Dout Frequency Spectrum - Quantization Noise Superposition')</code>\n</pre>"},{"location":"notebooks/practical_class_5/#practical-class-5-noise-modelling","title":"Practical Class 5 - Noise modelling","text":""},{"location":"notebooks/practical_class_5/#jitter-noise-modelling","title":"Jitter Noise Modelling","text":"<p>As stated before, jitter noise can be thought of as a representation of the time uncertainty at which the sampling switches sampled an analog signal. The uncertainty at which the time the sample was taken will introduce an error on the value of the sampled signal, at the corresponding sampling instant.</p> <p></p> <p>The sampled signal error can be represented by: $$ s[n] = s(nT_S + \\Delta t_j) = s(nT_S) + \\epsilon_s$$</p> <p>Considering a sinusoidal signal s(t) given by:</p> \\[ s(t) = A \\hspace{2 pt} sin(2\\pi f_S t + \\phi) \\] <p>Then using a first order Taylor series approximation of the sampled sinusoidal signal, we can obtain the contribution of the jitter noise to the error of the sampled signal:</p> \\[ s[n] = A \\hspace{2 pt} sin(2\\pi f_S (nT_S + \\Delta t_j)) \\approx A \\hspace{2 pt} sin(2\\pi f_S (nT_S)) + A 2\\pi f_S \\Delta t_j \\hspace{2 pt} cos(2\\pi f_S (nT_S) ) \\] <p>The power of the resulting signal can be separated into two main components, the power of the signal and the power of the jitter noise:</p> \\[ P = P_s + P_j = \\frac{A^2}{2} + \\frac{A^2}{2}f_S^24\\pi^2\\sigma_t^2 \\] <p>where \\(\\sigma_t\\) is the standard deviation of the random variable with normal distribution modelling the time uncertainty error \\(\\Delta t_j\\) - referred to as jitter -, \\(f_S\\) is the sampling frequency and \\(A\\) is the amplitude of the analog signal being sampled.</p>"},{"location":"notebooks/practical_class_5/#gaussian-noise-sources-thermal-noise","title":"Gaussian Noise Sources - Thermal Noise","text":""},{"location":"notebooks/practical_class_5/#quantization-noise-modelling","title":"Quantization Noise Modelling","text":"<p>Each conversion of a signal converter system is prone to quantization error, once the converter parses the signal between the digital and analog domain using a discrete quantization step (Vlsb). As such, each converter signal sample can present an error between [-Vlsb/2 ; Vlsb/2]. Because all systems are subjected to noise, the signal is always superposed by random gaussian noise, and this noise is also converted with the signal in the process of parsing from digital to analog domains. If the superposed noise of the signal doesn't surpass the threshold of |Vlsb/2| level, then the sufficiently random nature of the converted analog signal enables quantization error to be well approximated by white noise - a random noise variable with a uniform distribution of probability - becoming Quantization Noise.</p>"},{"location":"notebooks/practical_class_5/#total-output-noise","title":"Total Output Noise","text":"<p>When modelling a converter, the identified independent noise sources must be superposed to the converter's output signal, in order to obtain an accurate measurement of its SNR and linearity, as exemplified in the image below regarding the high-level model of an ideal ADC with superposed noise sources to develop a non-ideal model.</p> <p></p>"},{"location":"notebooks/practical_class_6/","title":"6. Resistive DAC Modeling","text":"<pre><code>from functools import partial\n\nimport numpy as np\nfrom numpy.random import normal\nimport seaborn as sns\n\nsns.set_theme()\nsns.set_palette(\"colorblind\")\n\nimport matplotlib.pyplot as plt\nfrom utils import dec2bin\n\n%matplotlib inline\n</code></pre> <pre><code>vref = 1  # V\nnbits = 8\nvlsb = vref / (2**nbits)  # V\nNp = 1e6\nFs = 1e9  # Hz\nTs = 1 / Fs  # s\ntD = np.arange(0, Np) * Ts\n\nA1 = vref / 2\nf1 = 1e3  # Hz\nph1 = 0  # rad\nofst = 0.0\nsi = A1 * np.sin(2 * np.pi * f1 * tD + ph1) + ofst + vref / 2\nincodes = np.round(si / vlsb).astype(int)\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD, si, \"b\", label=\"input signal\")\nax[0].set_xlabel(\"time (s)\")\nax[0].set_ylabel(\"voltage (V)\")\nax[0].legend()\nax[1].plot(tD, incodes, \"r\", label=\"input codes\")\nax[1].set_xlabel(\"time (s)\")\nax[1].set_ylabel(\"codes\")\nax[1].legend()\nax[0].set_xlim([0, 1 / f1])\nax[1].set_xlim([0, 1 / f1])\nax[1].grid(True)\nax[0].grid(True)\n</code></pre> <pre><code>ru = 100  # ohm\nR = np.ones(2**nbits) * ru\nu_err = 0.0\nu_std = 0.05  # 5% of the unit resistance\nr_err = normal(u_err, u_std, 2**nbits)\nR = R + r_err * ru\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(R, ax=ax, stat=\"density\", kde=True)\nax.set_xlabel(\"resistor value (ohm)\")\nax.set_ylabel(\"probability density\")\nax.grid(True)\nax.set_title(\"resistor values distribution\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'resistor values distribution')</code>\n</pre> <pre><code>codes = np.arange(2**nbits)\nvout_nonideal = np.array([np.sum(R[:code]) / np.sum(R) for code in codes])\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.step(codes, vout_nonideal, \"b\", label=\"DAC output\")\nax.set_xlabel(\"input codes\")\nax.set_ylabel(\"output voltage (V)\")\nax.legend()\nax.grid(True)\nax.set_title(\"DAC transfer function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC transfer function')</code>\n</pre> <pre><code>vlsb_measured = (vout_nonideal[2**nbits - 1] - vout_nonideal[0]) / (2**nbits - 1)\ndnl = np.diff(vout_nonideal) / vlsb_measured - 1\ninl = np.array([0] + np.cumsum(dnl).tolist())\nassert len(dnl) == 2**nbits - 1\nassert len(inl) == 2**nbits\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(codes[1:], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(codes, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <p>As it is possible to observe, the DAC is sensitive to resistor mismatch, severely impacting its INL, which translates in VLSB errors when producing the output voltage. Let us now compute the effective resolution of 100 thermometer ladder DAC converters for a random resistor mismatch following a normal distribution, and check the histogram of the effective resolution of the DACs. This is a very common analysis to check the impact of device mismatch on the performance of a converter circuit.</p> <p>The effective number of bits (ENOB) of the DAC is computed using the INL: the lost bits correspond to the difference between the minimum and maximum values of the INL, so ENOB = N - log2(max(INL) - min(INL)).</p> <pre><code>ncircuits = 100\ndac_res = []\nu_err = 0.0\nu_std = 0.05  # 5% of the unit resistance\nfor circ in range(ncircuits):\n    R = np.ones(2**nbits) * ru\n    r_err = normal(u_err, u_std, 2**nbits)\n    R = R + r_err * ru\n    codes = np.arange(2**nbits)\n    vout_nonideal = np.array([np.sum(R[:code]) / np.sum(R) for code in codes])\n    vlsb_measured = (vout_nonideal[2**nbits - 1] - vout_nonideal[0]) / (2**nbits - 1)\n    dnl = np.diff(vout_nonideal) / vlsb_measured - 1\n    inl = np.array([0] + np.cumsum(dnl).tolist())\n    dac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n    dac_res.append(dac_linearity)\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(dac_res, ax=ax, stat=\"density\", kde=True)\nax.set_xlabel(\"effective number of bits\")\nax.set_ylabel(\"probability density function of each circuit\")\nax.grid(True)\nax.set_title(\"DAC resolution distribution against resistor mismatch\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC resolution distribution against resistor mismatch')</code>\n</pre> <p>Let us now consider the dynamic operation of the DAC. Assume that the DAC's output voltage is charging a parasitic capacitor of 0.1 pF, and the DAC is clocked at 1 MHz. Due to the output node pole defined by its RC time constant, the output voltage will not change instantaneously but will follow a step response. Three main questions arise:</p> <ol> <li>How long does it take to charge the output capacitance to 95% of the final value?</li> <li>What impact do the unit resistances have on the step response?</li> <li>What impact do the switches have on the step response?</li> </ol> <p>The settling error is:</p> \\[\\epsilon_{S} = \\frac{V^*_{out} - V_{out}}{V_{out}} = e^{-\\frac{T_{CLK}}{\\tau}}\\] <p>where \\(V_{out} = V_{REF}\\frac{R_2}{R_2 + R_1}\\) is the ideal output voltage, and \\(V^*_{out}(t) = V_{REF}\\frac{R_2}{R_2 + R_1}(1-e^{-t/\\tau})\\) is the actual output voltage. The RC time constant is:</p> \\[\\tau = (C_L + C_P) \\cdot (R_1 \\| R_2 + R_{SW})\\] <p>where \\(C_L\\) is the load capacitance, \\(C_P\\) is the parasitic capacitance, \\(R_1\\) and \\(R_2\\) are the top and bottom equivalent resistances of the ladder, and \\(R_{SW}\\) is the switch ON resistance.</p> <pre><code>vo = lambda t_, vr_, r1_, r2_, tau_: vr_ * r2_ / (r1_ + r2_) * (1 - np.exp(-t_ / tau_))\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nCL = 1e-12  # F\nru = 1e3  # ohm\nnum_rus = 3\nrus = np.arange(1, num_rus + 1) * ru\nr_sw = 100  # ohm\ncp_sw = 0.01 * CL  # F\nfclk = 100e6  # Hz\ncolors = [\"b\", \"r\", \"k\", \"c\", \"m\", \"y\", \"g\"]\nfor ru, color in zip(rus, colors):\n    R = np.ones(2**nbits) * ru\n    assert len(R) == 2**nbits\n    req1 = sum(R[:-1])  # equivalent resistance of the top of the ladder for code 0\n    req2 = R[-1]  # equivalent resistance of the bottom of the ladder for code 1\n    tclk = 1 / fclk  # s\n    tauu = (CL + cp_sw) * (\n        (req1 * req2) / (req1 + req2) + r_sw\n    )  # tau = (CL + Cp) * (R1//R2 + Rsw)\n    vout_t = vo(tD, vref, req1, req2, tauu)\n    err_set = np.exp(-tclk / tauu)\n    err_set_pct = err_set * 100\n    ax.plot(\n        tD / 1e-9,\n        vout_t / vlsb,\n        color,\n        label=\"R = {:.1e} k\".format(ru)\n        + r\"$\\Omega$\"\n        + \"\\nSettling Error = {:.2f}%\".format(err_set_pct),\n    )\nax.set_xlabel(\"time (ns)\")\nax.set_ylabel(\"voltage (LSBs)\")\nax.grid(True)\nax.legend()\nax.set_title(\"step response for a transition\\n between code 0 and code 1\")\nax.set_xlim(0, tclk / 1e-9)\n</code></pre> <pre>\n<code>(0.0, 10.0)</code>\n</pre> <pre><code>codes = np.arange(2**nbits)\nru = 1e3  # ohm\nR = np.ones(2**nbits) * ru\ntauu = np.array(\n    [\n        (CL + cp_sw)\n        * (\n            (np.sum(R[:code]) * np.sum(R[code:]))\n            / (np.sum(R[:code]) + np.sum(R[code:]))\n            + r_sw\n        )\n        for code in codes[1:]\n    ]\n)\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 3))\nax.plot(codes[1:], tauu / tclk, \"b\", label=\"DAC delay\")\nax.set_xlabel(\"input codes\")\nax.set_ylabel(r\"$\\tau / T_{CLK}$\")\nax.legend()\nax.grid(True)\nax.set_title(\"DAC Output RC Delay vs. Input Code Transition\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC Output RC Delay vs. Input Code Transition')</code>\n</pre> <pre><code>ru = 1e3  # ohm\nR = np.ones(2**nbits) * ru\nPD = (vref**2) / (len(R) * ru)  # W\n</code></pre> <p>One conclusion from the previous analysis is that reducing the overall delay of the DAC requires reducing the RC time constant of the output node by lowering the unit resistance of the ladder. However, the lower the unit resistance, the higher the mismatch sensitivity and the total static power dissipation.</p> <p>This observation holds true in general for all DAC and ADC architectures. The total dissipated power will inevitably be higher when aiming for converters that present a lower settling error and operate at faster conversion rates.</p> <pre><code>vref = -1  # V\nvdd = 1  # V\nnbits = 8\nvlsb = np.abs(vref / (2**nbits))  # V\nNp = 1e6\nFs = 1e9  # Hz\nTs = 1 / Fs  # s\ntD = np.arange(0, Np) * Ts\n\nA1 = vdd / 2\nf1 = 1e3  # Hz\nph1 = 0  # rad\nofst = 0.0\nsi = A1 * np.sin(2 * np.pi * f1 * tD + ph1) + ofst + vdd / 2\nincodes = np.round(si / vlsb).astype(int)\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD, si, \"b\", label=\"input signal\")\nax[0].set_xlabel(\"time (s)\")\nax[0].set_ylabel(\"voltage (V)\")\nax[0].legend()\nax[1].plot(tD, incodes, \"r\", label=\"input codes\")\nax[1].set_xlabel(\"time (s)\")\nax[1].set_ylabel(\"codes\")\nax[1].legend()\nax[0].set_xlim([0, 1 / f1])\nax[1].set_xlim([0, 1 / f1])\nax[1].grid(True)\nax[0].grid(True)\n</code></pre> <pre><code>ru = 100  # ohm\nR_ideal = np.array([ru * (2**i) for i in range(nbits)])\nu_err = 0.0\nu_std = 0.05  # 5% of the unit resistance\nr_err = normal(u_err, u_std, nbits)\nR = np.array([r + r_err[i] * r for i, r in enumerate(R_ideal)])\nrf = ru / 2  # feedback resistor\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(r_err, ax=ax, stat=\"density\", kde=True)\nax.set_xlabel(\"resistor std error (%)\")\nax.set_ylabel(\"probability density\")\nax.grid(True)\nax.set_title(\"resistor values distribution\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'resistor values distribution')</code>\n</pre> <p>Note: The Most Significant Bit (MSB) is associated with the smallest resistor of the array in the DAC (the unit resistor Ru). This is because the MSB corresponds to the highest voltage change, and consequently must drive the highest current through the feedback resistor -- which occurs through the lowest resistance in the array.</p> <pre><code>codes = np.arange(2**nbits)\nincodes_bin = dec2bin(codes, nbits)\n\nG = np.array([1 / r for r in R])\ncurrents = np.array([vref * np.sum(G[icodes == 1]) for icodes in incodes_bin])\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 3))\nax.plot(codes, currents, \"b\", label=\"currents\")\nax.set_xlabel(\"codes\")\nax.set_ylabel(\"currents (A)\")\nax.grid(True)\nax.legend()\n\nvout_nonideal = -currents * rf\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 3))\nax.plot(codes, vout_nonideal, \"b\", label=\"DAC transfer function\")\nax.set_xlabel(\"input codes\")\nax.set_ylabel(\"output voltage (V)\")\nax.grid(True)\nax.set_title(\"Binary Weighted Resistor DAC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Binary Weighted Resistor DAC Transfer Function')</code>\n</pre> <pre><code>vlsb_measured = (vout_nonideal[2**nbits - 1] - vout_nonideal[0]) / (2**nbits - 1)\ndnl = np.diff(vout_nonideal) / vlsb_measured - 1\ninl = np.array([0] + np.cumsum(dnl).tolist())\nassert len(dnl) == 2**nbits - 1\nassert len(inl) == 2**nbits\n\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(codes[1:], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(codes, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <pre><code>ncircuits = 100\ndac_res = []\nu_err = 0.0\nu_std = 0.05  # 5% of the unit resistance\ncodes = np.arange(2**nbits)\nincodes_bin = dec2bin(codes, nbits)\nfor circ in range(ncircuits):\n    R_ideal = np.array([ru * (2**i) for i in range(-1, nbits)])\n    rf = R_ideal[0]  # feedback resistor\n    R_ideal = R_ideal[1:]  # resistor array\n    u_err = 0.0\n    u_std = 0.15  # 15% of the unit resistance\n    r_err = normal(u_err, u_std, nbits)\n    R = R_ideal + np.multiply(R_ideal, r_err)\n    G = np.array([1 / r for r in R])\n    currents = np.array([vref * np.sum(G[icodes == 1]) for icodes in incodes_bin])\n    vout_nonideal = -currents * rf\n    vlsb_measured = (vout_nonideal[2**nbits - 1] - vout_nonideal[0]) / (2**nbits - 1)\n    dnl = np.diff(vout_nonideal) / vlsb_measured - 1\n    inl = np.array([0] + np.cumsum(dnl).tolist())\n    dac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n    dac_res.append(dac_linearity)\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(dac_res, ax=ax, stat=\"density\", kde=True)\nax.set_xlabel(\"effective number of bits\")\nax.set_ylabel(\"probability density function of each circuit\")\nax.grid(True)\nax.set_title(\"DAC resolution distribution against resistor mismatch\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC resolution distribution against resistor mismatch')</code>\n</pre> <p>As it is possible to observe, the binary weighted resistor DAC is much more sensitive to resistor mismatch than the thermometer ladder DAC. This is because the higher the resistor's resistance, the higher the deviation (in ohms) from the ideal value, although it remains the same in percentage terms. As such, transitions associated with input codes featuring changes in the least significant bits will be more sensitive to resistor mismatch than transitions associated with changes only in the most significant bits.</p> <p>Note: In this layout configuration, there are \\(2^N - 1\\) resistors in the array, whereas in the previous configuration there were only N resistors. The smaller resistors are formed by parallel combinations of the unit resistor, reducing the effective mismatch.</p> <pre><code>ncircuits = 100\ndac_res = []\nru = 2e3  # ohm\nu_err = 0.0\nu_std = 0.05  # 5% of the unit resistance\ncodes = np.arange(2**nbits)\nincodes_bin = dec2bin(codes, nbits)\nfor circ in range(ncircuits):\n    R_ideal = np.ones(2**nbits - 1) * ru\n    R_idxs = {id: np.arange(2**id, 2 ** (id + 1)) - 1 for id in range(nbits)}\n    u_err = 0.0\n    u_std = 0.15  # 15% of the unit resistance\n    r_err = normal(u_err, u_std, len(R_ideal))\n    R = R_ideal + np.multiply(R_ideal, r_err)\n    R = np.array([1 / (np.sum([1 / R[i] for i in R_idxs[id]])) for id in range(nbits)])\n    R = R[::-1]  # reverse to associate MSB with lowest resistance\n    rf = ru / 2  # feedback resistor\n    G = np.array([1 / r for r in R])\n    currents = np.array([vref * np.sum(G[icodes == 1]) for icodes in incodes_bin])\n    vout_nonideal = -currents * rf\n    vlsb_measured = (vout_nonideal[2**nbits - 1] - vout_nonideal[0]) / (2**nbits - 1)\n    dnl = np.diff(vout_nonideal) / vlsb_measured - 1\n    inl = np.array([0] + np.cumsum(dnl).tolist())\n    dac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n    dac_res.append(dac_linearity)\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(dac_res, ax=ax, stat=\"density\", kde=True)\nax.set_xlabel(\"effective number of bits\")\nax.set_ylabel(\"probability density function of each circuit\")\nax.grid(True)\nax.set_title(\"DAC resolution distribution against resistor mismatch\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC resolution distribution against resistor mismatch')</code>\n</pre> <p>From the above histogram, it is possible to observe that the implementation of the binary weighted resistor array designed through parallel combinations of the highest unitary resistor enables the reduction of the mismatch sensitivity of the DAC, increasing its effective resolution.</p> <pre><code>vref = 1  # V\nnbits = 8\nvlsb = vref / (2**nbits)  # V\nNp = 1e6\nFs = 1e9  # Hz\nTs = 1 / Fs  # s\ntD = np.arange(0, Np) * Ts\n\nA4 = vref / 2\nf4 = 1e3  # Hz\nph4 = 0  # rad\nofst = 0.0\nsi = A4 * np.sin(2 * np.pi * f4 * tD + ph4) + ofst + vref / 2\nincodes = np.round(si / vlsb).astype(int)\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD, si, \"b\", label=\"input signal\")\nax[0].set_xlabel(\"time (s)\")\nax[0].set_ylabel(\"voltage (V)\")\nax[0].legend()\nax[1].plot(tD, incodes, \"r\", label=\"input codes\")\nax[1].set_xlabel(\"time (s)\")\nax[1].set_ylabel(\"codes\")\nax[1].legend()\nax[0].set_xlim([0, 1 / f4])\nax[1].set_xlim([0, 1 / f4])\nax[1].grid(True)\nax[0].grid(True)\n</code></pre> <pre><code>ru = 100  # ohm\nrv = 2 * ru * np.ones(nbits + 1)  # vertical resistors = 2*Ru\nrh = ru * np.ones(nbits - 1)  # horizontal resistors = Ru\nu_err = 0.0\nu_std = 0.05  # 5% of the unit resistance\nr_err = normal(u_err, u_std, nbits + 1)\nRv = rv + rv * r_err\nRg = Rv[-1]\nRv = Rv[:-1]\nr_err = normal(u_err, u_std, nbits - 1)\nRh = rh + rh * r_err\nRf = ru * 2**nbits  # feedback resistor\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 2, figsize=(9, 4))\nsns.histplot(Rv, ax=ax[0], stat=\"probability\", kde=True)\nax[0].set_xlabel(\"resistance (ohm)\")\nax[0].set_ylabel(\"probability density\")\nax[0].grid(True)\nax[0].set_title(\"vertical resistor\\nvalues distribution\")\nsns.histplot(Rh, ax=ax[1], stat=\"probability\", kde=True)\nax[1].set_xlabel(\"resistance (ohm)\")\nax[1].set_ylabel(\"probability density\")\nax[1].grid(True)\nax[1].set_title(\"horizontal resistor\\nvalues distribution\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'horizontal resistor\\nvalues distribution')</code>\n</pre> <pre><code>nodeVolt = np.zeros(nbits)  # node voltages, LSB to MSB\nreqs = np.zeros(nbits)  # equivalent resistances at each node, LSB to MSB\nreqs[0] = Rg * Rv[0] / (Rg + Rv[0])\n\n\ndef reqf(node, Rv=None, Rh=None, Req=None):\n    Req[node] = (\n        (Rh[node - 1] + Req[node - 1])\n        * Rv[node]\n        / (Rh[node - 1] + Rv[node] + Req[node - 1])\n    )\n\n\nreqfp = partial(reqf, Rv=Rv, Rh=Rh, Req=reqs)\nreqvf = np.vectorize(reqfp)\nreqvf(np.arange(1, nbits))\n\nnodeVolt[-1] = -vref\n\n\ndef vnf(node, nodeVolt=None, Req=None, Rh=None):\n    nodeVolt[node] = nodeVolt[node + 1] * Req[node] / (Req[node] + Rh[node])\n\n\nvnfp = partial(vnf, nodeVolt=nodeVolt, Req=reqs, Rh=Rh)\nvnvf = np.vectorize(vnfp)\nvnvf(np.arange(nbits - 1)[::-1])\nnodeVolt = nodeVolt[::-1]\n\ncurrents = nodeVolt / Rv\ncodes = np.arange(2**nbits)\nincodes_bin = dec2bin(codes, nbits)\ncurrent_out = np.array([np.sum(currents[incode == 1]) for incode in incodes_bin])\nvout = -current_out * Rf\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(np.arange(nbits), nodeVolt, \"b\", label=\"node voltages\")\nax[0].set_xlabel(\"node\")\nax[0].set_ylabel(\"voltage (V)\")\nax[0].grid(True)\nax[1].step(np.arange(nbits), reqs, \"r\", label=\"equivalent resistances\")\nax[1].set_xlabel(\"node\")\nax[1].set_ylabel(\"resistance (ohm)\")\nax[1].axhline(\n    np.mean(reqs),\n    color=\"k\",\n    linestyle=\"--\",\n    label=\"mean= {:.2f} ohm\".format(np.mean(reqs)),\n)\nax[1].legend()\nax[1].grid(True)\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(codes, current_out / 1e-3, c=\"k\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"output current (mA)\")\nax[0].grid(True)\nax[1].plot(codes, vout, c=\"k\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"output voltage (V)\")\nax[1].grid(True)\n</code></pre> <pre><code>vlsb_measured = (vout[-1] - vout[0]) / (2**nbits - 1)\ndnl = np.diff(vout) / vlsb_measured - 1\ninl = np.array([0] + np.cumsum(dnl).tolist())\nassert np.allclose(inl[-1], 0)\nassert np.allclose(inl[0], 0)\nassert len(dnl) == 2**nbits - 1\nassert len(inl) == 2**nbits\n\ndac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(codes[1:], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(codes, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <pre><code>ncircuits = 100\ndac_res = []\nru = 2e3  # ohm\nu_err = 0.0\nu_std = 0.05  # 5% of the unit resistance\ncodes = np.arange(2**nbits)\nincodes_bin = dec2bin(codes, nbits)\n\nfor circ in range(ncircuits):\n    ru = 100  # ohm\n    rv = 2 * ru * np.ones(nbits + 1)  # vertical resistors = 2*Ru\n    rh = ru * np.ones(nbits - 1)  # horizontal resistors = Ru\n    u_err = 0.0\n    sigma = 10  # consider: 1 sigma, 2 sigma or 3 sigma (worst case scenario)\n    u_std = 0.15  # 15% of the unit resistance\n    r_err = normal(u_err, u_std, nbits + 1)\n    Rv = rv + rv * r_err\n    Rg = Rv[-1]\n    Rv = Rv[:-1]\n    r_err = normal(u_err, u_std, nbits - 1)\n    Rh = rh + rh * r_err\n    Rf = ru * 2**nbits  # feedback resistor\n\n    nodeVolt = np.zeros(nbits)\n    reqs = np.zeros(nbits)\n    reqs[0] = Rg * Rv[0] / (Rg + Rv[0])\n    reqfp = partial(reqf, Rv=Rv, Rh=Rh, Req=reqs)\n    reqvf = np.vectorize(reqfp)\n    reqvf(np.arange(1, nbits))\n\n    nodeVolt[-1] = -vref\n    vnfp = partial(vnf, nodeVolt=nodeVolt, Req=reqs, Rh=Rh)\n    vnvf = np.vectorize(vnfp)\n    vnvf(np.arange(nbits - 1)[::-1])\n    nodeVolt = nodeVolt[::-1]\n\n    currents = nodeVolt / Rv\n    codes = np.arange(2**nbits)\n    incodes_bin = dec2bin(codes, nbits)\n    current_out = np.array([np.sum(currents[incode == 1]) for incode in incodes_bin])\n    vout = -current_out * Rf\n\n    vlsb_measured = (vout[-1] - vout[0]) / (2**nbits - 1)\n    dnl = np.diff(vout) / vlsb_measured - 1\n    inl = np.array([0] + np.cumsum(dnl).tolist())\n    dac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n    dac_res.append(dac_linearity)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(dac_res, ax=ax, stat=\"probability\", kde=True)\nax.set_xlabel(\"effective number of bits\")\nax.set_ylabel(\"probability density function of each circuit\")\nax.grid(True)\nax.set_title(\"DAC resolution distribution against resistor mismatch\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC resolution distribution against resistor mismatch')</code>\n</pre> <p>From the above histogram it is possible to observe that although the R-2R DAC uses much less hardware, drastically reducing the number of resistors to 2N + 1, it is much more sensitive to resistor mismatch than the binary weighted resistor DAC. The decreasing voltage at each successive node of the R-2R array is successively more sensitive to resistor mismatch, as the mismatch at higher voltage nodes is much more critical than the mismatch at lower voltage nodes.</p>"},{"location":"notebooks/practical_class_6/#practical-class-6-modelling-of-resistive-dacs-at-element-level","title":"Practical Class 6 - Modelling of Resistive DACs at element level","text":""},{"location":"notebooks/practical_class_6/#modelling-of-a-thermometer-code-dac","title":"Modelling of a Thermometer Code DAC","text":""},{"location":"notebooks/practical_class_6/#modelling-of-a-binary-weighted-resistor-dac","title":"Modelling of a Binary Weighted Resistor DAC","text":"<p>Note: B0 is the most significant bit of the DAC, associated with the lowest resistor value in the resistor array, and subsequently with the highest voltage output change when the bit is toggled.</p>"},{"location":"notebooks/practical_class_6/#reducing-mismatch-sensitivity-of-binary-weighted-resistor-arrays","title":"Reducing mismatch sensitivity of binary weighted resistor arrays","text":"<p>In practice, one way to reduce the mismatch sensitivity of the DAC is to correctly design the resistor array in the circuit's layout. Instead of using variable length polysilicon resistors, a single fixed length and width resistor is used for the highest resistor of the array, and the subsequent resistors are implemented as parallel combinations of a power-of-two number of this highest resistance resistor to make the smaller resistors of the array. Because the standard deviation of the mismatch value of the highest resistor is the same for all the resistors of the array, the mismatch sensitivity of the DAC is reduced since the effective mismatch of the smaller resistors (made by parallel combinations of the resistor with the highest mismatch) decreases.</p> <p>The tradeoff is the in-silicon occupied area. In the classic configuration there are N resistors in the array for a DAC with N bits of resolution, whereas in the proposed configuration there are \\(2^{N} - 1\\) resistors in the array.</p>"},{"location":"notebooks/practical_class_6/#modelling-a-r-2r-dac","title":"Modelling a R-2R DAC","text":""},{"location":"notebooks/practical_class_7/","title":"7. Charge Redistribution DAC","text":"<pre><code>from functools import partial\n\nimport numpy as np\nfrom numpy.random import normal\nimport matplotlib.pyplot as plt\nimport seaborn as sns\n\nsns.set_theme()\nsns.set_palette(\"colorblind\")\n\nfrom utils import dec2bin\n\n%matplotlib inline\n</code></pre> <pre><code>Np = 1000\nFs = 1e9  # Hz\nTs = 1 / Fs  # s\ntD = np.arange(0, Np) * Ts\n</code></pre> <pre><code>vref = 1  # V\nnbits = 8\nvlsb = vref / (2**nbits)  # V\n\nA1 = vref / 2\nf1 = 10e6  # Hz\nph1 = 0  # rad\nofst = 0.0\nsi = A1 * np.sin(2 * np.pi * f1 * tD + ph1) + ofst + vref / 2\nincodes = np.round(si / vlsb).astype(int)\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD, si, \"b\", label=\"input signal\")\nax[0].set_xlabel(\"time (s)\")\nax[0].set_ylabel(\"voltage (V)\")\nax[0].legend()\nax[1].plot(tD, incodes, \"r\", label=\"input codes\")\nax[1].set_xlabel(\"time (s)\")\nax[1].set_ylabel(\"codes\")\nax[1].legend()\nax[0].set_xlim([0, 1 / f1])\nax[1].set_xlim([0, 1 / f1])\nax[1].grid(True)\nax[0].grid(True)\n</code></pre> <pre><code>fF = 1e-15  # femto Farad\npF = 1e-12  # pico Farad\n\ncu = 2 * fF  # unit capacitance\nc_array = cu * 2 ** np.arange(nbits)\nc_array = np.array([cu] + list(c_array))\n</code></pre> <pre><code>u_dev = 0\nstd_dev = 0.05  # 5%-15% capacitance mismatch\nc_mismatch = normal(u_dev, std_dev, size=c_array.shape)\nc_array = c_array + c_mismatch * c_array\n</code></pre> <pre><code>def charge_ph1(incode, c_array, vref, ofst=0, cpb=0, cpvn=0, invert_code=True):\n    \"\"\"Computes the total accumulated charge in phase 1.\n\n    It is considered that:\n        - the input code is M(N,1) from LSB to MSB\n        - the capacitor array is M(N,1) from LSB Cap to MSB Cap\n\n    Params:\n        incode: input code\n        c_array: capacitor array\n        vref: reference voltage\n        ofst: offset voltage of the amplifier\n        cpb: parasitic capacitance at the bottom plates of the capacitors\n        cpvn: parasitic capacitance at the virtual v- input node of the amplifier\n        invert_code: flag to invert the input code\n\n    Return:\n        charge_ph1: total accumulated charge in phase 1\n    \"\"\"\n    icode = incode[:: -1 if invert_code else 1]\n    return (\n        (ofst - vref) * (icode.reshape((1, -1)) @ c_array.reshape((-1, 1)))\n        + vref * cpb\n        + ofst * cpvn\n    )\n\n\ndef charge_ph2(c_array, vout, ofst=0, cpb=0, cpvn=0):\n    \"\"\"Computes the total accumulated charge in phase 2.\"\"\"\n    return (ofst - vout) * np.sum(c_array) + vout * cpb + ofst * cpvn\n\n\ndef vout_dac(incode, c_array=None, vref=1, ofst=0, cpb=0, cpvn=0, invert_code=True):\n    \"\"\"Computes the output voltage of the DAC.\n\n    It is considered that:\n        - the input code is M(N,1) from LSB to MSB\n        - the capacitor array is M(N+1,1) from LSB Cap to MSB Cap\n\n    Params:\n        incode: input code\n        c_array: capacitor array\n        vref: reference voltage\n        ofst: offset voltage of the amplifier\n        cpb: parasitic capacitance at the bottom plates of the capacitors\n        cpvn: parasitic capacitance at the virtual v- input node of the amplifier\n    \"\"\"\n    return (\n        vref\n        * charge_ph1(incode, c_array[1:], vref, ofst, cpb, cpvn, invert_code)\n        / charge_ph2(c_array, vref, ofst, cpb, cpvn)\n    )\n</code></pre> <p>In this case, the offset of the amplifier is null, as well as the parasitic capacitances.</p> <pre><code>codes = np.arange(2**nbits)\nincodes_bin = dec2bin(codes, nbits)\n\nvof = partial(\n    vout_dac, c_array=c_array, vref=vref, ofst=0, cpb=0, cpvn=0, invert_code=True\n)\nvodac = np.vectorize(vof, signature=\"(k)-&amp;gt;(m)\")\nvout = vodac(incodes_bin).flatten()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.step(codes, vout, \"k\", label=\"output voltage\")\nax.set_xlabel(\"input code\")\nax.set_ylabel(\"output voltage (V)\")\nax.grid(True)\nax.set_title(\"DAC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC Transfer Function')</code>\n</pre> <pre><code>vlsb_measured = (vout[2**nbits - 1] - vout[0]) / (2**nbits - 1)\ndnl = np.diff(vout) / vlsb_measured - 1\ninl = np.array([0] + np.cumsum(dnl).tolist())\nassert len(dnl) == 2**nbits - 1, \"DNL length is not correct: {}\".format(len(dnl))\nassert len(inl) == 2**nbits\n\ndac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\ndac_linearity\n</code></pre> <pre>\n<code>np.float64(5.115060342301531)</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(codes[1:], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(codes, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <p>Consider now a 10 mV offset voltage of the amplifier and a parasitic capacitance equal to 10% of the total capacitance of the DAC.</p> <pre><code>cpvn = 0.1 * np.sum(c_array)  # parasitic capacitance at the v- node\ncpb = cpvn  # parasitic capacitance at the bottom plates\nvoff = 10e-3  # V\nvof = partial(\n    vout_dac,\n    c_array=c_array,\n    vref=vref,\n    ofst=voff,\n    cpb=cpb,\n    cpvn=cpvn,\n    invert_code=True,\n)\nvodac = np.vectorize(vof, signature=\"(k)-&amp;gt;(m)\")\nvout = vodac(incodes_bin).flatten()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.step(codes, vout, \"k\", label=\"output voltage\")\nax.set_xlabel(\"input code\")\nax.set_ylabel(\"output voltage (V)\")\nax.grid(True)\nax.set_title(\"DAC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC Transfer Function')</code>\n</pre> <pre><code>vlsb_measured = (vout[2**nbits - 1] - vout[0]) / (2**nbits - 1)\ndnl = np.diff(vout) / vlsb_measured - 1\ninl = np.array([0] + np.cumsum(dnl).tolist())\nassert len(dnl) == 2**nbits - 1, \"DNL length is not correct: {}\".format(len(dnl))\nassert len(inl) == 2**nbits\n\ndac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\ndac_linearity\n</code></pre> <pre>\n<code>np.float64(5.115060342301529)</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(codes[1:], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(codes, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <p>As it is possible to observe, the presence of an offset voltage and parasitics do not affect this DAC architecture, being primarily limited by the capacitor mismatch each component is subjected to. This is because the offset and the effect of the parasitic capacitances are present in both phases of the charge redistribution process, nulling out each other.</p>"},{"location":"notebooks/practical_class_7/#charge-redistribution-dac-modelling","title":"Charge Redistribution DAC Modelling","text":"<p>In this practical class, the modelling method for a DAC based on a capacitive array is thoroughly described, considering the effects of parasitic capacitances, amplifier offset, noise and delay.</p>"},{"location":"notebooks/practical_class_7/#capacitive-dac-charge-redistribution","title":"Capacitive DAC - Charge Redistribution","text":"<p>Understanding the operation of the capacitive DAC is essential to significantly ease the effort of understanding the capacitive-based/charge redistribution ADCs, which is the most common ADC architecture in modern circuits. The classical capacitive DAC architecture is presented below.</p> <p></p>"},{"location":"notebooks/practical_class_8/","title":"8. Current Steering DAC","text":"<pre><code>from functools import partial\n\nimport numpy as np\nimport seaborn as sns\n\nsns.set_theme()\nsns.set_palette(\"colorblind\")\n\nfrom numpy.random import normal\nimport matplotlib.pyplot as plt\nfrom utils import dec2bin, bin2dec\n\n%matplotlib inline\n</code></pre> <pre><code>Np = 1000\nFs = 1e9  # Hz\nTs = 1 / Fs  # s\ntD = np.arange(0, Np) * Ts\n</code></pre> <p>The current-steering DAC is based on a current mirror. It is well known that the current mismatch in a current mirror </p> <pre><code>nbits = 8\nvref = 1  # V\nvlsb = vref / 2**nbits\n</code></pre> <pre><code>uA = 1e-6  # uA\niu = 10 * uA  # unitary current\n\ncurrs = iu * np.ones(2**nbits - 1)\n\nu_err_curr = 0.0\nstd_err_curr = 0.01  # 1% of the gate width\ncurr_mismatch = normal(u_err_curr, std_err_curr, len(currs))\ncurrs = currs * (1 + curr_mismatch)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(curr_mismatch, ax=ax, stat=\"probability\", kde=True)\nax.set_xlabel(\"Current Mismatch (A/A)\")\nax.set_ylabel(\"probability density\")\nax.grid(True)\nax.set_title(\"Current Mismatch distribution\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Current Mismatch distribution')</code>\n</pre> <pre><code>i_idxs = {id: np.arange(2**id, 2 ** (id + 1)) - 1 for id in range(nbits)}\nbw_currs = np.array([np.sum([currs[i] for i in i_idxs[id]]) for id in range(nbits)])\n</code></pre> <pre><code>def get_iout(incode, bw_currs, invert_code=True):\n    \"\"\"Compute the output current of the DAC given the input code and the binary weighted currents\n    It is assumed that:\n        - the input code is a binary number with nbits from MSB to LSB\n        - the binary weighted currents are ordered from LSB to MSB\n    Parameters:\n        incode (int): input code\n        bw_currs (np.array): binary weighted currents\n    \"\"\"\n    icode = incode[:: -1 if invert_code else 1]  # invert the code if needed\n    return icode.reshape((1, -1)) @ bw_currs.reshape((-1, 1))\n</code></pre> <pre><code>codes = np.arange(2**nbits)\nincodes_bin = np.array([dec2bin(code, nbits) for code in codes])\n\niof = partial(get_iout, bw_currs=bw_currs)\niofv = np.vectorize(iof, signature=\"(k)-&amp;gt;(m)\")\niout = iofv(incodes_bin).flatten()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.step(codes, iout / uA, \"k\", label=\"output voltage\")\nax.set_xlabel(\"input code\")\nax.set_ylabel(\"output current (uA)\")\nax.grid(True)\nax.set_title(\"DAC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC Transfer Function')</code>\n</pre> <pre><code>ilsb_measured = (iout[2**nbits - 1] - iout[0]) / (2**nbits - 1)\ndnl = np.diff(iout) / ilsb_measured - 1\ninl = np.array([0] + np.cumsum(dnl).tolist())\nassert len(dnl) == 2**nbits - 1, \"DNL length is not correct: {}\".format(len(dnl))\nassert len(inl) == 2**nbits\n# ENOB from INL: lost bits = log2(max(INL) - min(INL))\ndac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(codes[1:], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(codes, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <pre><code>num_circuits = 100\n\nuA = 1e-6  # uA\niu = 10 * uA  # unitary current\n\ncodes = np.arange(2**nbits)\nincodes_bin = np.array([dec2bin(code, nbits) for code in codes])\n\ndac_res = []\n\nfor circ in range(num_circuits):\n    currs = iu * np.ones(2**nbits - 1)\n\n    u_err_curr = 0.0\n    std_err_curr = 0.01  # 1% of the gate width\n    curr_mismatch = normal(u_err_curr, std_err_curr, len(currs))\n    currs = currs * (1 + curr_mismatch)\n\n    i_idxs = {id: np.arange(2**id, 2 ** (id + 1)) - 1 for id in range(nbits)}\n    bw_currs = np.array([np.sum([currs[i] for i in i_idxs[id]]) for id in range(nbits)])\n\n    iof = partial(get_iout, bw_currs=bw_currs)\n    iofv = np.vectorize(iof, signature=\"(k)-&amp;gt;(m)\")\n    iout = iofv(incodes_bin).flatten()\n\n    ilsb_measured = (iout[2**nbits - 1] - iout[0]) / (2**nbits - 1)\n    dnl = np.diff(iout) / ilsb_measured - 1\n    inl = np.array([0] + np.cumsum(dnl).tolist())\n    # ENOB from INL: lost bits = log2(max(INL) - min(INL))\n    dac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n    dac_res.append(dac_linearity)\n\ndac_res = np.array(dac_res)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(dac_res, ax=ax, stat=\"probability\", kde=True)\nax.set_xlabel(\"effective number of bits\")\nax.set_ylabel(\"probability density function of each circuit\")\nax.grid(True)\nax.set_title(\n    \"Binary Weighted Current Steering DAC resolution distribution against current mismatch\"\n)\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Binary Weighted Current Steering DAC resolution distribution against current mismatch')</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(codes[1:], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(codes, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <pre><code>uA = 1e-6  # uA\niu = 10 * uA  # unitary current\n\ncurrs = iu * np.ones(2**nbits - 1)\n\nu_err_curr = 0.0\nstd_err_curr = 0.01  # 1% of the gate width\ncurr_mismatch = normal(u_err_curr, std_err_curr, len(currs))\ncurrs = currs * (1 + curr_mismatch)\n</code></pre> <pre><code>def decoder(incode):\n    \"\"\"Implements a digital decoder to retrieve 2^N\n    Parameters:\n        incode: binary word going from MSB to LSB\n    Returns:\n        outcode: output code\n    \"\"\"\n    outcode = np.zeros(2 ** len(incode) - 1)\n    dec = bin2dec(incode, len(incode))\n    outcode[:dec] = 1\n    return outcode\n\n\ndef get_iout_unitary(incode, unit_currs=None):\n    \"\"\"Computes the output current of a unitary current steering DAC\n    Parameters:\n        incode: binary word going from MSB to LSB\n        unit_currs: unitary current vector of the DAC\n    Returns:\n        outcode: output code\n    \"\"\"\n    return decoder(incode).reshape((1, -1)) @ unit_currs.reshape((-1, 1))\n</code></pre> <pre><code>codes = np.arange(2**nbits)\nincodes_bin = dec2bin(codes, nbits)\n\niof = partial(get_iout_unitary, unit_currs=currs)\niofv = np.vectorize(iof, signature=\"(k)-&amp;gt;(m)\")\niout = iofv(incodes_bin).flatten()\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.step(codes, iout / uA, \"k\", label=\"output voltage\")\nax.set_xlabel(\"input code\")\nax.set_ylabel(\"output current (uA)\")\nax.grid(True)\nax.set_title(\"DAC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'DAC Transfer Function')</code>\n</pre> <pre><code>ilsb_measured = (iout[2**nbits - 1] - iout[0]) / (2**nbits - 1)\ndnl = np.diff(iout) / ilsb_measured - 1\ninl = np.array([0] + np.cumsum(dnl).tolist())\nassert len(dnl) == 2**nbits - 1, \"DNL length is not correct: {}\".format(len(dnl))\nassert len(inl) == 2**nbits\n# ENOB from INL: lost bits = log2(max(INL) - min(INL))\ndac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(codes[1:], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(codes, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <pre><code>num_circuits = 100\n\nuA = 1e-6  # uA\niu = 10 * uA  # unitary current\n\ncodes = np.arange(2**nbits)\nincodes_bin = np.array([dec2bin(code, nbits) for code in codes])\n\ndac_res = []\n\nfor circ in range(num_circuits):\n    currs = iu * np.ones(2**nbits - 1)\n\n    u_err_curr = 0.0\n    std_err_curr = 0.01  # 1% of the gate width\n    curr_mismatch = normal(u_err_curr, std_err_curr, len(currs))\n    currs = currs * (1 + curr_mismatch)\n\n    iof = partial(get_iout_unitary, unit_currs=currs)\n    iofv = np.vectorize(iof, signature=\"(k)-&amp;gt;(m)\")\n    iout = iofv(incodes_bin).flatten()\n\n    ilsb_measured = (iout[2**nbits - 1] - iout[0]) / (2**nbits - 1)\n    dnl = np.diff(iout) / ilsb_measured - 1\n    inl = np.array([0] + np.cumsum(dnl).tolist())\n    # ENOB from INL: lost bits = log2(max(INL) - min(INL))\n    dac_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n    dac_res.append(dac_linearity)\n\ndac_res = np.array(dac_res)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(dac_res, ax=ax, stat=\"probability\", kde=True)\nax.set_xlabel(\"effective number of bits\")\nax.set_ylabel(\"probability density function of each circuit\")\nax.grid(True)\nax.set_title(\n    \"Unitary Current Steering DAC resolution distribution against current mismatch\"\n)\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Unitary Current Steering DAC resolution distribution against current mismatch')</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(codes[1:], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(codes, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre>"},{"location":"notebooks/practical_class_8/#practical-class-8-high-level-current-steering-dac-modelling-for-statistical-analysis","title":"Practical Class 8: High Level Current Steering DAC Modelling for Statistical Analysis","text":"<p>In this practical class it is demonstrated how to create a high level model of the ADC / DAC and its sub blocks to significantly speed up the statistical analysis of the converter, before starting its electrical simulation. This step enables to determine the specifications of each of the sub blocks of the converter, and as such it is very important.</p>"},{"location":"notebooks/practical_class_8/#current-steering-dac-modelling-binary-weighted-currents","title":"Current Steering DAC Modelling - Binary Weighted Currents","text":""},{"location":"notebooks/practical_class_8/#monte-carlo-simulation","title":"Monte Carlo Simulation","text":"<p>Run a Monte Carlo simulation to observe the binary weighted DAC linearity against randomized current mismatch.</p>"},{"location":"notebooks/practical_class_8/#current-steering-dac-modelling-unitary-currents","title":"Current Steering DAC Modelling - Unitary Currents","text":"<p>The use of unitary currents reduces the detrimental effect of current mismatch on the linearity of the DAC, as it is going to be observed at the end of this section. The fact that only a single switch and a single current is added to the output in the transition between each successive code severely reduces the effect of current mismatch, improving the DNL of the DAC. But this increased mismatch robustness comes at a cost of increased in-silicon area (which also increases the cost of fabrication of the IC) and circuit complexity, once it is necessary to design the digital decoder to activate the 2^N - 1 switches to control each unitary current.</p>"},{"location":"notebooks/practical_class_8/#monte-carlo-simulation_1","title":"Monte Carlo Simulation","text":"<p>Run a Monte Carlo simulation to observe the unitary current steering DAC linearity against randomized current mismatch.</p>"},{"location":"notebooks/practical_class_9/","title":"9. Resistive ADC Architectures","text":"<pre><code>import numpy as np\nimport seaborn as sns\nfrom numpy.random import normal\nimport matplotlib.pyplot as plt\n\nsns.set_theme()\nsns.set_palette(\"colorblind\")\n\n%matplotlib inline\n</code></pre> <pre><code>Np = 2**13\nFs = 1e9  # Hz\nTs = 1 / Fs  # s\ntD = np.arange(0, Np) * Ts\n</code></pre> <pre><code>Avth = 1e-9\nAbeta = 1e-9\n\nvddd = 0.9\nvssd = 0.0\nvcm = (vddd + vssd) / 2\nvs = 0.020\nvgs = vcm - vs\nvth = 0.330\nnm = 1e-9\num = 1e-6\nw = 20 * um\nl = 30 * nm\n\nstd_ofst = np.sqrt((Avth**2) / (w * l) + ((Abeta**2) / (w * l)) * ((vgs - vth) / 2))\nprint(f\"Comparator offset std: {std_ofst:.6e} V\")\n</code></pre> <pre>\n<code>Comparator offset std: 1.322876e-03 V\n</code>\n</pre> <pre><code>nbits = 8\nvref = vddd\nvlsb = vref / (2**nbits)\n\nru = 1e3  # ohm\nres = np.ones(2**nbits) * ru\n\nu_err = 0.0\nu_std = 0.03\nr_err = normal(u_err, u_std, len(res))\nres = res + r_err * ru\nassert len(res) == 2**nbits\n\nvcomps = np.array(\n    [vref * (np.sum(res[0 : i + 1]) / np.sum(res)) for i in range(len(res) - 1)]\n)\n\ncomps_ofst = np.random.normal(0, std_ofst, len(vcomps))\ncomps = vcomps + comps_ofst\nassert len(comps) == 2**nbits - 1, \"The number of comparators is not correct: %d\" % len(\n    comps\n)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(comps_ofst, ax=ax, stat=\"probability\", kde=True)\nsns.histplot(comps, ax=ax.twiny(), stat=\"probability\", kde=True, color=\"red\")\nax.set_xlabel(\"comparator offset distribution (V) - (blue)\")\nax.set_ylabel(\"probability density\")\nax.grid(True)\nax.set_title(\"comparator reference voltage distribution (V) - (red)\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'comparator reference voltage distribution (V) - (red)')</code>\n</pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(res, ax=ax, stat=\"probability\", kde=True)\nax.set_xlabel(\"resistor value (ohm)\")\nax.set_ylabel(\"probability density\")\nax.grid(True)\nax.set_title(\"resistor values mismatch distribution\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'resistor values mismatch distribution')</code>\n</pre> <pre><code>vdd = vref  # V\nvss = 0.0  # V\nnbits = 8\nvlsb = np.abs(vref / (2**nbits))  # V\n\nA1 = vdd / 2\nf1 = Fs / Np  # Hz\nph1 = 0  # rad\nofst = 0.0\nvin_t = A1 * np.sin(2 * np.pi * f1 * tD + ph1) + ofst + vdd / 2\nvin = np.linspace(vss, vdd, Np)\n\ndecoder_in_t = np.array([v &amp;gt; comps for v in vin_t])\ndout_t = np.array([np.count_nonzero(din) for din in decoder_in_t])\n</code></pre> <pre><code>ns = 1e-9\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD / ns, vin_t, \"b\", label=\"input signal\")\nax[0].set_xlabel(\"time (ns)\")\nax[0].set_ylabel(\"voltage (V)\")\nax[0].legend()\nax[1].plot(tD / ns, dout_t, \"r\", label=\"output codes\")\nax[1].set_xlabel(\"time (ns)\")\nax[1].set_ylabel(\"output codes\")\nax[1].legend()\nax[1].grid(True)\nax[0].grid(True)\n</code></pre> <pre><code>decoder_in = np.array([v &amp;gt; comps for v in vin])\ndout = np.array([np.count_nonzero(din) for din in decoder_in])\nvtrans = vin[np.where(np.diff(dout) &amp;gt; 0)]\ndout = dout[np.where(np.diff(dout) &amp;gt; 0)]\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.step(vtrans, dout, \"k\", label=\"output codes\")\nax.set_xlabel(\"input voltage (V)\")\nax.set_ylabel(\"output code\")\nax.grid(True)\nax.set_title(\"Flash ADC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Flash ADC Transfer Function')</code>\n</pre> <pre><code>vlsb_measured = (vtrans[-1] - vtrans[0]) / (2**nbits - 2)\nprint(\"True Vlsb: %f\" % vlsb)\nprint(\"Measured Vlsb: %f\" % vlsb_measured)\n\ndnl = np.diff(vtrans) / vlsb_measured - 1\ninl = np.array([0] + np.cumsum(dnl).tolist())\n\nadc_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\nprint(f\"ADC linearity: {adc_linearity:.4f} bits\")\n</code></pre> <pre>\n<code>True Vlsb: 0.003516\nMeasured Vlsb: 0.003515\nADC linearity: 6.5539 bits\n</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(dout[0:-1], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(dout, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <pre><code>circuits = 100\n\nvddd = 1.0\nvssd = 0.0\nvcm = (vddd + vssd) / 2\ncomp_offset = 100e-6  # V\n\nnbits = 8\nvref = vddd\nvlsb = vref / (2**nbits)\n\nNp = 2**15\nFs = 1e9  # Hz\nTs = 1 / Fs  # s\ntD = np.arange(0, Np) * Ts\n\nA1 = vddd / 2\nf1 = Fs / Np  # Hz\nph1 = 0  # rad\nofst = 0.0\nvin_t = A1 * np.sin(2 * np.pi * f1 * tD + ph1) + ofst + vcm\n\nru = 1e3  # ohm\nu_err = 0.0\nu_std = 0.05\n\nadc_lins = []\n\nfor _ in range(circuits):\n    res = np.ones(2**nbits) * ru\n    r_err = normal(u_err, u_std, len(res))\n    res = res + r_err * ru\n\n    vcomps = np.array(\n        [vref * (np.sum(res[0 : i + 1]) / np.sum(res)) for i in range(len(res) - 1)]\n    )\n\n    comps_ofst = np.random.normal(0, comp_offset, len(vcomps))\n    comps = vcomps + comps_ofst\n\n    vin = np.sort(np.unique(vin_t))\n    decoder_in = np.array([v &amp;gt; comps for v in vin])\n    dout = np.array([np.count_nonzero(din) for din in decoder_in])\n\n    vtrans = vin[np.where(np.diff(dout) &amp;gt; 0)]\n    dout = dout[np.where(np.diff(dout) &amp;gt; 0)]\n\n    vlsb_measured = (vtrans[-1] - vtrans[0]) / (2**nbits - 2)\n    dnl = np.diff(vtrans) / vlsb_measured - 1\n    inl = np.array([0] + np.cumsum(dnl).tolist())\n    assert len(dnl) == 2**nbits - 2, \"DNL length is not correct: {}\".format(len(dnl))\n    assert len(inl) == 2**nbits - 1, \"INL length is not correct: {}\".format(len(inl))\n\n    adc_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n    adc_lins.append(adc_linearity)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(adc_lins, ax=ax, stat=\"probability\", kde=True)\nax.set_xlabel(\"ADC linearity (bits)\")\nax.set_ylabel(\"probability density\")\nax.grid(True)\nax.set_title(\"ADC linearity histogram\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'ADC linearity histogram')</code>\n</pre> <pre><code>avg_adc_lins = []\ncomp_offsets = np.linspace(0, 1e-3, 3)\nu_std = 0.03\n\nfor comp_offset in comp_offsets:\n    adc_lins = []\n    for _ in range(circuits):\n        res = np.ones(2**nbits) * ru\n        r_err = normal(u_err, u_std, len(res))\n        res = res + r_err * ru\n\n        vcomps = np.array(\n            [vref * (np.sum(res[0 : i + 1]) / np.sum(res)) for i in range(len(res) - 1)]\n        )\n\n        comps_ofst = np.random.normal(0, comp_offset, len(vcomps))\n        comps = vcomps + comps_ofst\n\n        decoder_in = np.array([v &amp;gt; comps for v in vin])\n        dout = np.array([np.count_nonzero(din) for din in decoder_in])\n\n        vtrans = vin[np.where(np.diff(dout) &amp;gt; 0)]\n        dout = dout[np.where(np.diff(dout) &amp;gt; 0)]\n\n        vlsb_measured = (vtrans[-1] - vtrans[0]) / (2**nbits - 2)\n        dnl = np.diff(vtrans) / vlsb_measured - 1\n        inl = np.array([0] + np.cumsum(dnl).tolist())\n\n        adc_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n        adc_lins.append(adc_linearity)\n    avg_adc_lins.append(np.mean(adc_lins))\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(comp_offsets / 1e-6, avg_adc_lins, \"b\", label=\"ADC linearity\")\nax.set_xlabel(\"comparator offset (uV)\")\nax.set_ylabel(\"Average ADC linearity (bits)\")\nax.grid(True)\nax.set_title(\"ADC linearity against comparator offset\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'ADC linearity against comparator offset')</code>\n</pre> <pre><code>nbits = 8\nvdd = 1.0\nvss = 0.0\nvref = vdd\nvlsb = vref / (2**nbits)\n\nA2 = vref / 2\nf2 = Fs / Np  # Hz\nvcm = (vdd + vss) / 2\nph2 = 0  # rad\nvin2_t = A2 * np.sin(2 * np.pi * f2 * tD + ph2) + vcm\nvin2 = np.linspace(vss, vdd, Np)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(tD / ns, vin2_t, \"b\", label=\"input signal\")\nax.set_xlabel(\"time (ns)\")\nax.set_ylabel(\"voltage (V)\")\nax.legend()\n</code></pre> <pre>\n<code>&lt;matplotlib.legend.Legend at 0x117f9cbd0&gt;</code>\n</pre> <pre><code>Ncircuits = 100\n\nuV = 1e-6\naF = 1e-18\nfF = 1e-15\n\ncomp_offsets = np.linspace(0, 1e-3, 5)\nadc_lins = []\navg_adc_lins = []\n\nfor comp_offset in comp_offsets:\n    for _ in range(Ncircuits):\n        ru = 1e3  # ohm\n        res = np.ones(2**nbits) * ru\n        ru_err = 0.0\n        ru_std = 0.2\n        r_err = normal(ru_err, ru_std, len(res))\n        res = (1 + r_err) * res\n\n        vcomps = np.array(\n            [vref * (np.sum(res[0 : i + 1]) / np.sum(res)) for i in range(len(res) - 1)]\n        )\n        vcomps = comp_offset + vcomps\n\n        ron = 100  # ohm\n        ron_err = 0.0\n        ron_std = 0.05\n        r_err = normal(ron_err, ron_std, len(vcomps))\n        rons = np.ones(len(vcomps)) * ron\n        rons = (1 + r_err) * rons\n\n        cg_comp = 20 * fF  # F\n        cp = 1 * fF * len(rons) + cg_comp  # F\n\n        taus = rons * cp  # s\n\n        decoder_in = np.array([v &amp;gt; vcomps for v in vin2])\n        decoder_out = np.array([np.count_nonzero(din) for din in decoder_in])\n\n        vtrans = vin2[np.where(np.diff(decoder_out) &amp;gt; 0)]\n        decoder_out = decoder_out[np.where(np.diff(decoder_out) &amp;gt; 0)]\n\n        vlsb_measured = (vtrans[-1] - vtrans[0]) / (2**nbits - 2)\n        dnl = np.diff(vtrans) / vlsb_measured - 1\n        inl = np.array([0] + np.cumsum(dnl).tolist())\n\n        adc_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n        adc_lins.append(adc_linearity)\n    avg_adc_lins.append(np.mean(adc_lins))\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(res, ax=ax, stat=\"probability\", kde=True)\nax.set_xlabel(\"resistor value (ohm)\")\nax.set_ylabel(\"probability density\")\nax.grid(True)\nax.set_title(\"resistor values mismatch distribution\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'resistor values mismatch distribution')</code>\n</pre> <pre><code>ps = 1e-12\n\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(taus / ps, ax=ax, stat=\"probability\", kde=True)\nax.set_xlabel(\"RC time constant (ps)\")\nax.set_ylabel(\"probability density\")\nax.grid(True)\nax.set_title(\"RC time constant distribution\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'RC time constant distribution')</code>\n</pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.step(vtrans, decoder_out, \"k\", label=\"output codes\")\nax.set_xlabel(\"input voltage (V)\")\nax.set_ylabel(\"output code\")\nax.grid(True)\nax.set_title(\"Single Comparator Flash ADC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Single Comparator Flash ADC Transfer Function')</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(decoder_out[0:-1], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(decoder_out, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(\n    np.array(comp_offsets) / uV, np.array(avg_adc_lins), \"gray\", label=\"ADC linearity\"\n)\nax.set_xlabel(\"comparator offset (uV)\")\nax.set_ylabel(\"Average ADC linearity (bits)\")\nax.grid(True)\nax.set_title(\"Single Comp. Flash ADC linearity vs. comparator offset\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Single Comp. Flash ADC linearity vs. comparator offset')</code>\n</pre> <p>The 2-step architecture of an ADC is based on the block diagram below. The 2-step architecture can be thought of a prelude for pipeline ADCs, in which the total ADC's resolution is spatially divided between two stages. The first stage will lay out the Most Significant Bits, and the residue of the first stage will be converted in the second stage, laying out the Least Significant Bits of the ADC. Of course that the range of the residue must be adjusted to the resolution of the second stage, and for that it is possible to either: 1) adjust the reference voltage of the second stage to a lower value (sub-ranging), 2) amplify the residue by a gain dependant on the resolution of the first stage so that it covers the full conversion range of the second stage (with amplification), 3) a combination of both (which we call a cheat in the academia).</p> <p>&gt; TODO: Insert circuit diagram of the 2-step flash ADC without amplification.</p> <pre><code>nbits_first = 6\nnbits_second = 4\nvdd = 1.0\nvss = 0.0\nvref = vdd\nvlsb = vref / (2**nbits)\n\nA3 = vref / 2\nf3 = Fs / Np  # Hz\nvcm = (vdd + vss) / 2\nph3 = 0  # rad\nvin3_t = A3 * np.sin(2 * np.pi * f3 * tD + ph3) + vcm\nvin3 = np.linspace(vss, vdd, Np)\n</code></pre> <pre><code>Ncircuits = 100\n\nuV = 1e-6\naF = 1e-18\nfF = 1e-15\n\nru_err = 0.0\nru_std = 0.01\n\nNum_comp_offsets = 1\ncomp_offsets = np.linspace(0, 1e-3, Num_comp_offsets)\nadc_lins = []\navg_adc_lins = []\n\nfor comp_offset in comp_offsets:\n    for _ in range(Ncircuits):\n        # First stage resistor array\n        ru = 1e3  # ohm\n        res = np.ones(2**nbits_first) * ru\n        r_err = normal(ru_err, ru_std, len(res))\n        res = (1 + r_err) * res\n\n        vcomps = np.array(\n            [vref * (np.sum(res[0 : i + 1]) / np.sum(res)) for i in range(len(res) - 1)]\n        )\n        vcomps = comp_offset + vcomps\n\n        ron = 100  # ohm\n        ron_err = 0.0\n        ron_std = 0.05\n        r_err = normal(ron_err, ron_std, len(vcomps))\n        rons = np.ones(len(vcomps)) * ron\n        rons = (1 + r_err) * rons\n\n        cg_comp = 20 * fF  # F\n        cp = 1 * fF * len(rons) + cg_comp  # F\n        taus = rons * cp  # s\n\n        decoder_in_first = np.array([v &amp;gt; vcomps for v in vin3])\n        decoder_out_first = np.array(\n            [np.count_nonzero(din) for din in decoder_in_first]\n        )\n\n        dout_first = decoder_out_first[np.where(np.diff(decoder_out_first) &amp;gt; 0)]\n        vtrans_first = vin3[np.where(np.diff(decoder_out_first) &amp;gt; 0)]\n\n        vlsb_measured_first = (vtrans_first[-1] - vtrans_first[0]) / (\n            2**nbits_first - 2\n        )\n        dnl_first = np.diff(vtrans_first) / vlsb_measured_first - 1\n        inl_first = np.array([0] + np.cumsum(dnl_first).tolist())\n\n        # Second stage resistor array\n        ru = 1e3  # ohm\n        nbits = nbits_first + nbits_second\n        res = np.ones(2**nbits) * ru\n        r_err = normal(ru_err, ru_std, len(res))\n        res = (1 + r_err) * res\n\n        vcomps = np.array(\n            [vref * (np.sum(res[0 : i + 1]) / np.sum(res)) for i in range(len(res) - 1)]\n        )\n        vcomps = comp_offset + vcomps\n\n        lower_vcomp_idx = decoder_out_first * (2**nbits_second)\n        upper_vcomp_idx = lower_vcomp_idx + 2**nbits_second - 1\n\n        vcomps_second = np.array(\n            [\n                vcomps[lower_vcomp_idx[id] : upper_vcomp_idx[id]]\n                for id in range(len(decoder_out_first))\n            ]\n        )\n\n        decoder_in_second = np.array(\n            [v &amp;gt; vcomps_second[k, :] for k, v in enumerate(vin3)]\n        )\n        decoder_out_second = np.array(\n            [np.count_nonzero(din) for din in decoder_in_second]\n        )\n\n        dout_second = decoder_out_second[np.where(np.diff(decoder_out_second) &amp;gt; 0)]\n        vtrans_second = vin3[np.where(np.diff(decoder_out_second) &amp;gt; 0)]\n\n        vlsb_measured_second = (vtrans_second[-1] - vtrans_second[0]) / (\n            2**nbits_second - 2\n        )\n        dnl_second = np.diff(vtrans_second) / vlsb_measured_second - 1\n        inl_second = np.array([0] + np.cumsum(dnl_second).tolist())\n\n        decoder_out = decoder_out_first * 2**nbits_second + decoder_out_second\n\n        dout = decoder_out[np.where(np.diff(decoder_out) &amp;gt; 0)]\n        vtrans = vin3[np.where(np.diff(decoder_out) &amp;gt; 0)]\n\n        vlsb_measured = (vtrans[-1] - vtrans[0]) / (2**nbits - 2)\n        dnl = np.diff(vtrans) / vlsb_measured - 1\n        inl = np.array([0] + np.cumsum(dnl).tolist())\n\n        adc_linearity = nbits - np.log2(np.max(inl) - np.min(inl))\n        adc_lins.append(adc_linearity)\n\n    avg_adc_lins.append(np.mean(adc_lins))\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.step(vtrans, dout, \"k\", label=\"output codes\")\nax.set_xlabel(\"input voltage (V)\")\nax.set_ylabel(\"output code\")\nax.grid(True)\nax.set_title(\"Single Comparator 2-Step Sub-ranging Flash ADC Transfer Function\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'Single Comparator 2-Step Sub-ranging Flash ADC Transfer Function')</code>\n</pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(dout[0:-1], dnl, \"b\", label=\"DNL\")\nax[0].set_xlabel(\"input codes\")\nax[0].set_ylabel(\"DNL\")\nax[0].legend()\nax[0].grid(True)\nax[1].step(dout, inl, \"r\", label=\"INL\")\nax[1].set_xlabel(\"input codes\")\nax[1].set_ylabel(\"INL\")\nax[1].legend()\nax[1].grid(True)\n</code></pre> <pre><code>fig, ax = plt.subplots(1, 1, figsize=(4, 4))\nsns.histplot(adc_lins, ax=ax, stat=\"probability\", kde=True)\nax.set_xlabel(\"ADC linearity (bits)\")\nax.set_ylabel(\"probability density\")\nax.grid(True)\nax.set_title(\"ADC linearity histogram\")\n</code></pre> <pre>\n<code>Text(0.5, 1.0, 'ADC linearity histogram')</code>\n</pre>"},{"location":"notebooks/practical_class_9/#resistive-adc-modelling-and-high-level-simulation","title":"Resistive ADC Modelling and High-Level Simulation","text":"<p>In this notebook, three architectures of ADC are modelled and a high level simulation is performed for each architecture. The covered ADC architectures are based in resistive arrays to enable the discretization of the reference voltage in each discrete voltage level, implementing the intrinsic DAC of the ADC. The generality of the existing ADC architectures are based on the use of a comparator to effectively digitize the input voltage. This notebook will have a special emphasis on the impact of the comparator offset in the ADC performance.</p>"},{"location":"notebooks/practical_class_9/#comparators-a-brief-introduction","title":"Comparators: A brief introduction","text":"<p>A comparator can be thought of a 1 bit ADC. If the input voltage of the ADC (at v+) is higher than the reference voltage of the comparator (at v-), the comparator will output a bit with a logical value of '1', whereas if the input voltage is lower than the reference voltage, the comparator will output a bit with a logical value of '0'.</p> <p></p> <p>The higher the gain of the comparator, the faster is the transition between the two possible logical values at the output. Thus, comparators usually employ:</p> <ul> <li>Positive feedback</li> <li>Clock reset</li> </ul> <p>to ensure very high gains and fast transitions due to the instability of the comparator itself, using the clock reset to re-establish the correct operating point of the comparator.</p>"},{"location":"notebooks/practical_class_9/#the-main-problem-with-comparators","title":"The main problem with Comparators","text":"<p>The input of almost every comparator topology is based on the use of a differential pair. Due to mismatching, the employed transistors in the implementation of the input differential pair usually present different gate width and length dimensions, which ultimately affects the DC operating point of the comparator. This mismatching leads to a voltage offset between both inputs of the comparator. Of course that this offset is also present at the output of the circuit, once a comparator is basically an unstable amplifier with a \"linear\" response.</p> <p></p> <p>Considering that the gate area mismatch is a random variable following a normal distribution, the voltage offset arising through the differential pair mismatch can be given by the standard error \\(\\sigma_{ofst} \\approx V_{ofst}\\):</p> \\[ \\sigma_{ofst} = \\sqrt{ \\frac{A^2_{V_{TH}}}{W \\cdot L} + \\frac{A^2_{\\beta}}{W \\cdot L} \\left( \\frac{V_{GS} - V_{TH}}{2} \\right)^2 } \\] <p>where \\(A_{V_{TH}}\\) and \\(A_\\beta\\), with \\(\\beta = \\mu \\cdot C_{ox}\\) are technology-specific parameters modelling how the threshold voltage \\(V_{TH}\\) and the channel mobility \\(\\mu \\cdot C_{ox}\\) vary with the gate area of the transistor.</p> <p>The offset will lead the comparator to decide its output logical value relative to a lower (negative offset) or higher (positive offset) input voltage than the reference voltage. This offset is usually modelled as a random variable following a normal distribution with a mean of zero and a standard deviation equal to the maximum offset amplitude (Vofst). The decisions happening \"sooner\" or \"later\", in relation to the voltage reference, will provoke missing-codes in the ADC's transfer function.</p>"},{"location":"notebooks/practical_class_9/#flash-adc-modelling","title":"Flash ADC - Modelling","text":""},{"location":"notebooks/practical_class_9/#single-comparator-flash-adc","title":"Single Comparator Flash ADC","text":"<p>The number of comparators in a flash ADC grows exponentially with the number of bits of the architecture, and thus it is not feasible to implement a flash ADC with a high number of bits. However, it is possible to implement a flash ADC with a single comparator. By using each individual voltage level from the resistive array, and using sequential control logic to implement a Binary Search Algorithm (Successive Approximations Algorithm), one can determine each bit of the ADC by determining if the input voltage is above or below that level. There is a quite evident tradeoff here - instead of using multiple comparators to obtain the result in a single conversion step, we are using a single comparator to obtain the result in multiple conversion steps.</p> <p>&gt; TODO: Insert circuit diagram of the flash ADC with comparator switching scheme.</p>"},{"location":"notebooks/practical_class_9/#monte-carlo-simulation-of-the-single-comparator-flash-adc","title":"Monte Carlo simulation of the single comparator flash ADC","text":"<p>The comparison in this ADC architecture happens recursively, since there is only one comparator used for each bit determination. For simplicity, we compute the output code using the same approach as the classic flash ADC (comparing against all reference voltages at once).</p> <p>Because there is only one closed switch for each comparison, the RC time constant (tau) at the comparator input node is approximately the same for all comparisons.</p>"},{"location":"notebooks/practical_class_9/#2-step-flash-adc-without-amplification-sub-ranging","title":"2-Step Flash ADC (without amplification = Sub-Ranging)","text":""},{"location":"notebooks/practical_class_9/#2-step-sub-ranging-flash-adc-operation","title":"2-Step Sub-Ranging Flash ADC Operation","text":"<p>At a first glance, the 2-Step Sub-Ranging Flash ADC can be quite tricky to understand, but it is actually much easier than it seems. As it is possible to observe in the general block description of this ADC architecture in the figure B) above, the reference voltage of the second stage DAC and ADC must be limited to VREF/2^N1, where VREF is the reference voltage of the first stage and N1 is the resolution of the first stage. To effectively obtain the least significant bits using the second stage, we must inject the residue voltage of the first stage as the input voltage of the second stage, which requires the performance of a subtraction of the input voltage and the resulting DAC voltage of the first stage.</p> <p>Well... this subtraction can become a big bottleneck of our design, once it requires the use of a subtraction circuit (either using an OPAMP for switched capacitors or continuous circuits), and as it turns out we can actually avoid it. At the expense of using a second stage that has an effective resolution of N2' = N1 + N2, where N2 is the desired resolution of our second stage, we can avoid using a subtraction circuit, by:</p> <ul> <li>A) Using the first digital word to limit the range of comparison voltages in the second resistive string, defining an upper and lower limit</li> <li>B) Limiting the amount of comparisons of the second stage to 2^N2 - 1, within the defined range of voltages</li> <li>C) Obtaining the remaining Least Significant Bits from the same recursive algorithm within the defined range of comparison voltages of the second resistive DAC</li> </ul>"},{"location":"notebooks/practical_class_9/#2-step-sub-ranging-flash-adc-monte-carlo-simulation","title":"2-Step Sub-Ranging Flash ADC Monte Carlo Simulation","text":"<p>The simulation below models the two-stage sub-ranging flash ADC architecture. In each Monte Carlo iteration:</p> <ol> <li>First stage: A resistive DAC with mismatch generates the coarse comparison voltages. The input is compared against these to produce the MSBs.</li> <li>Second stage: A finer resistive DAC (with total resolution N1 + N2) provides the fine comparison voltages. The first-stage digital word selects the appropriate sub-range of comparison voltages for the second stage.</li> <li>The final output code is composed as: <code>MSBs * 2^N2 + LSBs</code>.</li> </ol> <p>The comparison in each stage happens recursively (one comparator per bit), but for simplicity we compute the output code using the classic flash approach (comparing against all reference voltages simultaneously). Because there is only one closed switch per comparison, the RC time constant at the comparator input is approximately the same for all comparisons.</p>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/","title":"Pipeline SAR ADC","text":"<p>If the current bit is decided to be logical \"0\", the symmetrical connections are performed: the top plate voltage of the positive array is pulled downwards and the top plate voltage of the negative array is pushed upwards, once again adjusting the decision scale for each comparator.</p> <p>The last Cs capacitor (lowest weight in the array) is also programmed. Usually this capacitor is not programmed, but to enable pipeline operation, the residue voltage must be available at the top plate of the capacitor array.</p> <p>&gt; Note: Bit D5 influences the connections of the 8C+8C split capacitor (which decides bit D4); bit D4 influences the connections of the 4C+4C split capacitor (which decides bit D3), and so on.</p> <pre><code>import numpy as np\nimport seaborn as sns\n\nsns.set_theme()\nsns.set_palette(\"colorblind\")\n\nfrom numpy.random import normal\nimport matplotlib.pyplot as plt\nfrom utils import bin2dec\nfrom fft import FFTEngine\n\n%matplotlib inline\n</code></pre> <pre><code>Np = 2**13\nFs = 1e9  # Hz\nTs = 1 / Fs  # s\ntD = np.arange(0, Np) * Ts\n</code></pre> <pre><code>vdd = 1.0\nvss = 0.0\nvcm = np.mean([vdd, vss])\nvref = vdd\n\n# first sub-ADC\nvref_first = vref\nvrefp_first = vcm + vref_first / 2\nvrefn_first = vcm - vref_first / 2\nNbits_first = 6\nvlsb_first = vref_first / (2**Nbits_first)\n\n# second sub-ADC\nsecond_adc_gain = 4\nvref_scale_factor = 1 / second_adc_gain\nvref_second = vref_first * vref_scale_factor\nvrefp_second = vcm + vref_second / 2\nvrefn_second = vcm - vref_second / 2\nNbits_second = 7\nvlsb_second = vref_second / (2**Nbits_second)\n</code></pre> <pre><code># scales\n# volts\nV = 1.0\nmV = V * 1e-3\nuV = V * 1e-6\nnV = V * 1e-9\n\n# seconds\ns = 1.0\nms = s * 1e-3\nus = s * 1e-6\nns = s * 1e-9\n\n# frequency\nHz = 1.0\nkHz = Hz * 1e3\nMHz = Hz * 1e6\nGHz = Hz * 1e9\n\n# length\nm = 1.0\nmm = m * 1e-3\num = m * 1e-6\nnm = m * 1e-9\n\n# farad\nF = 1.0\npF = F * 1e-12\nfF = F * 1e-15\n</code></pre> <pre><code>ENABLE_NOISE = False  # enable noise supperposed to the input signal\nvn_mean = 0.0 * V  # mean of the noise signal\nvn_std = 1 * mV  # standard deviation of the noise signal\nvn_in = normal(vn_mean, vn_std, Np)  # generate the gaussian/white (random) noise signal\n</code></pre> <pre><code># observe the noise signal\nif ENABLE_NOISE:\n    fig, ax = plt.subplots(figsize=(4, 3))\n    sns.histplot(\n        vn_in, ax=ax, kde=True, stat=\"probability\", color=\"gray\", label=\"noise signal\"\n    )\n    ax.set_xlabel(\"Voltage [V]\")\n    ax.set_ylabel(\"Probability density\")\n    ax.set_title(\"Histogram of the noise signal\")\n    plt.show()\n</code></pre> <pre><code># setup the input signal of the first sub ADC\nA1 = vref\nfbin = 7\nf1 = fbin * (Fs / Np)\nph1 = 0\nvin1t = A1 * np.sin(2 * np.pi * f1 * tD + ph1)\nvin1 = np.linspace(-A1, A1, Np)  # ramp input\nif ENABLE_NOISE:\n    vin1t = vin1t + vn_in\n\n# compute the spectrum of the input signal\nvin1f = np.abs(\n    np.fft.fftshift(np.fft.fft(vin1t) / Np)\n)  # [V] # voltage in the frequency axis\n# NOTE: fft(yf) is divided by Np in order to normalize the resulting frequency spectrum from the digital fourier transform\nfD = np.fft.fftshift(np.fft.fftfreq(Np, Ts))  # [Hz] # frequency axis\npower_vin1f = (\n    vin1f * vin1f\n)  # [V^2] - square the voltage spectrum to obtain the power spectrum\npower_vinf_dB = 10 * np.log10(power_vin1f)  # [dB] - convert the power spectrum to dB\n</code></pre> <pre><code># setup the input voltage array\nvint_p = (vin1t) / 2 + vcm\nvint_n = -(vin1t) / 2 + vcm\nvinp = (vin1) / 2 + vcm\nvinn = -(vin1) / 2 + vcm\n</code></pre> <pre><code># show the unique values of the input signal\nfig, ax = plt.subplots(figsize=(4, 3))\nax.plot(vin1 / mV, vinp / mV, label=\"vinp\")\nax.plot(vin1 / mV, vinn / mV, label=\"vinn\")\nax.set_xlabel(\"Vin1 (mV)\")\nax.legend()\n</code></pre> <pre>\n<code>&lt;matplotlib.legend.Legend at 0x117c8b790&gt;</code>\n</pre> <pre><code># observe the input signal in time and frequency\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(tD / us, vin1t, label=\"vi\", color=\"black\")\nax[0].plot(tD / us, vint_p, label=\"vip\", color=\"red\")\nax[0].plot(tD / us, vint_n, label=\"vin\", color=\"blue\")\nax[0].legend()\nax[0].set_xlabel(\"time [us]\")\nax[0].set_ylabel(\"voltage [V]\")\nax[0].set_title(\"Input signal\")\nax[0].grid(True)\nax[1].stem(\n    fD[fD &amp;gt;= 0] / MHz,\n    power_vinf_dB[fD &amp;gt;= 0],\n    label=\"input signal in frequency\",\n    bottom=np.min(power_vinf_dB),\n    linefmt=\"b-\",\n    markerfmt=\"none\",\n    basefmt=\"r-\",\n)\nax[1].grid(True)\nax[1].set_xlabel(\"frequency [MHz]\")\nax[1].set_ylabel(\"power [dB]\")\n# close up on the frequency axis\nax[1].set_xlim([0, 5 * f1 / MHz])\nplt.tight_layout()\nplt.show()\n</code></pre> <pre><code>def loop_unrolled_bottom_plate_sampling_monotonic_splitcap_sar_adc(\n    vin,\n    vrefp,\n    vrefn,\n    vdd,\n    vss,\n    nbits=6,\n    cap_err_mean=0.0,\n    cap_mismatch=0.05,\n    comp_offset=0.0,\n    compute_residue=True,\n):\n    \"\"\"High-level circuit model of a Differential\n    VCM-Based Loop Unrolled Bottom-Plate Sampling\n    Successive Approximation Register (SAR) Analog-to-Digital Converter (ADC).\n\n    Args:\n        vin (_type_)    : Differential input voltage of the ADC.\n        vrefp (_type_)  : Positive reference voltage of the ADC.\n        vrefn (_type_)  : Negative reference voltage of the ADC.\n        vdd (_type_)    : Positive supply voltage of the ADC.\n        vss (_type_)    : Negative supply voltage of the ADC.\n        nbits (int, optional): Output digital word resolution. Defaults to 6.\n        cap_err_mean (float, optional): Mean value of the capacitor mismatch. Defaults to 0.0.\n        cap_mismatch (float, optional): Standard deviation of the capacitance (Capacitor mismatch). Defaults to 0.05.\n        comp_offset (float, optional): Standard deviation of the comparators' offsets. Defaults to 0.0.\n        compute_residue (bool, optional): Flag to enable/disable the programming of the last (residue) capacitor.\n            Programming the residue capacitor enables an additional conversion step,\n            used to sample the residue voltage at the end of the conversion to enable pipelining.\n            Defaults to True.\n\n    Returns:\n        dout_dec (_type_)   : Digital output of the ADC in decimal format.\n        dout (_type_)       : Digital output of the ADC in binary format.\n        charge_p (_type_)   : Charge at the positive array.\n        charge_n (_type_)   : Charge at the negative array.\n        vres (_type_)       : Residue voltage sampled at the end of the conversion.\n        vp (_type_)         : Node voltage at the positive array in each conversion step.\n        vn (_type_)         : Node voltage at the negative array in each conversion step.\n\n    \"\"\"\n    vcm = np.mean([vdd, vss])\n    vinp = (vin) / 2 + vcm\n    vinn = -(vin) / 2 + vcm\n\n    std_cap_err = cap_mismatch\n    num_caps_array = nbits - 2\n    num_unit_caps_array = 2**num_caps_array\n\n    # positive array\n    cap_err_even = normal(cap_err_mean, std_cap_err, num_unit_caps_array)\n    unittary_caps = np.ones(len(cap_err_even)) * (1 + cap_err_even)\n    pos_cap_even = np.array(\n        [unittary_caps[-1]]\n        + [\n            np.sum(unittary_caps[2**b - 1 : 2 ** (b + 1) - 1])\n            for b in range(num_caps_array)\n        ]\n    )\n    cap_err_odd = normal(cap_err_mean, std_cap_err, num_unit_caps_array)\n    unittary_caps = np.ones(len(cap_err_odd)) * (1 + cap_err_odd)\n    pos_cap_odd = np.array(\n        [unittary_caps[-1]]\n        + [\n            np.sum(unittary_caps[2**b - 1 : 2 ** (b + 1) - 1])\n            for b in range(num_caps_array)\n        ]\n    )\n\n    # negative array\n    cap_err_even = normal(cap_err_mean, std_cap_err, num_unit_caps_array)\n    unittary_caps = np.ones(len(cap_err_even)) * (1 + cap_err_even)\n    neg_cap_even = np.array(\n        [unittary_caps[-1]]\n        + [\n            np.sum(unittary_caps[2**b - 1 : 2 ** (b + 1) - 1])\n            for b in range(num_caps_array)\n        ]\n    )\n    cap_err_odd = normal(cap_err_mean, std_cap_err, 2 ** (nbits - 2))\n    unittary_caps = np.ones(len(cap_err_odd)) * (1 + cap_err_odd)\n    neg_cap_odd = np.array(\n        [unittary_caps[-1]]\n        + [\n            np.sum(unittary_caps[2**b - 1 : 2 ** (b + 1) - 1])\n            for b in range(num_caps_array)\n        ]\n    )\n\n    # put heavier capacitors at the beginning of the array\n    pos_cap_even = pos_cap_even[::-1]\n    pos_cap_odd = pos_cap_odd[::-1]\n    neg_cap_even = neg_cap_even[::-1]\n    neg_cap_odd = neg_cap_odd[::-1]\n\n    vcomps_err = normal(0, comp_offset, nbits)\n    vcomps = vcomps_err\n\n    # Each conversion has nbits + 2 steps:\n    # a) bottom-plate sampling phase\n    # b) one step per bit conversion\n    # c) residue voltage sampling\n    vp = np.zeros((nbits + 2, len(vinp)))\n    vn = np.zeros((nbits + 2, len(vinn)))\n\n    charge_p = np.zeros((len(vp), len(vinp)))\n    charge_n = np.zeros((len(vn), len(vinn)))\n\n    charge_vref = np.zeros((len(vp), len(vinp)))\n    charge_vcm = np.zeros((len(vp), len(vinp)))\n\n    pos_total_cap = np.sum(pos_cap_even) + np.sum(pos_cap_odd)\n    neg_total_cap = np.sum(neg_cap_even) + np.sum(neg_cap_odd)\n\n    # bottom-plate sampling phase\n    vp[0, :] = vcm\n    vn[0, :] = vcm\n    charge_p[0, :] = (vp[0, :] - vinp) * pos_total_cap\n    charge_n[0, :] = (vn[0, :] - vinn) * neg_total_cap\n    charge_vcm[0, :] = vcm * (neg_total_cap + pos_total_cap)\n\n    dout = np.zeros((nbits, len(vinp)))\n    dout_neg = np.zeros((nbits, len(vinp)))\n\n    # conversion steps: MSB to second-to-last bit\n    for k in range(0, nbits - 1):\n        cap_vrefp_pos = (\n            np.sum(pos_cap_odd[k:])\n            + dout[:-2, :].T @ pos_cap_even[:-1].reshape(-1, 1)\n            + dout[:-2, :].T @ pos_cap_odd[:-1].reshape(-1, 1)\n        )\n        cap_vrefn_pos = (\n            np.sum(pos_cap_even[k:])\n            + dout_neg[:-2, :].T @ pos_cap_even[:-1].reshape(-1, 1)\n            + dout_neg[:-2, :].T @ pos_cap_odd[:-1].reshape(-1, 1)\n        )\n\n        cap_vrefp_pos = cap_vrefp_pos.T\n        cap_vrefn_pos = cap_vrefn_pos.T\n\n        vp[k + 1, :] = (\n            charge_p[k, :] + vrefp * cap_vrefp_pos + vrefn * cap_vrefn_pos\n        ) / pos_total_cap\n        charge_p[k + 1, :] = (vp[k + 1, :] - vrefp) * cap_vrefp_pos + (\n            vp[k + 1, :] - vrefn\n        ) * cap_vrefn_pos\n\n        cap_vrefp_neg = (\n            np.sum(neg_cap_odd[k:])\n            + dout_neg[:-2, :].T @ neg_cap_even[:-1].reshape(-1, 1)\n            + dout_neg[:-2, :].T @ neg_cap_odd[:-1].reshape(-1, 1)\n        )\n        cap_vrefn_neg = (\n            np.sum(neg_cap_even[k:])\n            + dout[:-2, :].T @ neg_cap_even[:-1].reshape(-1, 1)\n            + dout[:-2, :].T @ neg_cap_odd[:-1].reshape(-1, 1)\n        )\n\n        cap_vrefp_neg = cap_vrefp_neg.T\n        cap_vrefn_neg = cap_vrefn_neg.T\n\n        vn[k + 1, :] = (\n            charge_n[k, :] + vrefp * cap_vrefp_neg + vrefn * cap_vrefn_neg\n        ) / neg_total_cap\n        charge_n[k + 1, :] = (vn[k + 1, :] - vrefp) * cap_vrefp_neg + (\n            vn[k + 1, :] - vrefn\n        ) * cap_vrefn_neg\n\n        dout[k, :] = vp[k + 1, :] - vn[k + 1, :] &amp;lt; vcomps[k]\n        dout_neg[k, :] = 1 - dout[k, :]\n\n        charge_vref[k + 1, :] = (vp[k + 1, :] - vrefp) * cap_vrefp_pos + (\n            vn[k + 1, :] - vrefp\n        ) * cap_vrefp_neg\n\n    # LSB conversion: only one capacitor commutes per array\n    k += 1\n    cap_vrefp_pos = (\n        pos_cap_odd[-1]\n        + dout[:-1, :].T @ pos_cap_even.reshape(-1, 1)\n        + dout[:-2, :].T @ pos_cap_odd[:-1].reshape(-1, 1)\n    )\n    cap_vrefn_pos = dout_neg[:-1, :].T @ pos_cap_even.reshape(-1, 1) + dout_neg[\n        :-2, :\n    ].T @ pos_cap_odd[:-1].reshape(-1, 1)\n\n    cap_vrefp_pos = cap_vrefp_pos.T\n    cap_vrefn_pos = cap_vrefn_pos.T\n\n    vp[k + 1, :] = (\n        charge_p[k, :] + vrefp * cap_vrefp_pos + vrefn * cap_vrefn_pos\n    ) / pos_total_cap\n    charge_p[k + 1, :] = (vp[k + 1, :] - vrefp) * cap_vrefp_pos + (\n        vp[k + 1, :] - vrefn\n    ) * cap_vrefn_pos\n\n    cap_vrefp_neg = (\n        neg_cap_odd[-1]\n        + dout_neg[:-1, :].T @ neg_cap_even.reshape(-1, 1)\n        + dout_neg[:-2, :].T @ neg_cap_odd[:-1].reshape(-1, 1)\n    )\n    cap_vrefn_neg = dout[:-1, :].T @ neg_cap_even.reshape(-1, 1) + dout[\n        :-2, :\n    ].T @ neg_cap_odd[:-1].reshape(-1, 1)\n\n    cap_vrefp_neg = cap_vrefp_neg.T\n    cap_vrefn_neg = cap_vrefn_neg.T\n\n    vn[k + 1, :] = (\n        charge_n[k, :] + vrefp * cap_vrefp_neg + vrefn * cap_vrefn_neg\n    ) / neg_total_cap\n    charge_n[k + 1, :] = (vn[k + 1, :] - vrefp) * cap_vrefp_neg + (\n        vn[k + 1, :] - vrefn\n    ) * cap_vrefn_neg\n\n    dout[k, :] = vp[k + 1, :] - vn[k + 1, :] &amp;lt; vcomps[k]\n    dout_neg[k, :] = 1 - dout[k, :]\n\n    charge_vref[k + 1, :] = (vp[k + 1, :] - vrefp) * cap_vrefp_pos + (\n        vn[k + 1, :] - vrefp\n    ) * cap_vrefp_neg\n\n    k += 1\n    vp[k + 1, :] = vp[k, :]\n    vn[k + 1, :] = vn[k, :]\n    charge_p[k + 1, :] = charge_p[k, :]\n    charge_n[k + 1, :] = charge_n[k, :]\n    if compute_residue:\n        # Residue voltage sampling: program the residue capacitor asymmetrically\n        # so that vres = vp - vn is obtained correctly, enabling pipelining.\n        cap_vrefp_neg = (\n            (dout_neg[-1, :] * neg_cap_odd[-1]).reshape((-1, 1))\n            + dout_neg[:-1, :].T @ neg_cap_even.reshape(-1, 1)\n            + dout_neg[:-2, :].T @ neg_cap_odd[:-1].reshape(-1, 1)\n        )\n        cap_vrefn_neg = (\n            (dout[-1, :] * neg_cap_odd[-1]).reshape((-1, 1))\n            + dout[:-1, :].T @ neg_cap_even.reshape(-1, 1)\n            + dout[:-2, :].T @ neg_cap_odd[:-1].reshape(-1, 1)\n        )\n\n        cap_vrefp_neg = cap_vrefp_neg.T\n        cap_vrefn_neg = cap_vrefn_neg.T\n\n        vn[k + 1, :] = (\n            charge_n[k, :] + vrefp * cap_vrefp_neg + vrefn * cap_vrefn_neg\n        ) / neg_total_cap\n        charge_n[k + 1, :] = (vn[k + 1, :] - vrefp) * cap_vrefp_neg + (\n            vn[k + 1, :] - vrefn\n        ) * cap_vrefn_neg\n\n        charge_vref[k + 1, :] = (vp[k + 1, :] - vrefp) * cap_vrefp_pos + (\n            vn[k + 1, :] - vrefp\n        ) * cap_vrefp_neg\n\n    vres = vp[-1, :] - vn[-1, :]\n    dout_dec = bin2dec(dout.T, width=nbits, reverse=False)\n\n    charge_vref = np.diff(charge_vref, axis=0)\n    charge_vcm = np.diff(charge_vcm, axis=0)\n\n    return dout_dec, dout.T, charge_vref, charge_vcm, vres, vp, vn\n</code></pre> <pre><code>def residue_amplifier(\n    vin,\n    vdd,\n    vss,\n    gain=8.0,\n    offset=0.0,\n    settling_factor=5,\n):\n    \"\"\"High-level circuit model of a residue amplifier.\n    This model includes a settling factor to account for\n    the settling time of the amplifier. The settling factor\n    is SF = Ta/Tau, where Ta is the available amplification time\n    (which usually corresponds to half of the period of the sampling clock)\n    of the amplifier and Tau is the time constant of the amplifier,\n    corresponding to the inverse of the amplifier's bandwidth.\n    Args:\n        vin (ndarray)    : Input voltage of the amplifier.\n        vdd (ndarray)    : Positive supply voltage of the amplifier.\n        vss (ndarray)    : Negative supply voltage of the amplifier.\n        gain (float, optional): Gain of the amplifier. Defaults to 8.0.\n        offset (float, optional): Offset of the amplifier. Defaults to 0.0.\n        settling_factor (int, optional): Settling factor of the amplifier. Defaults to 5.\n\n    Returns:\n        vout (ndarray)   : Output voltage of the amplifier.\n    \"\"\"\n    vout = gain * vin * (1 - np.exp(-settling_factor)) + offset\n    vout[vout &amp;gt; vdd] = vdd\n    vout[vout &amp;lt; vss] = vss\n    return vout\n</code></pre> <pre><code># CONTROL VARIABLES FOR ALL SIMULATIONS\nNUM_CIRCUITS = 1000  # number of circuits in monte carlo simulation\nCAP_MISMATCH_MEAN = 0.0\nCAP_MISMATCH_STD = 0.01  # 1% Capacitance mismatch\nNUM_COMP_OFSTS = 5\nMAX_COMP_OFST = 5 * mV\n</code></pre> <pre><code>first_sub_adc_avg_lin = []\n\ncomp_offsets = np.linspace(0, MAX_COMP_OFST, NUM_COMP_OFSTS)\ncap_err_mean = CAP_MISMATCH_MEAN\ncap_mismatch = CAP_MISMATCH_STD\n\nfor comp_offset in comp_offsets:\n    first_sub_adc_lins = []\n    for _ in range(NUM_CIRCUITS):\n        (\n            dout_dec_first,\n            _,\n            charge_vref_first,\n            charge_vcm_first,\n            vres_first,\n            vp_first,\n            vn_first,\n        ) = loop_unrolled_bottom_plate_sampling_monotonic_splitcap_sar_adc(\n            vin=vin1,\n            vrefp=vrefp_first,\n            vrefn=vrefn_first,\n            vdd=vdd,\n            vss=vss,\n            nbits=Nbits_first,\n            cap_err_mean=cap_err_mean,\n            cap_mismatch=cap_mismatch,\n            comp_offset=comp_offset,\n            compute_residue=True,\n        )\n        # get the transition voltages of the first sub ADC\n        dout_first = dout_dec_first[np.where(np.diff(dout_dec_first) &amp;gt; 0)]\n        vtrans_first = vin1[np.where(np.diff(dout_dec_first) &amp;gt; 0)]\n\n        vlsb_measured_first = (vtrans_first[-1] - vtrans_first[0]) / (\n            2**Nbits_first - 2\n        )\n        dnl_first = np.diff(vtrans_first) / vlsb_measured_first - 1\n        inl_first = np.array([0] + np.cumsum(dnl_first).tolist())\n        adc_linearity = Nbits_first - np.log2(np.max(inl_first) - np.min(inl_first))\n        first_sub_adc_lins.append(adc_linearity)\n\n    first_sub_adc_avg_lin.append(np.mean(first_sub_adc_lins))\n</code></pre> <pre><code># residue voltage of the first sub SAR-ADC vs. input voltage\nfig, ax = plt.subplots(2, 1, figsize=(6, 6))\nax[0].plot(vin1, vres_first / mV)\nax[0].set_xlabel(\"input voltage [V]\")\nax[0].set_ylabel(\"residue voltage [mV]\")\nax[0].set_title(\"First sub ADC Residue Voltage\")\nax[0].grid(True)\nax[1].plot(vin1, np.sum(charge_vref_first, axis=0), label=r\"$Q_{VREF}$\", color=\"black\")\nax[1].plot(vin1, np.sum(charge_vcm_first, axis=0), label=r\"$Q_{VCM}$\", color=\"orange\")\nax[1].set_xlabel(\"input voltage [V]\")\nax[1].set_ylabel(\"normalized charge [C/C]\")\nax[1].set_title(\"First sub ADC Total Reference Drawn Charge\")\nax[1].grid(True)\nplt.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(figsize=(4, 4))\nax.step(\n    vtrans_first,\n    dout_first,\n    label=\"first sub ADC transfer function\",\n    color=\"black\",\n    where=\"pre\",\n)\nax.set_xlabel(\"input voltage [V]\")\nax.set_ylabel(\"digital output\")\nax.set_title(\"First sub ADC transfer function\")\nax.grid(True)\nplt.tight_layout()\n</code></pre> <pre><code># plot the INL and DNL of the first sub ADC\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(dout_first[:-1], dnl_first, label=\"DNL\")\nax[0].set_xlabel(\"code\")\nax[0].set_ylabel(\"DNL [LSB]\")\nax[0].set_title(\"DNL of the first sub ADC\")\nax[0].grid(True)\nax[1].step(dout_first, inl_first, label=\"INL\")\nax[1].set_xlabel(\"code\")\nax[1].set_ylabel(\"INL [LSB]\")\nax[1].set_title(\"INL of the first sub ADC\")\nax[1].grid(True)\nplt.tight_layout()\n</code></pre> <pre><code># plot the linearity histogram of the first sub ADC\nfig, ax = plt.subplots(figsize=(4, 3))\nsns.histplot(\n    first_sub_adc_lins,\n    ax=ax,\n    kde=True,\n    stat=\"probability\",\n    color=\"gray\",\n    label=\"first sub ADC linearity\",\n)\nax.set_xlabel(\"Linearity (Bit)\")\nax.set_ylabel(\"Probability density\")\nax.set_title(\"Histogram of the first sub ADC linearity\")\nplt.tight_layout()\n</code></pre> <pre><code># observe the average linearity of the first sub ADC\n# against comparator offset\nfig, ax = plt.subplots(figsize=(4, 3))\nsns.barplot(x=comp_offsets / mV, y=first_sub_adc_avg_lin, color=\"black\", ax=ax)\nax.set_xlabel(\"Comparator Offset [mV]\")\nax.set_ylabel(\"ENOB (Average)\")\nax.set_title(\n    \"First sub ADC Average Linearity \\n Vs.\\n Comparator Offset Standard Deviation\"\n)\nax.set_xticklabels(np.round(comp_offsets / mV, 2), rotation=45)\nax.grid(True)\nplt.tight_layout()\n</code></pre> <pre>\n<code>/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33619/1566581067.py:10: UserWarning: set_ticklabels() should only be used with a fixed number of ticks, i.e. after set_ticks() or using a FixedLocator.\n  ax.set_xticklabels(np.round(comp_offsets / mV, 2), rotation=45)\n</code>\n</pre> <pre><code>second_sub_adc_avg_lins = []\n\ncomp_offsets = np.linspace(0, MAX_COMP_OFST, NUM_COMP_OFSTS)\ncap_err_mean = CAP_MISMATCH_MEAN\ncap_mismatch = CAP_MISMATCH_STD\n\nvin2 = vin1 / second_adc_gain\n\nfor comp_offset in comp_offsets:\n    second_sub_adc_lins = []\n    for _ in range(NUM_CIRCUITS):\n        (\n            dout_dec_second,\n            _,\n            charge_vref_second,\n            charge_vcm_second,\n            vres_second,\n            vp_second,\n            vn_second,\n        ) = loop_unrolled_bottom_plate_sampling_monotonic_splitcap_sar_adc(\n            vin=vin2,\n            vrefp=vrefp_second,\n            vrefn=vrefn_second,\n            vdd=vdd,\n            vss=vss,\n            nbits=Nbits_second,\n            cap_err_mean=cap_err_mean,\n            cap_mismatch=cap_mismatch,\n            comp_offset=comp_offset,\n            compute_residue=True,\n        )\n        # get the transition voltages of the second sub ADC\n        dout_second = dout_dec_second[np.where(np.diff(dout_dec_second) &amp;gt; 0)]\n        vtrans_second = vin2[np.where(np.diff(dout_dec_second) &amp;gt; 0)]\n\n        vlsb_measured_second = (vtrans_second[-1] - vtrans_second[0]) / (\n            2**Nbits_second - 2\n        )\n        dnl_second = np.diff(vtrans_second) / vlsb_measured_second - 1\n        inl_second = np.array([0] + np.cumsum(dnl_second).tolist())\n        adc_linearity_second = Nbits_second - np.log2(\n            np.max(inl_second) - np.min(inl_second)\n        )\n        second_sub_adc_lins.append(adc_linearity_second)\n\n    second_sub_adc_avg_lins.append(np.mean(second_sub_adc_lins))\n</code></pre> <pre><code># residue voltage of the second sub SAR-ADC vs. input voltage\nfig, ax = plt.subplots(2, 1, figsize=(4, 3))\nax[0].plot(vin2 / mV, vres_second / mV)\nax[0].set_xlabel(\"input voltage [mV]\")\nax[0].set_ylabel(\"residue voltage [mV]\")\nax[0].set_title(\"2nd sub ADC Residue Voltage\")\nax[0].grid(True)\nax[1].plot(\n    vin2 / mV, np.sum(charge_vref_second, axis=0), label=r\"$Q_{VREF}$\", color=\"black\"\n)\nax[1].plot(\n    vin2 / mV, np.sum(charge_vcm_second, axis=0), label=r\"$Q_{VCM}$\", color=\"orange\"\n)\nax[1].set_xlabel(\"input voltage [mV]\")\nax[1].set_ylabel(\"normalized\\ncharge [C/C]\")\nax[1].set_title(\"2nd sub ADC Total Reference Drawn Charge\")\nax[1].grid(True)\nplt.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(figsize=(6, 6))\nax.step(\n    vtrans_second / mV,\n    dout_second,\n    label=\"2nd sub ADC transfer function\",\n    color=\"black\",\n    where=\"pre\",\n)\nax.set_xlabel(\"input voltage [mV]\")\nax.set_ylabel(\"digital output\")\nax.set_title(\"2nd sub ADC transfer function\")\nax.grid(True)\nplt.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(dout_second[:-1], dnl_second, label=\"DNL\")\nax[0].set_xlabel(\"code\")\nax[0].set_ylabel(\"DNL [LSB]\")\nax[0].set_title(\"DNL of the 2nd sub ADC\")\nax[0].grid(True)\nax[1].step(dout_second, inl_second, label=\"INL\")\nax[1].set_xlabel(\"code\")\nax[1].set_ylabel(\"INL [LSB]\")\nax[1].set_title(\"INL of the 2nd sub ADC\")\nax[1].grid(True)\nplt.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(figsize=(4, 3))\nsns.histplot(\n    second_sub_adc_lins,\n    ax=ax,\n    kde=True,\n    stat=\"probability\",\n    color=\"gray\",\n    label=\"2nd sub ADC linearity\",\n)\nax.set_xlabel(\"Linearity (Bit)\")\nax.set_ylabel(\"Probability density\")\nax.set_title(\"Histogram of the 2nd sub ADC linearity\")\nplt.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(figsize=(4, 3))\nsns.barplot(x=comp_offsets / mV, y=second_sub_adc_avg_lins, color=\"black\", ax=ax)\nax.set_xlabel(\"Comparator Offset [mV]\")\nax.set_ylabel(\"ENOB (Average)\")\nax.set_title(\n    \"2nd sub ADC Average Linearity \\n Vs.\\n Comparator Offset Standard Deviation\"\n)\nax.set_xticklabels(np.round(comp_offsets / mV, 2), rotation=45)\nax.grid(True)\nplt.tight_layout()\n</code></pre> <pre>\n<code>/var/folders/58/9x4h23m57hl3nj4g9kjnldhw0000gn/T/ipykernel_33619/3165211460.py:8: UserWarning: set_ticklabels() should only be used with a fixed number of ticks, i.e. after set_ticks() or using a FixedLocator.\n  ax.set_xticklabels(np.round(comp_offsets / mV, 2), rotation=45)\n</code>\n</pre> <pre><code>adc_avg_lins = []\n\ncomp_offsets = np.linspace(0, MAX_COMP_OFST, 1)\ncap_err_mean = CAP_MISMATCH_MEAN\ncap_mismatch = CAP_MISMATCH_STD\n\nNum_gers = 11\nmax_ger = 0.03\nra_gers = np.linspace(-max_ger, max_ger, Num_gers)\n\nfor ger in ra_gers:\n    for comp_offset in comp_offsets:\n        adc_lins = []\n\n        for _ in range(NUM_CIRCUITS):\n            \"\"\" FIRST STAGE \"\"\"\n            (\n                dout_dec_first,\n                dout_bin_first,\n                charge_vref_first,\n                charge_vcm_first,\n                vres_first,\n                vp_first,\n                vn_first,\n            ) = loop_unrolled_bottom_plate_sampling_monotonic_splitcap_sar_adc(\n                vin=vin1,\n                vrefp=vrefp_first,\n                vrefn=vrefn_first,\n                vdd=vdd,\n                vss=vss,\n                nbits=Nbits_first,\n                cap_err_mean=cap_err_mean,\n                cap_mismatch=cap_mismatch,\n                comp_offset=comp_offset,\n                compute_residue=True,\n            )\n\n            \"\"\" RESIDUE AMPLIFIER \"\"\"\n            # Gain is inverted because the residue voltage is inverted\n            # at the output of the first quantizer stage\n            ra_gain = -(1 + ger) * (2 ** (Nbits_first - 1) / second_adc_gain)\n            vout_ra = residue_amplifier(\n                vres_first - 0.0 * np.mean(vres_first),\n                vdd,\n                -vdd,\n                gain=ra_gain,\n                offset=0.0,\n                settling_factor=100,\n            )\n\n            \"\"\" SECOND STAGE \"\"\"\n            (\n                dout_dec_second,\n                dout_bin_second,\n                _,\n                _,\n                vres_second,\n                vp_second,\n                vn_second,\n            ) = loop_unrolled_bottom_plate_sampling_monotonic_splitcap_sar_adc(\n                vin=vout_ra,\n                vrefp=vrefp_second,\n                vrefn=vrefn_second,\n                vdd=vdd,\n                vss=vss,\n                nbits=Nbits_second,\n                cap_err_mean=cap_err_mean,\n                cap_mismatch=cap_mismatch,\n                comp_offset=comp_offset,\n                compute_residue=True,\n            )\n\n            dout_dec = dout_dec_first * 2 ** (Nbits_second - 1) + (\n                dout_dec_second - 2 ** (Nbits_second - 2)\n            )\n\n            dout = dout_dec[np.where(np.diff(dout_dec) &amp;gt; 0)]\n            vtrans = vin1[np.where(np.diff(dout_dec) &amp;gt; 0)]\n\n            vlsb_measured = (vtrans[-1] - vtrans[0]) / (\n                2 ** (Nbits_first + Nbits_second - 1) - 2\n            )\n            dnl = np.diff(vtrans) / vlsb_measured - 1\n            inl = np.array([0] + np.cumsum(dnl).tolist())\n            adc_linearity = (\n                Nbits_first - 1 + Nbits_second - np.log2(np.max(inl) - np.min(inl))\n            )\n            adc_lins.append(adc_linearity)\n\n        adc_avg_lins.append(np.mean(adc_lins))\n</code></pre> <pre><code># observe vout of the residue amplifier versus vres 1st stage\nfig, ax = plt.subplots(1, 1, figsize=(4, 3))\nax.plot(vres_first / mV, vout_ra / mV, label=\"vout_ra\", color=\"black\")\nax.set_xlabel(\"Residue 1st Stage [mV]\")\nax.set_ylabel(\"Residue Amplifier Output [mV]\")\nax.set_title(\"Residue Amplifier Output\")\nprint(\"Total Amp RA: \", (np.max(vout_ra) - np.min(vout_ra)) / mV, \" mV\")\nprint(\"RA Gain: \", ra_gain, \" V/V\")\n</code></pre> <pre>\n<code>Total Amp RA:  273.2263943094236  mV\nRA Gain:  -8.24  V/V\n</code>\n</pre> <pre><code># plot the voltage input of the first stage vs the voltage output\nfig, ax = plt.subplots(2, 1, figsize=(4, 5))\nax[0].plot(vin1 / mV, vres_first / mV, color=\"blue\")\nax[1].plot(vin1 / mV, vres_second / mV, color=\"orange\")\nax[0].set_xlabel(\"Vin (mV)\")\nax[0].set_ylabel(\"Residue 1st Stage (mV)\")\nax[1].set_xlabel(\"Vout Residue Amplifier (mV)\")\nax[1].set_ylabel(\"Residue 2nd Stage (mV)\")\nax[0].grid(True)\nax[1].grid(True)\nfig.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(figsize=(6, 6))\nax.step(\n    vtrans,\n    dout,\n    label=\"2-step ADC transfer function\",\n    color=\"black\",\n    where=\"pre\",\n)\nax.set_xlabel(\"input voltage [mV]\")\nax.set_ylabel(\"digital output\")\nax.set_title(\"2-step ADC transfer function\")\nax.grid(True)\nplt.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].step(dout[:-1], dnl, label=\"DNL\")\nax[0].set_xlabel(\"code\")\nax[0].set_ylabel(\"DNL [LSB]\")\nax[0].set_title(\"DNL of the 2-step ADC\")\nax[0].grid(True)\nax[1].step(dout, inl, label=\"INL\")\nax[1].set_xlabel(\"code\")\nax[1].set_ylabel(\"INL [LSB]\")\nax[1].set_title(\"INL of the 2-step ADC\")\nax[1].grid(True)\nplt.tight_layout()\n</code></pre> <pre><code>fig, ax = plt.subplots(figsize=(4, 3))\nsns.histplot(\n    adc_lins,\n    ax=ax,\n    kde=True,\n    stat=\"probability\",\n    color=\"gray\",\n    label=\"2-step ADC linearity\",\n)\nax.set_xlabel(\"Linearity (Bit)\")\nax.set_ylabel(\"Probability density\")\nax.set_title(\"Histogram of the 2-step ADC linearity\")\nplt.tight_layout()\n</code></pre> <pre><code># observe the linearity against GER\nfig, ax = plt.subplots(1, 1, figsize=(4, 4))\nax.plot(ra_gers * 100, adc_avg_lins, color=\"black\", lw=2, marker=\"D\", mfc=\"None\")\nax.set_xlabel(\"Gain Error Ratio (GER) (%)\")\nax.set_ylabel(\"Average Linearity (Bits)\")\nax.set_title(\"Average Linearity vs. GER\\n@1% Capacitor Mismatch\")\nax.grid(True)\nfig.tight_layout()\n</code></pre> <pre><code>cap_err_mean = CAP_MISMATCH_MEAN\ncap_mismatch = CAP_MISMATCH_STD\n\n\"\"\" FIRST STAGE \"\"\"\n(dout_dec_first_t, _, _, _, vres_first_t, *_) = (\n    loop_unrolled_bottom_plate_sampling_monotonic_splitcap_sar_adc(\n        vin=vin1t,\n        vrefp=vrefp_first,\n        vrefn=vrefn_first,\n        vdd=vdd,\n        vss=vss,\n        nbits=Nbits_first,\n        cap_err_mean=cap_err_mean,\n        cap_mismatch=cap_mismatch,\n        comp_offset=comp_offset,\n        compute_residue=True,\n    )\n)\n\n\"\"\" RESIDUE AMPLIFIER \"\"\"\n# Gain is inverted because the residue voltage is inverted\n# at the output of the first quantizer stage\nra_gain = -(1 + ger) * (2 ** (Nbits_first - 1) / second_adc_gain)\nvout_ra_t = residue_amplifier(\n    vres_first_t,\n    vdd,\n    -vdd,\n    gain=ra_gain,\n    offset=0.0,\n    settling_factor=100,\n)\n\n\"\"\" SECOND STAGE \"\"\"\n(dout_dec_second_t, *_) = (\n    loop_unrolled_bottom_plate_sampling_monotonic_splitcap_sar_adc(\n        vin=vout_ra_t,\n        vrefp=vrefp_second,\n        vrefn=vrefn_second,\n        vdd=vdd,\n        vss=vss,\n        nbits=Nbits_second,\n        cap_err_mean=cap_err_mean,\n        cap_mismatch=cap_mismatch,\n        comp_offset=comp_offset,\n        compute_residue=True,\n    )\n)\n\ndout_dec_t = dout_dec_first_t * 2 ** (Nbits_second - 1) + (\n    dout_dec_second_t - 2 ** (Nbits_second - 2)\n)\n\n# center the output around 0 and normalize\ndout_dec_t = (dout_dec_t - 2 ** (Nbits_first + Nbits_second - 2)) / (\n    2 ** (Nbits_first + Nbits_second - 1)\n)\n</code></pre> <pre><code>NUM_HARMONICS = 9\nNUM_SIGNAL_BINS = 1\n\nffte = FFTEngine(dout_dec_t, time=tD)\n*_, dout_p_f = ffte.fft(Np, fs=Fs, window=\"rectangular\")\n</code></pre> <pre>\n<code>/Users/gfdasdias/Documents/Personal/signal-converters-notebooks/practical_classes/fft.py:108: UserWarning: fs - sampling frequency will be ignored since a time axis is provided.\n  warn(\n</code>\n</pre> <pre><code># plot the FFT of the output\nfig, ax = plt.subplots(2, 1, figsize=(4, 4))\nax[0].plot(ffte.ff / MHz, 20 * np.log10(np.abs(dout_p_f)), color=\"gray\")\nax[0].set_xlabel(\"Frequency (MHz)\")\nax[0].set_ylabel(\"Magnitude (dB)\")\nax[0].set_title(\"FFT of the ADC Output\")\nax[0].grid(True)\nax[1].plot(\n    ffte.ff[ffte.ff &amp;gt; 1] / MHz,\n    20 * np.log10(np.abs(dout_p_f[ffte.ff &amp;gt; 1])),\n    color=\"gray\",\n)\nax[1].set_xlabel(\"Frequency (MHz)\")\nax[1].set_ylabel(\"Magnitude (dB)\")\nax[1].set_title(\"FFT of the ADC Output\")\nax[1].grid(True)\nfig.tight_layout()\n</code></pre> <pre><code>spectral_analysis = ffte.spectral_analysis(\n    span=NUM_SIGNAL_BINS, harmonics=NUM_HARMONICS\n)\n\nprint(\"Spectral Analysis:\")\nprint(\"    SNDR : {:.2f} dB\".format(spectral_analysis[\"sndr\"]))\nprint(\"    ENOB : {:.2f} bits\".format(spectral_analysis[\"enob\"]))\nprint(\"    SFDR : {:.2f} dB\".format(spectral_analysis[\"sfdr\"]))\nprint(\"    THD  : {:.2f} dB\".format(spectral_analysis[\"thd\"]))\nprint(\"    SNR  : {:.2f} dB\".format(spectral_analysis[\"snr\"]))\n</code></pre> <pre>\n<code>Spectral Analysis:\n    SNDR : 58.48 dB\n    ENOB : 9.42 bits\n    SFDR : 65.72 dB\n    THD  : 63.34 dB\n    SNR  : 60.20 dB\n</code>\n</pre>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#practical-project-1-modelling-of-a-2-step-sar-based-adc","title":"Practical Project 1 \u2014 Modelling of a 2-Step SAR-based ADC","text":"<p>In this first project the goal is to develop a high-level model of a 2-step pipeline ADC based on sub SAR-ADCs. The sub ADCs feature loop-unrolling, using a comparator for each bit extraction in an asynchronous fashion for each successive bit, reducing the timing diagram of the SAR-ADCs to a single conversion period and a single reset period.</p>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#sub-adc-operation","title":"Sub-ADC Operation","text":"<p>The operation of the sub loop-unrolled SAR-ADC must be understood in order to proceed with the modelling activity. A few important notes can be taken from the figure above:</p> <ol> <li> <p>The SAR-ADC uses an asynchronous comparator for the extraction of each bit \u2014 the loop-unrolling technique. This differs from the classical architecture of a single comparator for all bits.</p> </li> <li> <p>The SAR-ADC behaves like a VCM-Based SAR ADC BUT IT IS NOT! This SAR ADC is a Monotonic SAR-ADC, but because it uses a split capacitor array in the intrinsic DAC, it behaves like a VCM-Based SAR ADC.</p> </li> <li> <p>The differential architecture of the SAR-ADC enables to extract the Most Significant Bit for free right after sampling, without using the DAC capacitive arrays, using comparator D5.</p> </li> <li> <p>The SAR-ADC is performing bottom-sampling to sample the input differential signal, and as such, the first step after sampling is in charge of transposing the input signal from the bottom plate to the top plate of the capacitors of the DAC array, where the comparators are connected to.</p> </li> <li> <p>The capacitive DAC arrays feature a split-capacitor architecture, unfolding the capacitance of each element in half. This enables the split capacitor to have one of its bottom plates connected to VREF- and the other to VREF+ at all times during bit extraction \u2014 thus allowing the voltage at the top plate of the capacitor array to stay relatively stable around the common mode voltage VCM. The stability of the top plate voltage around VCM is very important to maintain the correct operation and operating point of the comparators D&lt;5:0&gt;.</p> </li> <li> <p>After each bit decision, if the current bit is a logical \"1\", then both of the bottom plates of the corresponding positive(top)-array split-capacitors are connected to VREF+, and consequently the bottom plates of the corresponding negative(bottom)-array split-capacitor are connected to VREF-, symmetrically. This will pull the top plate voltage of the positive capacitor array upwards, and pull the top plate voltage of the negative capacitor array downwards, adjusting the decision scale for each comparator. The symmetric operation happens if the current bit is a logical \"0\".</p> </li> <li> <p>Because the LSB capacitor is not split, if the current bit is a logical \"1\", then the bottom plate of the corresponding positive(top)-array unitary LSB capacitor will be connected to VREF+, and consequently the bottom plate of the corresponding negative(bottom)-array unitary LSB capacitor is connected to VREF-, symmetrically. The symmetric operation happens if the current bit is a logical \"0\".</p> </li> <li> <p>The Residue Capacitor is a unitary capacitor that will be programmed by the last bit (the LSB). If the LSB is a logical \"1\", then the bottom plate of the corresponding positive(top)-array unitary Residue capacitor will be connected to VREF+, but the bottom plate of the corresponding negative(bottom)-array unitary Residue capacitor WILL NOT be programmed to VREF-! If the LSB is a logical \"0\", then the bottom plate of the corresponding negative(bot)-array unitary Residue capacitor will be connected to VREF-, but the bottom plate of the corresponding positive(top)-array unitary Residue capacitor WILL NOT be programmed to VREF+! This asymmetric operation allows Vres = VCP - VCN to be obtained correctly \u2014 symmetric programming would double the residue voltage.</p> </li> </ol>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#timing-diagram-of-sub-sar-adc","title":"Timing Diagram of sub SAR-ADC","text":""},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#enable-noise-simulation","title":"ENABLE NOISE SIMULATION","text":""},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#first-sub-sar-adc-transfer-function","title":"First Sub SAR-ADC Transfer Function","text":"<p>The general analog to digital transfer function Dout(Vin) of the sub SAR-ADC can be derived using the charge conservation principle (CCP) to derive the voltage at the top plate of the capacitor arrays in each step of the conversion process.</p> <p>There are two main approaches:</p> <ul> <li>Long form: Applies the CCP to the sampling phase and each corresponding phase of each extracted bit, taking into account all the capacitors connected to each array.</li> <li>Short form: Applies the CCP to each pair of successive phases of each bit extraction, taking into account only the capacitance associated with the changed bottom plate connections \u2014 simplifying the analysis when writing the code.</li> </ul>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#sar-adc-conversion-phases","title":"SAR ADC Conversion Phases","text":"<p>The <code>loop_unrolled_bottom_plate_sampling_monotonic_splitcap_sar_adc</code> function models the conversion in <code>nbits + 2</code> discrete phases:</p> <ol> <li> <p>Bottom-plate sampling: The input differential voltage is sampled at the bottom plates of the capacitor arrays while the top plates are held at VCM. The initial charge on each array is set by this phase.</p> </li> <li> <p>Bit conversion \u2014 MSB to second-to-last bit (for loop): For each bit <code>k</code> from 0 to <code>nbits \u2212 2</code>, the capacitor bottom plates are commuted between VREFP and VREFN according to all previous bit decisions. The new top-plate voltage is compared against VCM (plus the comparator offset) to decide the current bit.</p> </li> <li> <p>LSB conversion: The unsplit LSB capacitor commutes in one array while the residue capacitor in the odd array remains pre-connected to VREFP. This is handled separately from the main loop because only one switch commutes per array.</p> </li> <li> <p>Residue voltage sampling (optional): When <code>compute_residue=True</code>, the residue capacitor of the negative array is programmed asymmetrically to expose the residue voltage Vres = Vp \u2212 Vn at the top-plate nodes, making it available for the second stage.</p> </li> </ol>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#first-sub-adc-high-level-model","title":"First Sub-ADC High-Level Model","text":""},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#residue-amplifier-transfer-function","title":"Residue Amplifier Transfer Function","text":"<p>The residue amplifier amplifies the residue voltage Vres from the first sub-ADC and feeds it into the second sub-ADC. The gain is set to <code>-(2^(Nbits_first - 1) / second_adc_gain)</code> (negative, because the first-stage residue is inverted). A settling factor models incomplete settling: <code>Vout = G * Vin * (1 - exp(-SF))</code>, where SF = Ta/\u03c4.</p>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#monte-carlo-simulation-control-parameters","title":"Monte Carlo Simulation Control Parameters","text":"<p>The following control variables govern all Monte Carlo simulations. Capacitor mismatch is modelled as a Gaussian distribution with zero mean and a standard deviation of 1% (<code>CAP_MISMATCH_STD = 0.01</code>). Comparator offsets are swept from 0 to <code>MAX_COMP_OFST</code> across <code>NUM_COMP_OFSTS</code> steps.</p>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#first-sub-adc-monte-carlo-analysis","title":"First Sub-ADC Monte Carlo Analysis","text":"<p>The first sub-ADC (6-bit) is evaluated by running <code>NUM_CIRCUITS</code> independent Monte Carlo trials for each comparator offset standard deviation value. For each trial, the DNL and INL are computed from the measured transition voltages of the transfer function, and the achievable ENOB is derived from the peak INL.</p>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#second-sub-adc-monte-carlo-analysis","title":"Second Sub-ADC Monte Carlo Analysis","text":"<p>The second sub-ADC (7-bit, operating on the residue-amplified output of the first stage) is evaluated under the same Monte Carlo conditions as the first sub-ADC. Its input range is scaled by <code>1/second_adc_gain</code> relative to the first stage.</p>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#full-2-step-adc-monte-carlo-analysis","title":"Full 2-Step ADC Monte Carlo Analysis","text":"<p>The complete 2-step pipeline ADC is now simulated via Monte Carlo to evaluate linearity as a function of residue amplifier gain error (GER). The gain error sweeps from \u22123% to +3%, and for each gain error value the linearity (ENOB) is averaged across all Monte Carlo trials.</p>"},{"location":"notebooks/project1_loopunrolled_pipeline_sar_adc/#full-2-stage-pipeline-adc-spectral-analysis","title":"Full 2-Stage Pipeline ADC \u2014 Spectral Analysis","text":"<p>The full 2-stage pipeline ADC is now simulated on a sinusoidal input to evaluate spectral performance. The output digital code is reconstructed by combining the first and second sub-ADC outputs, then passed through the <code>FFTEngine</code> to compute SNR, SNDR, ENOB, SFDR, and THD.</p>"},{"location":"reference/fft/","title":"FFT Engine Reference","text":"<p>Source: <code>practical_classes/fft.py</code></p> <p>Spectral analysis engine for converter output evaluation.</p>"},{"location":"reference/fft/#fftengine","title":"<code>FFTEngine</code>","text":"<p>A dataclass that computes the FFT of a signal and extracts spectral performance metrics.</p>"},{"location":"reference/fft/#constructor","title":"Constructor","text":"<pre><code>FFTEngine(signal, time=None, unit=\"V\")\n</code></pre> <ul> <li>signal \u2014 Input signal array (e.g., ADC/DAC output)</li> <li>time \u2014 Time axis array (optional)</li> <li>unit \u2014 Signal unit label (default: <code>\"V\"</code>)</li> </ul>"},{"location":"reference/fft/#typical-usage","title":"Typical Usage","text":"<pre><code>engine = FFTEngine(signal)\nengine.fft(n_points=1024, fs=1e6, window=\"blackmanharris\")\nengine.compute_harmonics(fin=1e3, fs=1e6, n_harmonics=5)\nmetrics = engine.spectral_analysis()\n</code></pre>"},{"location":"reference/fft/#core-methods","title":"Core Methods","text":""},{"location":"reference/fft/#fftn_points-fsnone-windowrectangular","title":"<code>fft(n_points, fs=None, window=\"rectangular\")</code>","text":"<p>Compute the power spectrum of the signal.</p> <ul> <li>n_points \u2014 Number of FFT points</li> <li>fs \u2014 Sampling frequency</li> <li>window \u2014 Window function: <code>\"rectangular\"</code>, <code>\"hanning\"</code>, <code>\"hamming\"</code>, <code>\"blackmanharris\"</code>, <code>\"blackman\"</code>, <code>\"gaussian\"</code>, <code>\"kaiser\"</code>, <code>\"cosine\"</code>, <code>\"parzen\"</code></li> </ul>"},{"location":"reference/fft/#compute_harmonicsfin-fs-n_harmonics5","title":"<code>compute_harmonics(fin, fs, n_harmonics=5)</code>","text":"<p>Identify harmonic bins and power levels from the computed spectrum.</p> <ul> <li>fin \u2014 Input signal frequency</li> <li>fs \u2014 Sampling frequency</li> <li>n_harmonics \u2014 Number of harmonics to extract</li> </ul>"},{"location":"reference/fft/#spectral_analysis","title":"<code>spectral_analysis()</code>","text":"<p>Compute all spectral metrics at once. Returns a dictionary with all metrics below.</p>"},{"location":"reference/fft/#metric-getters","title":"Metric Getters","text":"<p>Call these after <code>fft()</code> and <code>compute_harmonics()</code>:</p> Method Returns Unit <code>get_snr()</code> Signal-to-Noise Ratio dB <code>get_sfdr()</code> Spurious-Free Dynamic Range dB <code>get_sndr()</code> Signal-to-Noise-and-Distortion Ratio dB <code>get_thd()</code> Total Harmonic Distortion dB <code>get_enob()</code> Effective Number of Bits bits <code>get_h2()</code> 2nd Harmonic Distortion dB <code>get_h3()</code> 3rd Harmonic Distortion dB"},{"location":"reference/fft/#supported-windows","title":"Supported Windows","text":"Window Key Rectangular (none) <code>\"rectangular\"</code> Hanning <code>\"hanning\"</code> Hamming <code>\"hamming\"</code> Blackman-Harris <code>\"blackmanharris\"</code> Blackman <code>\"blackman\"</code> Gaussian <code>\"gaussian\"</code> Kaiser <code>\"kaiser\"</code> Cosine <code>\"cosine\"</code> Parzen <code>\"parzen\"</code>"},{"location":"reference/utils/","title":"Utilities Reference","text":"<p>Source: <code>practical_classes/utils.py</code></p> <p>Core converter modeling functions shared across all notebooks.</p>"},{"location":"reference/utils/#binarydecimal-conversion","title":"Binary/Decimal Conversion","text":""},{"location":"reference/utils/#bin2decx-width-reversefalse","title":"<code>bin2dec(x, width, reverse=False)</code>","text":"<p>Convert a binary NumPy array (or array of binary arrays) to decimal value(s).</p> <ul> <li>x \u2014 Binary array of 0s and 1s, or 2D array of binary words</li> <li>width \u2014 Bit width of each binary word</li> <li>reverse \u2014 If <code>True</code>, treat input as LSB-first (default: MSB-first)</li> </ul>"},{"location":"reference/utils/#dec2binx-width-reversefalse","title":"<code>dec2bin(x, width, reverse=False)</code>","text":"<p>Convert a decimal value (or array of decimals) to binary NumPy array(s).</p> <ul> <li>x \u2014 Integer or array of integers</li> <li>width \u2014 Bit width of the output binary word(s)</li> <li>reverse \u2014 If <code>True</code>, output in LSB-first order</li> </ul>"},{"location":"reference/utils/#dac-transfer-functions","title":"DAC Transfer Functions","text":""},{"location":"reference/utils/#ideal_dacvref-n_bits","title":"<code>ideal_dac(vref, n_bits)</code>","text":"<p>Returns a callable <code>f(Din)</code> implementing the transfer function of an ideal DAC.</p> <ul> <li>vref \u2014 Reference voltage</li> <li>n_bits \u2014 Resolution in bits</li> <li>Returns \u2014 <code>lambda x: bin2dec(x, n_bits) * vlsb</code></li> </ul>"},{"location":"reference/utils/#adc-transfer-functions","title":"ADC Transfer Functions","text":""},{"location":"reference/utils/#ideal_adcvref-nbits-roundf","title":"<code>ideal_adc(vref, nbits, roundf)</code>","text":"<p>Returns a callable <code>f(Vin)</code> implementing the transfer function of an ideal ADC.</p> <ul> <li>vref \u2014 Reference voltage</li> <li>nbits \u2014 Resolution in bits</li> <li>roundf \u2014 Rounding function (<code>np.round</code>, <code>np.floor</code>, or <code>np.ceil</code>)</li> </ul>"},{"location":"reference/utils/#nonideal_adcvref-n_bits-ofst0-gain1-vnq0-roundfnpround","title":"<code>nonideal_adc(vref, n_bits, ofst=0, gain=1, vnq=0, roundf=np.round)</code>","text":"<p>Returns <code>(transfer_function, vtrans)</code> for a non-ideal ADC with linear errors.</p> <ul> <li>ofst \u2014 Offset error voltage</li> <li>gain \u2014 Gain error factor</li> <li>vnq \u2014 Quantization noise voltage level</li> <li>Returns \u2014 Tuple of (callable, transition voltages array)</li> </ul>"},{"location":"reference/utils/#binary-arithmetic","title":"Binary Arithmetic","text":""},{"location":"reference/utils/#binadda-b","title":"<code>binadd(a, b)</code>","text":"<p>Binary word addition. Returns <code>dec2bin(bin2dec(a) + bin2dec(b), max_width)</code>.</p>"},{"location":"reference/utils/#binsuba-b","title":"<code>binsub(a, b)</code>","text":"<p>Binary word subtraction. Returns <code>dec2bin(bin2dec(a) - bin2dec(b), max_width)</code>.</p>"},{"location":"reference/utils/#pipeline-adc-support","title":"Pipeline ADC Support","text":""},{"location":"reference/utils/#digital_error_correctiondouts-scale_factors-reversefalse-bintrue","title":"<code>digital_error_correction(douts, scale_factors, reverse=False, bin=True)</code>","text":"<p>Perform digital error correction for pipeline ADC stages.</p> <ul> <li>douts \u2014 List of output code arrays from each pipeline stage</li> <li>scale_factors \u2014 List of scale factors between stages</li> <li>reverse \u2014 Output word bit order</li> <li>bin \u2014 If <code>True</code>, return binary; otherwise return decimal</li> </ul>"}]}